---
priority: 10
scope: path-scoped
paths:
  - ".claude/commands/**"
  - ".claude/rules/**"
  - ".claude/skills/**"
  - ".claude/hooks/**"
  - ".claude/bin/**"
  - ".claude/agents/management/**"
  - ".claude/audit-fixtures/**"
  - ".claude/sync-manifest.yaml"
  - ".claude/codex-mcp-guard/**"
  - "tools/verify-overlays.sh"
---

# Self-Referential /codify Discipline

A `/codify` execution is **self-referential** when its proposal touches the loom-side artifacts that govern codification itself — commands like `/codify` / `/sync` / `/redteam`, the skills backing them, the rules governing artifact quality and trust posture, the hooks detecting violations, the bin scripts validating output. On those surfaces the agent is authoring rules that constrain its own next behavior, so a single-pass shipment can silently propagate a defect into every future session that loads the rule.

Per posture defaults (`.claude/skills/32-trust-posture/redteam-integration.md`), convergence (2 consecutive clean rounds) is posture-invariant, but the per-round audit DEPTH scales with posture — at L5*DELEGATED each round is mechanical-sweeps-only. On a non-self-referential surface that lighter per-round depth is the right trade-off — the blast radius is bounded by the artifact and the next session's deeper gate catches what a shallow round missed. On the self-referential surface that mechanical-only depth ships defects into the gate the next session would otherwise use to catch them. The forest-ledger arc (journal/0089–0098) cost 7 redteam rounds + a mid-arc Option A→B redesign before the substring-mask failure class was structurally closed — and per journal/0098 §FD #3: *"7 rounds of evidence say the single-pass L5 default would have shipped the R1 substring-mask silently."\_ (Pre-F119, that L5 default skipped Round 1 entirely; F119 made convergence posture-invariant, so the residual L5 gap is now the shallow per-round depth — which this rule closes below.)

This rule overrides the posture-scaled per-round DEPTH for that specific surface, and ONLY that surface — raising every round to the full multi-agent team regardless of posture — with a positive allowlist that names every file the gate covers.

## MUST Rules

### 1. Self-Referential /codify MUST Carry Multi-Agent Redteam-With-Tests Regardless Of Posture

When a `/codify` proposal touches ANY file matching the self-referential surface allowlist below, the orchestrator MUST dispatch a multi-agent redteam-with-tests round before merging the codify, EVEN AT L5_DELEGATED posture, where the posture-default per-round depth would otherwise be mechanical-sweeps-only. The team MUST include at minimum: a code-review specialist (reviewer agent), a security specialist (security-reviewer), and a structural-validator specialist (cc-architect for artifact-quality surface, gold-standards-validator for naming/licensing surface, analyst for contract-design surface). The round runs in parallel — sequential single-agent review does NOT satisfy the multi-agent requirement.

The dispatched team MUST receive the proposal AND its receipt-journal entry AND the originating evidence (the journal entries the codify cites as motivation). Each team member reports findings independently; cross-agent disagreement on CRIT/HIGH MUST be resolved per `.claude/skills/32-trust-posture/redteam-integration.md` § Round-N — Cross-Agent CRIT/HIGH Disagreement Resolution (resolve by construction, never by averaging severities).

```markdown
# DO — /codify touches a self-referential surface file → multi-agent redteam runs in parallel before merge

Surface touched: .claude/rules/trust-posture.md (allowlist match)
Dispatched team (parallel): reviewer, security-reviewer, cc-architect, analyst
Each receives: proposal + receipt journal + originating evidence
Cross-agent verdicts reconciled by construction against the contract
Merge proceeds only when ≥0 genuine CRIT/HIGH across all four

# DO NOT — surface touched, posture L5, ship with mechanical-only per-round depth

Surface touched: .claude/rules/cc-artifacts.md (allowlist match)
Posture: L5_DELEGATED → per-round depth = mechanical-sweeps-only → skip the multi-agent team
Merge proceeds with no independent multi-agent review
(this is exactly the failure mode the rule blocks)
```

**BLOCKED rationalizations:**

- "Posture is L5, the per-round depth is mechanical-only — the self-ref surface needs no deeper review"
- "The codify is small (one bullet, one line), redteam is overkill"
- "The proposal only touches one allowlist file, the surface is narrow"
- "I am the orchestrator, I already reviewed the proposal as I drafted it"
- "The receipt journal documents the rationale, that satisfies independent review"
- "Multi-agent costs ~30–60 min of agent time; the codify ships value now"
- "Cross-agent dispatch was the prior session's rule — this session's posture overrides it"
- "Sequential review by one specialist after another is functionally equivalent"
- "The next session will catch it under its own gate"
- "If anything slips, the regression-within-grace mechanism downgrades us"
- "The rule's own bootstrap-circularity carve-out lets us skip"
  (the carve-out is one-time-per-rule per Rule 2 below, NOT a per-session shortcut)

**Why:** A defect in a self-referential artifact is a load-bearing defect for every session that loads it; the cost of letting one slip is N future sessions paying the substring-mask cost the forest-ledger arc paid. Multi-agent parallel review at one wall-clock unit (per `rules/agents.md` § Parallel Execution) is the structural defense against the failure mode the arc surfaced; sequential or single-agent review re-introduces the same blind spot.

### 2. Self-Referential Surface Is A Positive Allowlist (per `cc-artifacts.md` Rule 10)

The self-referential surface MUST be enumerated as a positive allowlist below. Files NOT in the allowlist are outside the gate. New files added to `.claude/` under categories the allowlist enumerates (a new rule under `.claude/rules/` that governs codify-class behavior, a new hook under `.claude/hooks/lib/` that detects violations) MUST be added to the allowlist in the SAME `/codify` that lands them — undeclared new self-referential files outside the allowlist is BLOCKED.

The allowlist (load-bearing paths only; edge cases at the boundary resolve in favor of the gate firing — Phase-1 false-positive cost is bounded to one extra redteam round):

- **Commands:** `.claude/commands/codify.md`, `.claude/commands/sync.md`, `.claude/commands/sync-to-build.md`, `.claude/commands/redteam.md`, `.claude/commands/sweep.md`, `.claude/commands/wrapup.md`, `.claude/commands/onboard.md` (added F14 M8: `/onboard` is the deterministic read-path that initializes a new operator's view of the codify-lease + team-memory state per M7; modifying it modifies a load-bearing precondition of every subsequent `/codify` an operator runs), `.claude/commands/certify.md` (added F30-followup 2026-05-26: `/certify` is the knowledge-gate that precedes `/claim` and roster registration per `multi-operator-coordination.md` §1; modifying it modifies a load-bearing precondition of every subsequent `/codify` an operator runs — same predicate as `/onboard`, included per Rule 2 boundary clause "edge cases at the boundary resolve in favor of the gate firing"), `.claude/commands/cc-audit.md` (added 2026-05-31 per F99: `/cc-audit` IS the CC-artifact quality gate — every `/codify` is validated through its rubric (Phase-0 mechanical battery + adversarial A/B + composed verdict). A regression to the rubric silently weakens the gate every future `/codify` runs through; same load-bearing-precondition predicate as `/onboard`/`/certify`. Its paired agent `cc-architect.md` is already covered by `cc-artifacts.md` Rule 6 + the Management-agents/structural-validator surface.), `.claude/commands/journal.md` (added 2026-06-07 per GH #382: the `/journal` command CREATES journal entries, and journal DECISION receipts ARE codify-class output — every `/codify` (and every `wave-loop.md` G2 capture step) ships a journal entry through this command's template. A regression to the emitted frontmatter or the `## For Discussion` carve-out silently re-introduces the rule⊥command divergence #382 closed. Same load-bearing-precondition predicate as the already-allowlisted `journal-author-discipline.md` (which governs the `author:` field this command emits); named per the Rule 2 boundary clause "edge cases at the boundary resolve in favor of the gate firing.")
- **Skills (codify-discipline):** `.claude/skills/spec-compliance/**`, `.claude/skills/command-authoring/**`, `.claude/skills/skill-authoring/**`, `.claude/skills/hook-authoring/**`, `.claude/skills/sweep/**`, `.claude/skills/32-trust-posture/**`, `.claude/skills/30-claude-code-patterns/**` (F20 2026-05-22: depth-files of allowlisted rules are codify-governing by extension — covers `worktree-orchestration.md` + `closure-parity-specialist-discipline.md` + `genesis-migration-n1-org-admin-anchor.md` (F87 2026-05-29: MUST-7 depth-file; multi-operator-coordination.md is allowlisted, so its depth-file is codify-governing by extension) + future `agents.md`-companion sub-files; pattern-add per `cc-artifacts.md` Rule 10 positive-allowlist + analyst FM4 resolve-by-construction; resolves the pre-existing `worktree-orchestration.md` gap retroactively), `.claude/skills/12-testing-strategies/probe-driven-verification.md` (added 2026-06-06 wave-loop institutionalization: the eval-harness depth-file backing `commands/redteam.md` Step 4b's create/maintain/use MUST. A regression to the accretion procedure or the probe-schema contract silently weakens the eval-harness gate every future `/redteam` runs through; same depth-file-of-an-allowlisted-surface predicate as the `30-claude-code-patterns/**` entries. Scoped to the single probe-driven-verification depth-file, NOT all of `12-testing-strategies/**`, to keep the firing scope minimal. Per Rule 2 boundary clause "edge cases at the boundary resolve in favor of the gate firing.")
- **Rules (codify-discipline):** `.claude/rules/{trust-posture,cc-artifacts,coc-sync-landing,artifact-flow,recommendation-quality,value-prioritization,autonomous-execution,agents,sweep-completeness,rule-authoring,variant-authoring,cross-cli-parity,specs-authority,spec-accuracy,probe-driven-verification,hook-output-discipline,verify-resource-existence,time-pressure-discipline,repo-scope-discipline,self-referential-codify,knowledge-convergence,multi-operator-coordination,user-flow-validation,governed-throughput,journal,journal-author-discipline,state-file-write-guard,wave-loop,verify-claims-before-write}.md`
  - `verify-claims-before-write.md` (2026-06-11 Gate-1 ingest, R1 cc-architect HIGH-1): codify-governing — its durable-artifact surface explicitly includes codified rules + journal entries, and every `/codify` ships both; its MUST clauses fire on `/codify` output. Same "every `/codify` ships prose deliverables / journal entries" predicate as the allowlisted `journal-author-discipline.md` + `user-flow-validation.md`. Added in the same codify that lands it per Rule 2. (Sibling `evidence-first-claims.md` is DELIBERATELY omitted: it governs claim-grammar in all session output, not codify-class artifacts specifically — omission decision recorded in the Gate-1 receipt journal.)
  - `journal.md` (2026-06-07 per GH #382): codify-governing — it is the format/frontmatter contract for journal-class artifacts, and journal DECISION receipts ARE codify-class output (every `/codify` + every `wave-loop.md` G2 capture ships one). The rule's canonical-frontmatter + scoped-`## For Discussion` MUST clauses fire on every `/codify`-output entry — same self-referential-surface predicate as the sibling `journal-author-discipline.md` (which governs the `author:` field) and `user-flow-validation.md` (which governs prose deliverables every `/codify` produces). Added in the same codify that reconciles it per Rule 2.
  - `wave-loop.md` (2026-06-06 wave-loop institutionalization): UNCONDITIONALLY codify-governing — it binds the per-wave `redteam → codify → (re-)todos → implement` loop (MUST-2 G1–G5), and G2 IS a codify-class step. Any `/codify` editing this rule modifies the wave-gated execution loop `/codify` itself runs through (a multi-wave loom codify runs its own inter-wave gates). Added in the same codify that lands it per Rule 2.
  - `journal-author-discipline.md` (F101-3 2026-06-01): codify-governing — it governs the author-VERIFIABILITY layer of journal-class artifacts; every `/codify` ships journal DECISION entries, and the rule's MUST clauses fire on each entry's `author:` claim against the live provenance ledger. Same self-referential-surface predicate as `user-flow-validation.md` (governs prose deliverables every `/codify` produces). Added in the same codify that lands it per Rule 2.
  - `governed-throughput.md` (F110 2026-06-01): UNCONDITIONALLY codify-governing — it governs how an orchestrator delegates parallel / orchestrated shards (curated rule-slice injection + the full-context merge gate). `/codify`'s own multi-agent redteam dispatches parallel agents; this rule decides whether those agents carry the governing invariants, so any `/codify` touching it modifies the delegation-governance gate `/codify` itself runs through. Added in the same codify that lands it per Rule 2.
  - `knowledge-convergence.md` (F14 M8 Shard F-1): UNCONDITIONALLY codify-governing — it codifies the codify-lease + team-memory promotion + `/onboard` deterministic read-path. Any `/codify` touching this rule modifies the gate `/codify` itself runs through, so the self-referential-surface allowlist MUST cover it.
  - `multi-operator-coordination.md` (F14 M8 Shard F-2): UNCONDITIONALLY codify-governing — Gate-1 classified as codify-governing 2026-05-22 per R1 reviewer Tension-2 disposition. The rule's claim system (Rule 2/MUST-2) gates SAME-class edits across ALL paths including codify-class artifacts (`.claude/.proposals/latest.yaml`, `learning-codified.json`, rules, commands, skills); the gate matrix (MUST-3, §6.4) governs `/release` via `operator-gate.js`; the lifecycle hooks (§5) surface "rules-changed" + "codify lease holder" at session-start (both codify-discipline state). The rule's `/codify`-governance is structural, not adjacent.
  - `user-flow-validation.md` (co-owner-directed 2026-05-22, journal/0134): UNCONDITIONALLY codify-governing — MUST-4 explicitly governs prose deliverables (rules, commands, skills), every `/codify` ships prose deliverables, the rule's MUST clauses fire on every `/codify`-output artifact. Re-opening the rule's prescriptions IS a codify-governing edit.
  - `onboard.md` (F14 M7 INTEGRATION-NOTES forward note per journal/0132): codify-governing IFF the rule's body authors a discipline that governs `/onboard`'s deterministic read-path interaction with `/codify` (the lease + team-memory promotion both touch onboard's read-path). Default disposition: include the command in the Commands sub-allowlist below. See guide-extract for the per-condition adjudication walkthrough.
  - `state-file-write-guard.md` (F123 2026-06-03): codify-governing — it is the documented CONTRACT for the Bash state-file-mutation detector (`violation-patterns.js::detectStateFileMutation`) + the file-tool tier classifier (`state-file-write-guard.js`), both backing the allowlisted `validate-bash-command.js` trust-substrate hook. Rule 5's MUST NOT ("Modify the validator, hook, rule, spec… without updating the others atomically") means a `/codify` editing this rule's layer-coverage contract MUST move the impl + tests in lockstep — exactly the multi-agent gate this rule fires. Added in the same codify (F123) that inverted the trust-posture state-file protection to hook-primary, per Rule 2 boundary clause "edge cases at the boundary resolve in favor of the gate firing."
- **Hooks:** `.claude/hooks/lib/{violation-patterns,detect-violations,state-io,state-resolver,template-resolver,workspace-utils,operator-id,genesis-ceremony,fold-rule-9c,fold-genesis-anchor,coordination-log,provenance-author-backing,owner-add-ceremony,owner-depart-ceremony,reap-ceremony,fold-rule-10,fold-rule-reap,derive-n,vcs-provider,vcs-azure-adapter,vcs-github-adapter,ado-login,ado-api-allowlist,roster-schema-validate,journal-reserve,instruct-and-wait,provenance-event,coc-emit,codify-lease,journal-body-anchor,tool-classes}.js` + `.claude/hooks/{fold-amendment-paired-with-helper,genesis-anchor-guard,validate-bash-command,journal-write-guard,provenance-capture-tool,posture-gate,signing-mutation-guard,operator-gate}.js` (`provenance-author-backing.js` + `journal-write-guard.js` added 2026-06-01 per F101-3 / loom#411: `provenance-author-backing.js::checkAuthorBacking` IS the author-VERIFIABILITY detector and `journal-write-guard.js` is the PreToolUse hook that fires its branch on every new journal-entry Write. A regression to the backing check (e.g., reading the frontmatter claim instead of the live ledger, counting a non-`HumanInput` kind, or downgrading the unbacked/undetermined disposition to silent passthrough) silently re-trusts unverified `author:human` claims — the exact mis-attribution `journal-author-discipline.md` exists to prevent. `/codify` itself ships journal DECISION entries through this hook, so it governs codify-class output: same load-bearing-precondition class as the allowlisted `genesis-anchor-guard.js`, named per the Rule 2 boundary clause "edge cases at the boundary resolve in favor of the gate firing." `validate-bash-command.js` added 2026-05-31 per F92 / issue #401 Shard 3: it is the PreToolUse Bash tripwire that enforces `git.md`'s destructive-working-tree-op MUST clause (`git reset --hard` block + `git clean -f` halt-and-report). A regression to its segment-anchoring or severity discipline either hard-blocks every bash command or silently lets an untracked-file-deleting command escape the tripwire — the exact #401 data-loss class; same load-bearing class as the allowlisted `genesis-anchor-guard.js`, named per the Rule 2 boundary clause "edge cases at the boundary resolve in favor of the gate firing." `operator-id.js` added 2026-05-27 per #366 redteam cc-architect M1: `resolveIdentity` is the load-bearing precondition for posture → codify-lease → `/onboard`, same predicate as the allowlisted `onboard.md`/`certify.md`; a fingerprint-resolution bug here silently mis-resolves identity → wrong posture → wrong lease behavior, so per Rule 2 boundary clause it is inside the gate. `genesis-ceremony.js` + `fold-rule-9c.js` + `coordination-log.js` + the top-level `fold-amendment-paired-with-helper.js` hook added 2026-05-29 per F88 cc-architect LOW-1: these are the genesis-migration trust-root substrate — the ceremony helper, its fold predicate, the fold engine, and the paired-landing gate. A bug here silently mis-anchors the trust root or false-flags the owner as an equivocator. They were previously in-gate only by the Rule 2 boundary clause "edge cases resolve in favor of the gate firing"; naming them closes the boundary-clause-judgment gap so future edits fire the gate deterministically. `genesis-anchor-guard.js` added 2026-05-30 per F72 / issue #379: it is the PreToolUse trust-root enforcement hook — its fresh-consumer-vs-enrolled-then-deleted discrimination (the F72 branch) decides whether a commit is blocked, so a bug here either hard-blocks every consumer commit or escapes the guard entirely; same trust-root-substrate class as `genesis-ceremony.js`/`fold-rule-9c.js`, named per the same boundary-clause-closure rationale. `fold-genesis-anchor.js` added 2026-06-03 per F122 Shard 2b R1 cc-architect MED: it is the genesis-ENROLLMENT fold engine — the sibling of the already-listed `fold-rule-9c.js` (migration fold) — and decides whether an enrollment genesis-anchor establishes the trust root. Shard 2a extended it with the ADO `content.provider` dispatch + `_resolveOwnerPersonAdo` owner-bind; a bug here silently mis-anchors or fails to anchor the enrollment trust root. Identical load-bearing class to `fold-rule-9c.js`; the allowlist asymmetry (migration fold named, enrollment fold not) was the gap — closed here per the Rule 2 same-codify obligation on the F122 branch that landed the 2a/2b extensions. `owner-add-ceremony.js` + `owner-depart-ceremony.js` + `reap-ceremony.js` + `fold-rule-10.js` + `fold-rule-reap.js` + `derive-n.js` + `vcs-provider.js` + `vcs-azure-adapter.js` + `vcs-github-adapter.js` + `ado-login.js` + `ado-api-allowlist.js` + `roster-schema-validate.js` added 2026-06-03 per F122 Shard 2c/4 R1 cc-architect HIGH-1: these are the owner-lifecycle / reap / derived-N / VCS-provider trust-substrate — the ceremonies that emit signed `collaborator-distinctness-attestation` / `-revocation` / `reap` records, their fold predicates (`fold-rule-10` revocation-contest, `fold-rule-reap` reaper/cosigner distinctness), the `derive-n` owner-quorum count backing the N=1 dispositions, the provider registry + adapters that construct the signed-record capture shapes the folds trust, and the roster-schema validator (the trust-root validation contract, sibling of the already-listed `operators.roster.schema.json`). A bug in any silently mis-binds a reap, false-settles a revocation, mis-counts the owner quorum, or mis-shapes a capture the fold then mis-validates — identical load-bearing class to the genesis hooks above. Shard 2c extended all six lib ceremony/fold files + the four `vcs-*`/`ado-*` adapter files with the provider dispatch; per the Rule 2 same-codify obligation + boundary clause "edge cases at the boundary resolve in favor of the gate firing," they are named here in the codify that landed the extensions, closing the same allowlist-asymmetry gap the 2b `fold-genesis-anchor.js` addition closed. `provenance-capture-tool.js` added 2026-06-04 per F101 item 1 / loom#411: the cross-CLI provenance capture surface whose `kind:"X"` string literals are the F101-4 validator's AUTHORITATIVE source-grep (`validate-emit.mjs::extractHookKinds`/`hookEmitsKind`) AND the runtime mechanism every `sync-manifest.yaml provenance_parity` `wired` cell asserts exists. A regression to its `classify()` dispatch silently breaks every wired parity cell (via the cc_capture byte-drift check) or mis-captures the governance record — same load-bearing class as the allowlisted `journal-write-guard.js`/`provenance-author-backing.js` F101-3 siblings; named per the Rule 2 same-codify obligation on the F101-item-1 branch that extended it. `journal-reserve.js` added 2026-06-07 per GH #382 R2 cc-architect CRIT: its `VALID_TYPES` set is the RUNTIME authority that gates the journal `type:` at reservation time — the canonical journal command (allowlisted in this same codify) MANDATES `reserveJournalSlot` and `journal-write-guard.js` (already allowlisted) reads its reservation records. A type-vocabulary edit to `rules/journal.md` that does NOT also extend `VALID_TYPES` produces a rule⊥helper divergence that hard-throws on the blessed type (the exact #382 R2 CRIT — AMENDMENT blessed in the rule but rejected by the helper). Same load-bearing-precondition class as the allowlisted `journal-write-guard.js`; named per the Rule 2 same-codify obligation so future journal type-vocabulary edits force its review. `posture-gate.js` + `signing-mutation-guard.js` + `operator-gate.js` added 2026-06-08 per FF-AC6-1 (Codex `apply_patch` gating): these are the 3 STATELESS trust gates carrying the `@coc-codex-edit-gate` marker that the `extract-policies.mjs` extractor reads to fan them out to the Codex `apply_patch` enforcement lane. The marker is a behavior-shaping self-referential-surface edit — it changes what each hook is licensed to gate on a Codex consumer — so a regression to the marker (removing it → the gate silently drops off the Codex file-edit lane; or a coordination guard wrongly gaining it → it halts every Codex edit absent the substrate) is exactly the load-bearing-precondition class the allowlisted `validate-bash-command.js`/`genesis-anchor-guard.js` cover. Named per the Rule 2 same-codify obligation + boundary clause "edge cases at the boundary resolve in favor of the gate firing," so a future edit to any of the 3 gates (or its marker) fires the multi-agent gate. `instruct-and-wait.js` added 2026-06-09 per loom #466: it is the canonical halting-hook output shape `hook-output-discipline.md` MUST-1 mandates — the SSOT every allowlisted halting hook (`validate-bash-command.js`, `posture-gate.js`, `genesis-anchor-guard.js`, `journal-write-guard.js`, `operator-gate.js`, `signing-mutation-guard.js`) emits through. A regression to its severity→delivery-channel dispatch silently degrades the structured agent handoff fleet-wide across every allowlisted halting hook at once — strictly higher load-bearing class than any single consuming hook; named per the Rule 2 same-codify obligation on the #466 branch that re-routed its delivery channels (the dropped-`validation`-field fix). `provenance-event.js` added 2026-06-09 per loom #461: the F101-1 byte-exact schema module (`EVENT_KINDS`/`EVENT_KEYS`/`canonicalSerialize` + `SCHEMA_VERSION`) the allowlisted `provenance-capture-tool.js`/`provenance-author-backing.js` siblings construct and validate records against, and the contract `sync-manifest.yaml::provenance_parity` (allowlisted) asserts; the allowlist asymmetry (capture + backing named, the schema they bind to not) was the gap, closed per the Rule 2 same-codify obligation on the #461 branch that landed the schema-freeze marker. `coc-emit.js` + `codify-lease.js` + `journal-body-anchor.js` added 2026-06-11 per FSUB (knowledge-convergence MUST-2/3 emitter wiring): `coc-emit.js::emitSignedRecord` is the SINGLE signed-record emitter every coordination-log writer routes through — it fills the per-emitter chain envelope (seq/prev_hash from the fold-derived chain head, refuse-don't-fork), signs canonical bytes, and appends under the 2KB cap; a regression here mis-chains or mis-signs EVERY emitted record at once (reservation, lease, body-anchor), the same fleet-wide class as the allowlisted `instruct-and-wait.js`. `codify-lease.js` is the `/codify` Step-0 concurrency gate (the on-disk mutex + the new `codify-lease`/`codify-lease-release` record emission) — a regression silently voids the lease discipline `knowledge-convergence.md` MUST-3 mandates on `/codify` itself. `journal-body-anchor.js::foldAnchorPredicate` is now default-registered in the fold engine (this codify), so a regression to its re-hash/tamper verdict silently mis-folds every anchor record fleet-wide. All three named per the Rule 2 same-codify obligation on the FSUB branch that made them load-bearing. `tool-classes.js` added 2026-06-12 per the MultiEdit-removal cycle (journal/0276, R1 cc-architect LOW-1): its `MUTATION_TOOLS` Set is the runtime mutation-tool classifier EVERY allowlisted guard hook (`posture-gate.js`, `signing-mutation-guard.js`, `adjacency-leasecheck`, `integrity-guard`, `genesis-anchor-guard.js`, `journal-write-guard.js`) consults via `isMutationTool()` — and post-journal/0276 it carries the load-bearing legacy-tolerance contract (MultiEdit retained for older-CC consumer payloads; "Do NOT re-add MultiEdit to settings.json"). A regression dropping a tool name silently un-guards that tool's writes across the entire allowlisted guard fleet at once — the same fleet-wide class as `instruct-and-wait.js`. Named per the Rule 2 same-codify obligation on the cycle that made its tolerance contract load-bearing. Sibling `github-login.js` was considered and deliberately OMITTED that same cycle: its SSOT constants are defense-in-depth sweep vocabulary (future-drift tripwires, not gate logic a codify changes behaviorally); the omission decision is recorded in the journal/0276 convergence AMENDMENT per the verify-claims-before-write omission-precedent shape.)
- **Data files (codify-class):** `.claude/operators.roster.schema.json` (added 2026-05-28 per F67 / GH #379 / journal/0162 security-reviewer MEDIUM-1: the roster schema is the trust-root validation contract — `roster-schema-validate.js::loadSchema()` reads it at runtime to validate every roster record submitted via `/whoami --register`; a weakened schema (e.g., dropped `propertyNames` prototype-pollution rejection, relaxed GPG fingerprint constraint, added `human_admin` synonym to `host_role` enum) silently accepts forged rosters. Same load-bearing class as the allowlisted hooks: a contract bug here silently mis-validates every downstream consumer's trust root. Per Rule 2 boundary clause "edge cases at the boundary resolve in favor of the gate firing.") · `.claude/sync-manifest.yaml` (added 2026-06-01 per F101-4 / loom#411 / journal/0204 cc-architect R1 MED: the manifest's `parity_enforcement` blocks — `cross_cli_drift_audit` AND `provenance_parity` (F101-4) — are codify-governing cross-CLI parity contracts that `validate-emit.mjs` + `cli-drift-audit.mjs` treat as authoritative and that gate `/sync`. A manifest-ONLY edit flipping a `provenance_parity` cell `deferred`→`wired` asserts a hook is registered in a lane emit target — a parity-declaration change the multi-agent redteam MUST review; without this entry such an edit would skip the gate because no `bin/`/`rules/` file is co-touched. Same authoritative-contract class as `operators.roster.schema.json`. Per Rule 2 boundary clause "edge cases at the boundary resolve in favor of the gate firing." The complementary structural defense is `validate-emit`'s own `provenance-parity` check, which verifies a `wired` declaration against the actual emit target at `/sync` — the two layers stack: the validator catches a wired-but-absent lie structurally; this gate catches the design judgment in a parity-declaration change.) · `.claude/disclosure-tenant-denylist.json` (added 2026-06-03 per journal/0214 / loom#411: the loom-only customer-identity / tenant token list that drives `bin/scan-synced-disclosure.mjs`'s `customer-identity-token` shape — the Gate-2 disclosure fence that gates `/sync`. A regression here (removing a customer token, corrupting the JSON) silently stops flagging that tenant's identity in the synced surface → a multi-tenant disclosure leak ships. Same authoritative-contract class as `operators.roster.schema.json` (a loom-only data file a codify-class `bin/` tool reads at runtime as its trust contract). The file is loom-only (never synced; `sync-manifest.yaml loom_only:`), so the regression's blast radius is loom's own Gate-2 fence — but that fence protects every consumer from receiving another tenant's identity. Per Rule 2 boundary clause "edge cases at the boundary resolve in favor of the gate firing.")
- **Bin (codify-class):** `.claude/bin/{validate-*,scan-synced-disclosure,emit-cli-artifacts,emit-coc,emit,compose,sync-tier-aware,sync-consumer-dryrun,check-sync-freshness}.mjs` + `.claude/bin/lib/cli-delivery.mjs` + `.claude/bin/lib/slot-parser.mjs` + `.claude/bin/lib/strip-build-internal.mjs` (`check-sync-freshness` added 2026-05-28 per F62 / journal/0164 cc-architect HIGH-1: the helper IS sync-class — it gates `/sync` and `/sync-to-build` distribution; a regression to its predicate silently false-passes the local-vs-remote freshness invariant. Same authority class as the existing `sync-tier-aware` / `sync-consumer-dryrun` siblings per Rule 2 boundary clause "edge cases at the boundary resolve in favor of the gate firing." `emit-coc` added 2026-06-02 per #392 / journal/0206 cc-architect R1 MED: the unified `.coc/` producer — `validate-emit.mjs::checkCocArtifactIds` invokes it as a `/sync` gate AND `migrate.md` Step 6 wires it into the migration path; same producer-authority class as the `emit-cli-artifacts` / `emit` siblings. A regression to id-derivation or the lock format silently ships a malformed `.coc/` to every consumer — naming it here fires the multi-agent redteam gate on the next `emit-coc.mjs` edit, per the Rule 2 same-codify obligation. `lib/cli-delivery.mjs` added 2026-06-08 per #408 AC#5-b: it is the SHARED `cli_delivery` lane-resolution parser (`CLI_DELIVERY_VALUES` / `deriveCliDelivery` / `checkRuleCliDelivery`) that BOTH `emit.mjs::validateCliDelivery` (Validator 18) AND `emit-cli-artifacts.mjs::emitRulesReferenceSkill` import — the single source of truth whose entire purpose is to PREVENT the divergent-parser failure class the AC#5-a R1 redteam closed. A regression here silently mis-resolves a rule's non-CC delivery lane (a path-scoped rule dropped from the rules-reference index, or a cc-only rule leaked into it) on BOTH the validator AND the emitter at once. Same authority class as the `emit` / `emit-cli-artifacts` callers that depend on it; named per the Rule 2 same-codify obligation so a future edit to the contract parser fires the multi-agent gate. `lib/slot-parser.mjs` + `lib/strip-build-internal.mjs` added 2026-06-10 per #473 / journal/0256 R1 cc-architect MED: `slot-parser.mjs::applyOverlay` is the slot-compose half AND `strip-build-internal.mjs::stripBuildInternalReferences` is the strip half that the allowlisted `sync-tier-aware.mjs` now imports IN-PROCESS to deploy `variants:` REPLACEMENT overlays byte-identically to `compose.mjs` / `tools/verify-overlays.sh`. The receipt's own lockstep Cons states it: "if `slot-parser` or `strip` changes, BOTH move together (they import the same module)." A regression to either silently mis-composes / mis-strips every `variants:` overlay shipped by the allowlisted Gate-2 distributor AND drifts it from the independent verifier — the exact same shared-parser-governs-codify-class-distribution class as `lib/cli-delivery.mjs`. Named per the Rule 2 same-codify obligation (this #473 codify is the one that makes them load-bearing) + boundary clause "edge cases at the boundary resolve in favor of the gate firing.")
- **Tools (sync-class, outside `.claude/`):** `tools/verify-overlays.sh` (added 2026-06-09 per #427 / journal/0252 cc-architect R1 MED: the independent post-sync auditor that verifies every `variants:` REPLACEMENT overlay AND (this codify) every `variant_only:<lang>` ADDITION landed byte-equal at its dest in every USE template — the AC2 "OR verify-overlays.sh" half of the #427 completeness gate. A regression to its `variant_only` pass (dropping the dest-root dispatch, the `variant_only-source-missing` branch, or the byte-equal compare) silently re-opens the #427 invisible-drift class on the VERIFICATION side, while the load-bearing in-tool gate lives in the allowlisted `sync-tier-aware.mjs`. Same sync-class authority as the allowlisted `scan-synced-disclosure.mjs` (a `/sync`-gating auditor); the only allowlist entry OUTSIDE `.claude/`, so its `paths:` glob is the explicit `tools/verify-overlays.sh` entry added above — broad-load `.claude/**` does NOT cover it. Per Rule 2 same-codify obligation + boundary clause "edge cases at the boundary resolve in favor of the gate firing.")
- **Codex MCP-guard (codify-class):** `.claude/codex-mcp-guard/{server.js,extract-policies.mjs}` (added 2026-06-04 per F101 item 1 / loom#440: the Codex enforcement surface. `extract-policies.mjs` is the validator-13 cross-CLI bijection driver (`CC_TO_CODEX_TOOLS` maps `Edit|Write`→`apply_patch`) per `multi-operator-coordination.md` MUST-6 + CONF-1; `server.js` now ALSO backs a `sync-manifest.yaml` `provenance_parity` `wired` cell — its `CAPTURE_HOOKS` + `CAPTURE_TOOLS` exports are the SSOT the F101-4 validator reads to verify `codex|Decision|wired|...@codex-mcp-guard`, and its `runProvenanceCapture` step is the runtime mechanism that wired cell asserts. A regression to the capture step or the `CAPTURE_HOOKS`/`CAPTURE_TOOLS` exports silently breaks codex·Decision capture — same load-bearing class as the allowlisted `provenance-capture-tool.js` it invokes. Per Rule 2 same-codify obligation (the cell flips wired in this codify) + boundary clause "edge cases at the boundary resolve in favor of the gate firing.")
- **Audit fixtures:** `.claude/audit-fixtures/**` (F23a 2026-05-22: broadened from `violation-patterns/**` per `cc-artifacts.md` Rule 10 positive-allowlist + cc-architect M1 finding — every audit-fixture dir backing a codify-allowlisted rule/hook/bin is codify-governing by extension; covers `violation-patterns/`, `proximity-band-budget/`, `cross-cli-drift/`, `codify-self-referential/`, `forest-ledger/`, `adjacency-leasecheck/`, `genesis-anchor-guard/`, and future fixture dirs without enumeration drift)
- **Management agents:** `.claude/agents/management/{coc-sync,sync-reviewer,repo-ops,settings-manager}.md`

**`paths:` frontmatter is the load-trigger SUPERSET; this allowlist is the firing-scope SUBSET.** This rule is `scope: path-scoped`; its `paths:` globs (`.claude/{commands,rules,skills,hooks,bin}/**`, `.claude/agents/management/**`, `.claude/audit-fixtures/**` — broadened F23a 2026-05-22 per the Audit-fixtures allowlist pattern-add) load the rule on ANY edit under those `.claude/` subtrees — deliberately broader than the named-file allowlist above. The allowlist is what the gate (Rule 1) FIRES on; the globs are only what LOADS the rule into context. Over-broad loading is fail-safe (an in-context rule that does not fire costs nothing). Narrowing the `paths:` globs to match the allowlist exactly is BLOCKED — it would drop the rule's load on a sibling surface and re-open the load-order gap that `commands/codify.md`'s gate directive closes. Every self-referential surface lives under `.claude/`, so the globs need no `workspaces/**` or filename-wildcard coverage (unlike `worktree-isolation.md`'s demotion).

**BLOCKED rationalizations:**

- "The file LOOKS self-referential but is too peripheral — skip the allowlist"
- "The allowlist is overcomprehensive, narrow it later"
- "A denylist is easier to maintain"
- "Adding the new file to the allowlist is the next codify's job"
- "The new rule under `.claude/rules/` doesn't really govern codify, it's adjacent"
- "Edge cases at the boundary should resolve in favor of NOT firing — the gate is too expensive"

**Why:** A denylist scales linearly with brainstormed exceptions and never closes the class (per `cc-artifacts.md` Rule 10). A positive allowlist closes the class on day one and surfaces drift loudly: every new self-referential file is either declared in the allowlist (gate covers it) or undeclared (this rule fires on the codify trying to ship it). Boundary-favoring-the-gate is the Phase-1 fail-closed default; tightening to fail-open requires Phase-2 evidence.

### 3. Bootstrap-Circularity Carve-Out Is One-Time-Per-Rule

The codify that AUTHORS this rule (and any future codify that authors a similar meta-rule about codify discipline) IS itself a self-referential codify under Rule 1's gate. Running the gate on the codify that authors the gate is circular. Per `rules/trust-posture.md` MUST Rule 7 § Two-Phase Rollout: _"A meta-rule and its enforcement should never bootstrap in the same release — the rule is then drafted by an agent operating without it (mitigates red-team H4 bootstrapping circularity)."_

The carve-out: the FIRST codify that lands this rule (or a similar meta-rule about codify discipline) MAY ship as Phase-1 observer under the prevailing posture default (L5_DELEGATED's reduced per-round depth — mechanical-sweeps-only). The carve-out is one-time-per-rule and MUST be declared in the codify's receipt journal under a "Bootstrap-circularity disposition" section. Phase-2 enforcement (this rule's gate fires on every subsequent self-referential codify, regardless of posture) starts at the NEXT self-referential codify, NOT at the next session.

```markdown
# DO — first codify of this rule ships Phase-1 observer, declared in receipt

journal/NNNN § Bootstrap-circularity disposition:
"F4 IS a self-referential codification. Per trust-posture.md MUST-7
two-phase rollout, this codify ships F4 as Phase-1 observer under
L5_DELEGATED's reduced per-round depth. Phase-2 enforcement starts at
the NEXT self-referential codify."

# DO NOT — every future self-referential codify cites bootstrap-circularity to skip the gate

journal/NNNN: "this codify is self-referential, citing bootstrap-circularity
carve-out to skip multi-agent redteam-with-tests"
(BLOCKED — the carve-out is one-time-per-rule, not a per-session shortcut)
```

**Why:** Without the one-time-per-rule constraint, "bootstrap circularity" becomes a rubber stamp every self-referential codify cites to skip the gate. The carve-out exists for the genuine chicken-and-egg case (a meta-rule authoring itself); every subsequent invocation has the prior rule already in scope and IS the case the gate exists to govern.

## Trust Posture Wiring

- **Severity:** `halt-and-report` at /codify gate (cc-architect surfaces the violation at proposal-validation); `advisory` at hook layer (Phase 2, when a self-referential-detection hook lands). Per `rules/hook-output-discipline.md` MUST-2: judgment-bearing gates do not carry `block` severity.
- **Grace period:** 7 days from rule landing.
- **Regression-within-grace:** any same-class violation (a self-referential `/codify` that ships without multi-agent redteam-with-tests within 7 days of this rule landing) triggers emergency downgrade L5→L4 per `rules/trust-posture.md` MUST Rule 4. Trigger key `self_referential_codify_without_redteam` added to trust-posture.md emergency-trigger list (1× = drop 1 posture).
- **Receipt requirement:** SessionStart MUST require `[ack: self-referential-codify]` in the agent's first response IF `posture.json::pending_verification` includes this rule_id.
- **Detection (Phase 1 — this codify):** MANUAL. The `/codify` orchestrator reads the allowlist in Rule 2, checks the proposal's file list against it, and dispatches the multi-agent redteam-with-tests team described in Rule 1 when any match. No hook detection in Phase 1.
- **Detection (Phase 2 — deferred):** a planned `codify-self-referential.js` hook under `.claude/hooks/lib/` (not yet created). Runs on `/codify` proposal generation; mechanically detects allowlist matches per Rule 2 and emits an advisory finding the orchestrator MUST resolve before merge. Audit fixtures will be committed under `.claude/audit-fixtures/` (the `codify-self-referential/` subdir) per `rules/cc-artifacts.md` Rule 9. Phase 2 lands after ≥3 real self-referential `/codify` cycles have exercised Phase 1 (per `rules/trust-posture.md` MUST Rule 7 § Two-Phase Rollout: enforcement bootstraps after real-session-based exercise).

## Distinct From / Cross-References

- **Extends:** `rules/cc-artifacts.md` Rule 6 (every /codify deploys cc-architect) → this rule adds a parallel multi-agent redteam-with-tests requirement on the self-referential subset of `/codify` executions. cc-artifacts.md Rule 6 still applies to every /codify; this rule layers additional gating on the self-referential surface.
- **Pairs with:** `.claude/skills/32-trust-posture/redteam-integration.md` § Patterns From Convergence Arcs (Patterns a, b, c) — those patterns govern WHAT the redteam round checks; this rule governs WHEN the round MUST fire regardless of posture.
- **Distinct from:** `rules/trust-posture.md` MUST Rule 4 (downgrade triggers) — that rule defines posture-downgrade math; this rule's regression-within-grace clause feeds into MUST-4's emergency-trigger list but does NOT replace it. `rules/trust-posture.md` MUST Rule 7 § Two-Phase Rollout — that rule's principle is the foundation Rule 3 above instantiates; the carve-out language is borrowed and constrained to one-time-per-rule.

## Origin

2026-05-18 — journal/0098 §For Discussion #3 surfaced the question; co-owner approval ("approved") on the F4 recommendation in this session, both cons disclosed (bootstrap-circularity + surface-area definition). Receipt-first journal/0100 (this codify's provenance, per `rules/artifact-flow.md` § Co-Owner-Directed Origination). Originating evidence: forest-ledger arc journal/0089–0098, 7 redteam rounds + mid-arc Option A→B redesign, the substring-mask failure class structurally closed only after Option B inflection.

{
  "version": "2.12.0",
  "type": "coc-source",
  "description": "COC artifact source of truth (codegen)",
  "released": "2026-05-01",
  "upstream": {
    "name": "co",
    "repo": "terrene-foundation/co",
    "version": "1.0.0",
    "version_url": "https://raw.githubusercontent.com/terrene-foundation/co/main/.claude/VERSION"
  },
  "changelog": [
    {
      "version": "2.12.0",
      "date": "2026-05-01",
      "summary": "MINOR: codify spec-accuracy rule from loom issue #18 (origin: 2026-04-21 tpc/tpc_cash_treasury-scenario /redteam audit surfacing 8 phantom Databricks-accessor citations + 6 plugged Monte-Carlo volatility constants). Rule blocks gap-annotated specs (Phase-1/Phase-2, Promised/Current, Target/Fallback, scaffold-now-wire-later framings) — every cited symbol MUST resolve via grep / ast.parse / find at merge time; work trackers MUST live in workspaces/todos/ or GH issues, never inline in spec content; spec extension follows merged code (spec-first BLOCKED, code-first-without-spec also BLOCKED per specs-authority.md Rule 5). STRUCTURAL CLASSIFICATION CHOICE: rule shipped as `priority: 10, scope: path-scoped` (paths: specs/**, workspaces/**/specs/**, 02-plans/**, briefs/**) — NOT priority:0 baseline as the issue framing suggested. Rationale: (a) per `rule-authoring.md` MUST Rule 5, path-scoped is the default — baseline requires justification; (b) failure mode (phantom citations) only fires when editing spec files, exactly the trigger condition for path-scoping; (c) sister rule `specs-authority.md` is already path-scoped at the same paths, spec-accuracy is its content sibling; (d) avoids the rs-axis cap-headroom trap entirely (post-2.11.2 rs-axis emission was 59,879 B / 61,440 B block_cap, 2.54% headroom — adding a CRIT-baseline rule would have pushed AGENTS.md/GEMINI.md emission into BLOCK without a CRIT demotion first). Path-scoped classification means baseline emission stays at 59,879 B (verified — 10 CRIT rules unchanged). Issue's reference to `data-availability.md` as a precedent is from the downstream reporter's mental model — that file does not exist in loom canonical (verified). Rule body is 196 lines (under 200-line rule-authoring guidance), 10,581 B raw. Companion `guides/rule-extracts/spec-accuracy.md` carries the full 2026-04-21 phantom-Databricks post-mortem, the gap-tracker migration playbook, the BLOCKED-rationalization with-Why list, the relationship-to-other-rules table, and the full 3-sweep audit protocol (split-state framing scan + citation resolution + future-tense scan).",
      "changes": [
        "NEW RULE: .claude/rules/spec-accuracy.md (196 LOC, 10,581 B raw) — priority:10 / scope:path-scoped — paths: specs/**, specs/_index.md, workspaces/**/specs/**, 02-plans/**, briefs/**. Six MUST clauses (citations resolve, no split-state, out-of-scope is not a gap, work trackers outside specs, incremental extension, historical change logs permitted past-tense), five MUST NOT (phantom citations, Phase-1/2 framings, gap-honesty as virtue, permanent gap trackers, spec for unimplemented behavior), three structural carve-outs (out-of-scope sections, append-only change logs, reserved-for-future-work numbering anchors), audit protocol with 2-sweep grep set.",
        "NEW EXTRACT: .claude/guides/rule-extracts/spec-accuracy.md — full 2026-04-21 phantom-Databricks post-mortem (8 cited accessors, 0 implemented; 6 plugged Monte-Carlo volatility constants), 4-step migration playbook for existing gap trackers, 8 BLOCKED rationalizations enumerated with Why for each, relationship-to-other-rules cross-reference table (zero-tolerance.md Rule 2, specs-authority.md, autonomous-execution.md, rule-authoring.md), full 3-sweep audit protocol (extends rule body's 2-sweep with future-tense scan).",
        "MANIFEST: sync-manifest.yaml::tiers.co += `rules/spec-accuracy.md` (ships to all USE templates as path-scoped CO rule alongside specs-authority.md and rule-authoring.md).",
        "DOC: CLAUDE.md Rules Index += `rules/spec-accuracy.md` row labelled 'Spec accuracy (no phantom citations / no Phase-1/2 framings)' under CO category, immediately after specs-authority.md.",
        "BASELINE TELEMETRY (verified post-add): rs-axis codex AGENTS.md unchanged at 59,879 B [WARN tier, 1,561 B headroom]; rs-axis gemini GEMINI.md unchanged at 59,888 B [WARN tier, 1,552 B headroom]; py-axis codex AGENTS.md unchanged at 58,058 B. Path-scoped rule contributes zero baseline emission — cap-headroom trap fully side-stepped.",
        "FUTURE WORK FLAG (carry into next session): rs-axis cap-headroom trap remains at 2.54% post-add (no regression, no improvement). Cleanest demotion candidate identified: `cross-cli-parity.md` (3,064 B emission, structurally loom-only meta-authoring rule — same Phase F4 precedent that demoted `rule-authoring.md` and `variant-authoring.md` from CRIT-baseline to HIGH path-scoped on 2026-04-22). Demotion would free ~3 KB headroom and is independent of the spec-accuracy add — file separately as 2.13.0 single-purpose cycle.",
        "ISSUE: closes loom #18 with structural-classification deviation note (path-scoped vs reporter's baseline framing) referenced in PR / issue comment."
      ]
    },
    {
      "version": "2.11.2",
      "date": "2026-04-30",
      "summary": "PATCH: cap-headroom restoration + new `use_obsoleted:` manifest tier (active-purge complement of use_exclude:). (1) Trimmed `rules/git.md` § Discipline section: collapsed three secondary subsections (Issue Closure / Pre-Commit Hook Workarounds / Commit-Message Claim Accuracy) into a tight bullet list — preserves the load-bearing MUST clauses + Why and routes full DO/DO NOT examples to `guides/rule-extracts/git.md`. Drop ~1.0 KB raw / ~0.8 KB post-abridgement. git.md emission: 6,431 B → 5,627 B (within +30% of 4,400 B budget). (2) Added per-rule budget entry `upstream-issue-hygiene.md: 4000` to sync-manifest.yaml::cli_variants.context/root.md.codex.abridgement_protocol.per_rule_size_budget_bytes — clears the validator-14 'CRIT rule requires a budget' warning. (3) AGENTS.md / GEMINI.md emission headroom restored from ~4.2% to ~5.5% (3,382 B / 61,440 B block_cap on rs language axis). Still WARN tier (steady-state per v6 §2.2) but next CRIT addition has more buffer. (4) NEW manifest tier `use_obsoleted:` symmetric with `use_exclude:` — declares paths to actively purge from USE templates AND propagate to downstream consumers via `.coc-obsoleted`, while BUILD repos preserve them. coc-sync agent Step 4.5 updated to UNION `obsoleted:` + `use_obsoleted:` for purge + emit. /sync-to-build IGNORES use_obsoleted: (BUILD repos legitimately own those paths). (5) Populated use_obsoleted: with 7 paths flagged by 2.11.0 sync agents as pre-existing copies in USE templates after the use_exclude: classification: rules/documentation.md, skills/30-claude-code-patterns/sdk-upstream-donation.md, guides/co-setup/, guides/deterministic-quality/, guides/automated-maintenance.md, guides/rule-extracts/{git,independence-rs}.md.",
      "changes": [
        "RULE TRIM: rules/git.md — collapsed Issue Closure Discipline + Pre-Commit Hook Workarounds + Commit-Message Claim Accuracy into a single bullet-list `## Discipline` section with one combined Why. MUST clauses preserved as bullets; full DO/DO NOT examples routed to guides/rule-extracts/git.md. Raw size 8,673 → ~7,650 B; post-abridgement 6,431 → 5,627 B (within +30% of 4,400 B budget).",
        "MANIFEST: sync-manifest.yaml::cli_variants.context/root.md.codex.abridgement_protocol.per_rule_size_budget_bytes += `upstream-issue-hygiene.md: 4000` (10 CRIT-baseline rules total now have explicit budgets).",
        "MANIFEST: sync-manifest.yaml — NEW top-level `use_obsoleted:` block (after use_exclude:). 7 entries covering pre-existing-in-USE-templates copies of newly-use_excluded paths. Documented as symmetric with use_exclude: — purges from USE templates + propagates to downstream consumers; BUILD repos preserve.",
        "AGENT: agents/management/coc-sync.md Step 4.5 — read BOTH obsoleted: AND use_obsoleted:; purge UNION; emit UNION to `.coc-obsoleted`.",
        "COMMAND: commands/sync-to-build.md § 3 — added `use_obsoleted:` documentation; clarified that /sync-to-build IGNORES use_obsoleted: (BUILD repos legitimately own those paths).",
        "EFFECT ON 2026-04-30 RE-SYNC: next /sync to USE templates will (a) purge the 7 use_obsoleted: paths from kailash-coc-claude-{py,rs,rb} + kailash-coc-{py,rs}, (b) emit the union to each target's .coc-obsoleted, (c) downstream consumer apps' next /sync will inherit the purge contract and remove their stale copies. BUILD repos kailash-py / kailash-rs / kailash-prism are unaffected — they retain the BUILD-only artifacts."
      ]
    },
    {
      "version": "2.11.1",
      "date": "2026-04-30",
      "summary": "PATCH: manifest accuracy correction + cap-headroom flag. (1) Add 3 missing variant entries to use_exclude per 2.11.0 changelog claim — variants/py/rules/ci-runners.md, variants/rs/rules/ci-runners.md, variants/rs/skills/management/** (these have zero functional effect on USE-template emission because variants/** is already universally excluded; the entries document BUILD-only intent at the manifest layer and are a backstop if variant emission semantics ever change). (2) Document the CRIT-baseline cap-headroom regression flagged by both multi-CLI sync agents during 2.11.0 distribution: adding upstream-issue-hygiene.md as the 10th priority:0 baseline rule consumed most of the post-2.10.1 extraction buffer; AGENTS.md / GEMINI.md emission headroom dropped from ~12.7% (53,620 B) to ~1.2-4.2% (~58,800-60,700 B / 61,440 block_cap) on the rs language axis. Both targets remain in WARN tier (no BLOCK), but the next CRIT addition will block emission unless an existing rule is demoted to path-scoped per v6 §A.2 OR upstream-issue-hygiene is itself trimmed. Captured in per-target .coc-sync-marker files at both kailash-coc-py and kailash-coc-rs. No code change this release; flag-only.",
      "changes": [
        "MANIFEST: sync-manifest.yaml — appended 3 entries to `use_exclude:`: variants/py/rules/ci-runners.md, variants/rs/rules/ci-runners.md, variants/rs/skills/management/**. Per `rules/git.md` § Commit-Message Claim Accuracy: the 2.11.0 changelog promised these entries; this patch delivers them.",
        "FLAG (no code change): CRIT-baseline cap headroom dropped to ~1.2% on rs language axis (multi-CLI emission); next priority:0 rule will hit block_cap_bytes=61,440. Captured in `.coc-sync-marker` at kailash-coc-py + kailash-coc-rs USE templates. Remediation paths: (a) demote a CRIT to path-scoped per v6 §A.2 (precedent: `rule-authoring.md` + `variant-authoring.md` demoted at Phase F4 for the same reason); (b) trim upstream-issue-hygiene.md body; (c) raise block_cap_bytes (structurally constrained — Codex override-flag ceiling is 64 KiB minus 4 KiB safety margin = 61,440)."
      ]
    },
    {
      "version": "2.11.0",
      "date": "2026-04-30",
      "summary": "MINOR: USE-template downstream-context leakage remediation. (1) NEW manifest tier `use_exclude:` symmetric with `build_exclude:` — paths listed there ship to BUILD repos but NOT to USE templates. (2) NEW rule `rules/upstream-issue-hygiene.md` (priority:0 / scope:baseline) with three MUSTs: human-gate before `gh issue create` against any SDK repo, downstream-context redaction (no consumer project name, internal paths, workspace IDs, finding tags), minimal-repro shape (5 required sections only). (3) Moved 6 BUILD-only artifacts to use_exclude (rules/cross-sdk-inspection.md, rules/documentation.md, skills/30-claude-code-patterns/sdk-upstream-donation.md, guides/co-setup/**, guides/deterministic-quality/**, guides/automated-maintenance.md, guides/rule-extracts/{git,independence-rs}.md, plus variant/rs CI/release ops). (4) Anonymized ~80 leakage sites across rules/skills/agents/extracts: removed `kailash-py`/`kailash-rs`/`terrene-foundation/...`/`esperie-enterprise/...` identifiers from Origin lines, dropped Cross-SDK-applicable footers, replaced absolute-path examples with placeholders. coc-sync agent + commands/sync.md + commands/sync-to-build.md updated to honor `use_exclude:` at Gate 2. Origin: 2026-04-30 leakage audit prompted by downstream apps cross-filing parity issues to upstream SDK repos because USE-template artifacts had the cross-SDK convention baked in.",
      "changes": [
        "MANIFEST: sync-manifest.yaml — added `use_exclude:` block (lines after build_exclude). Includes rules/cross-sdk-inspection.md (was mis-placed in universal exclude:), rules/documentation.md (kailash-py-URL-hardcoded), skills/30-claude-code-patterns/sdk-upstream-donation.md (auto-cross-file instructions), guides/co-setup/**, guides/deterministic-quality/**, guides/automated-maintenance.md, guides/rule-extracts/independence-rs.md, guides/rule-extracts/git.md, variants/py/rules/ci-runners.md, variants/rs/rules/ci-runners.md, variants/rs/skills/management/**.",
        "MANIFEST: removed `rules/cross-sdk-inspection.md` from universal exclude:; removed `guides/automated-maintenance.md` from universal exclude:; removed `guides/co-setup/**` from co tier; removed `guides/deterministic-quality/**` from co tier (these were mis-classifying — co tier ships to ALL USE templates).",
        "MANIFEST: added `rules/upstream-issue-hygiene.md` to co tier (ships to all USE templates as priority:0 baseline).",
        "NEW RULE: rules/upstream-issue-hygiene.md (~150 lines) — three MUSTs (human gate, context redaction, minimal-repro shape) + MUST NOT (auto-cross-file, standing approval, leak-by-edit). Uses standard rule format: BLOCKED rationalizations, DO/DO NOT examples, Why per clause, Origin.",
        "AGENT: agents/management/coc-sync.md Step 2 += honor `use_exclude:` at Gate 2 file-manifest construction (parallel to existing exclude: skip).",
        "COMMAND: commands/sync.md Gate 2 step 1+3 += honor `use_exclude:` symmetric with `build_exclude:` for /sync-to-build.",
        "COMMAND: commands/sync-to-build.md § 3 += explanation of `use_exclude:` symmetry (file lists BUILD-only paths; /sync-to-build INCLUDES them; /sync EXCLUDES them at USE-template emission).",
        "RULE TRIMS (anonymized identifiers + dropped Cross-SDK footers): zero-tolerance.md L275, build-repo-release-discipline.md L23 + L163, git.md L40 + L82 + L99 + L146, testing.md L127 + L198, schema-migration.md L71 + L203, eatp.md § Cross-SDK Alignment (entire subsection removed), agents.md L124 + L142 + L165, autonomous-execution.md L144 + L148, ci-runners.md L228 + L266 + L319 + L359, dataflow-classification.md L151, dataflow-identifier-safety.md L198, framework-first.md L175, nexus-http-status-convention.md L17 + L178, nexus-webhook-hmac.md L160, orphan-detection.md L100, patterns.md L210, tenant-isolation.md L101 + L125, worktree-isolation.md L22, artifact-flow.md L81 + L110, event-payload-classification.md L26 + L47 + L76 + L107 + L146 + L148 + L163.",
        "SKILL TRIMS: 04-kaizen/kaizen-llm-deployment.md L135 + L185, 01-core-sdk/runtime-lifecycle.md L14, 02-dataflow/migration-scaffold.md L15 + L51, 03-nexus/nexus-extractors.md L19 + L263, 03-nexus/nexus-http-status-convention.md L179, 13-architecture-decisions/allowlist-extension-pattern.md L183, 16-validation-patterns/orphan-audit-playbook.md L9 + L19 + L44 + L52 + L60 + L64 + L72, 12-testing-strategies/impossibility-surface.md L84 + L152, test-skip-discipline/SKILL.md L236, spec-compliance/SKILL.md L71, 18-security-patterns/dataflow-rls-posture.md L7 + L46 + L57 + L83, 18-security-patterns/SKILL.md L123 + L136 + L165 + L172, 18-security-patterns/rls-security-definer-preauth-carveout.md L465, 10-deployment-git/ci-runner-troubleshooting.md L185, 10-deployment-git/todo-github-sync.md L482-483, 31-error-troubleshooting/SKILL.md L156.",
        "AGENT TRIMS: agents/frameworks/kaizen-specialist.md L198 (drop `esperie/kailash-rs#406+#393+#394` cross-SDK parity clause), agents/frameworks/pact-specialist.md L143 + L145 (drop `(Cross-SDK)` heading + `Discovered during kailash-rs red team` line).",
        "EXTRACT TRIMS: guides/rule-extracts/agents.md L44, observability.md L32, worktree-isolation.md L13, testing.md L75 + L101 + L134.",
        "PRIVACY POSTURE: USE-template-distributed surface no longer carries cross-SDK awareness, sibling-SDK identifiers, BUILD-internal architecture narratives, or auto-cross-file instructions. The new `upstream-issue-hygiene.md` rule is the active tripwire that catches `gh issue create` invocations against any SDK repo without explicit user approval and without minimal-repro body shape. BUILD repos and loom retain full cross-SDK content via /sync-to-build (use_exclude does NOT affect that flow).",
        "AUDIT-DRIVEN: leakage inventory + remediation plan generated by cc-architect audit (~80 sites identified across C1 auto-file-instructions / C2 hardcoded-org-URLs / C3 cross-SDK-Origin-footers / C4 whole-guide-trees). Existing 13 public-issue body scrubs (kailash-py #707, #673, #618, #731, #596, #491, #643, #523, #529 + kailash-rs #424, #402, #589, #447) are pending separate user-approved batch via `gh issue edit` — out of this loom-side release scope."
      ]
    },
    {
      "version": "2.10.2",
      "date": "2026-04-29",
      "summary": "PATCH: sync-hygiene remediation across all 8 BUILD/USE consumers — completes the claude-code-architect → cc-architect rename (commands/codify.md lines 101 + 128 missed in v2.9.x rename); closes 4 sync-pollution leaks (.claude/projects/, .claude/scheduled_tasks.lock, .claude/test-harness/ propagating from loom local state to consumers; cross-sdk-inspection.md stale in rb USE template after exclusion; .claude/learning/ stale in 5 USE templates after exclusion). Manifest: obsoleted += 4 entries (claude-code-architect.md + 3 loom-local pollution paths) with paired exclude entries (projects/**, scheduled_tasks.lock, test-harness/**) so the sync mechanism prevents re-introduction. Surgical patch — only the modified file (commands/codify.md) was distributed; existing artifact set unchanged. Stale state purged across all 8 consumers via .coc-obsoleted contract. Multi-CLI .codex/.gemini trees regenerated for kailash-coc-py and kailash-coc-rs to propagate the codify.md rename through emit-cli-artifacts.mjs.",
      "changes": [
        "FIX: .claude/commands/codify.md — lines 101 + 128 stale 'claude-code-architect' renamed to 'cc-architect'. Source-rename in v2.9.x missed these two narrative references. Distributed to all 8 consumers (commands/codify.md content-sync) + regenerated multi-CLI .codex/prompts/codify.md and .gemini/commands/codify.toml.",
        "BACKFILL: pulled loom-source-current versions of guides/co-setup/05-variant-architecture.md + rules/cc-artifacts.md + skills/30-claude-code-patterns/SKILL.md to kailash-rs and kailash-prism BUILD repos. These files had been renamed to cc-architect in loom global on a prior cycle but the BUILD repos hadn't been resynced (kailash-py was on the current rev — only rs and prism BUILD lagged). Final state: zero claude-code-architect refs across all 8 BUILD/USE consumers.",
        "MANIFEST OBSOLETED: += .claude/agents/claude-code-architect.md (rename remediation; was missing as obsoleted entry — agent file lingered in 5 of 8 BUILD/USE consumers).",
        "MANIFEST OBSOLETED: += .claude/projects/ (Claude Code per-user session/state cache; loom-local; surfaced in audit on rs USE templates carrying loom's own -Users-esperie-repos-loom/ cache).",
        "MANIFEST OBSOLETED: += .claude/scheduled_tasks.lock (Claude Code per-user scheduled-task lock; loom-local; surfaced in audit — 5 of 5 USE templates + 2 BUILD repos carried it).",
        "MANIFEST OBSOLETED: += .claude/test-harness/ (loom multi-CLI parity test fixtures + ~70 result logs; consumers do not run the harness; surfaced on rs USE templates at 644K, 100 files).",
        "MANIFEST EXCLUDE: += projects/**, scheduled_tasks.lock, test-harness/** — paired with the 3 new obsoleted entries so /sync no longer propagates loom-local state to consumers.",
        "ONE-SHOT PURGE: .claude/learning/ removed from all 5 USE templates (528K total stale data; manifest already excluded learning/** but no obsoleted-purge mechanism — manual one-shot sufficient since BUILD repos legitimately own learning/).",
        "ONE-SHOT PURGE: .claude/rules/cross-sdk-inspection.md removed from kailash-coc-claude-rb (excluded from sync; stale leftover from before exclusion was added).",
        "DISTRIBUTION: .claude/.coc-obsoleted regenerated (16 lines = 13 obsoleted paths + 3 comment lines) in all 8 BUILD/USE consumers so downstream /sync inherits the same purge contract on next pull.",
        "MULTI-CLI EMIT: ran emit-cli-artifacts.mjs --out, regenerated kailash-coc-py + kailash-coc-rs trees: .codex/prompts (30) + skills (34) + .gemini/commands (30) + skills (34) + agents (22). Verified 0 stale claude-code-architect refs in regenerated codify files.",
        "AUDIT MATRIX: 8 BUILD/USE consumers audited — 6 had pollution; all clean post-patch. BUILD repos (kailash-py, kailash-rs, kailash-prism) keep own .claude/learning/ (legitimate local data; manifest excludes from propagation but does not purge BUILD-owned state)."
      ]
    },
    {
      "version": "2.10.1",
      "date": "2026-04-29",
      "summary": "PATCH: per-CLI baseline cap remediation. Extract heavy bodies of rules/git.md (carried from 2.9.7 git reset --hard clause) + variants/rs/rules/independence.md (rs proprietary-licensing variant) into new guides/rule-extracts/{git.md, independence-rs.md}. Pre-extraction state shipped in 2.10.0 produced rs baseline emission 61,627 B (codex) / 61,636 B (gemini) — both OVER the 61,440 B per-CLI block_cap by 187 / 196 B; AGENTS.md and GEMINI.md emit was BLOCKED, agent-coc-sync held both at prior 2.9.5 emission (55,413 / 55,422 B WARN tier). Post-extraction: rs baseline emits at 56,810 B (codex) / 56,819 B (gemini) — 4,630 / 4,621 B headroom (~7.5%) under the 61,440 cap, restoring ~1.5 future-clause's worth of buffer. Per-rule budgets: git.md 6,306 B (still over 4,400 B budget at WARN, but under +30% block threshold of 5,720 B... actually still 586 B over block-threshold but no longer blocking baseline emit since total is under cap); independence.md 4,300 B (still over 2,500 B budget at WARN). The remaining per-rule WARNs are refactoring-signal tier per spec v6 §2.2; total emission proceeds. Extraction follows the established pattern (`See .claude/guides/rule-extracts/<rule>.md for ...`) already used by zero-tolerance, agents, security, worktree-isolation. No semantic rule changes — every MUST clause + at least one DO/DO-NOT pair preserved in the rule file; only extended bash examples, full BLOCKED rationalization lists, and full Origin prose moved to extracts. Multi-CLI rs USE template (kailash-coc-rs) can now re-emit AGENTS.md / GEMINI.md cleanly on next /sync rs cycle.",
      "changes": [
        "NEW EXTRACT: .claude/guides/rule-extracts/git.md — companion to rules/git.md. Holds extended bash examples for release/v* convention, Pre-FIRST-Push CI parity, git reset --hard, atomic commits, issue closure, pre-commit hook workarounds, commit-message claim accuracy. Full BLOCKED rationalization lists + Origin evidence + branch-protection repository table.",
        "TRIM: .claude/rules/git.md — 18,065 B → 8,548 B raw (cut ~52%). Emitted (post-abridgement) 7,456 B → 6,306 B. All MUST clauses preserved + 1 DO/DO-NOT example per clause + 1-line Why per clause + extract cross-reference at top.",
        "NEW EXTRACT: .claude/guides/rule-extracts/independence-rs.md — companion to variants/rs/rules/independence.md. Holds the boundary table, full key facts, extended DO/DO-NOT examples for all 5 MUST rules, BLOCKED rationalizations, Why-extended prose, and the relationship-to-other-rules cross-reference list.",
        "TRIM: .claude/variants/rs/rules/independence.md — 8,492 B → ~5,000 B raw. Emitted 6,647 B → 4,300 B. All 5 MUST clauses preserved with DO/DO-NOT pair + 1-line Why each + extract cross-reference at top.",
        "BASELINE TELEMETRY (post-extraction, verified via emit.mjs): codex AGENTS.md 56,810 B [WARN tier, 4,630 B headroom]; gemini GEMINI.md 56,819 B [WARN tier, 4,621 B headroom]. Both pass block_cap (61,440 B) and proceed to emission.",
        "PER-RULE STATUS (no manifest budget changes — refactoring signal preserved): git.md 6,306/4,400 (WARN); independence.md 4,300/2,500 (WARN); other 7 baseline rules OK. Per-rule WARNs surface drift but do not block emission per spec v6 §A.2.",
        "NEXT-CYCLE GUIDANCE: future CRIT-baseline additions still MUST extract or path-scope first per multi-cli-coc spec §A.2. ~7.5% headroom is comfortable for one more clause without further extraction; planning extraction at 2.10.x → 2.11.x if/when more global MUSTs arrive."
      ]
    },
    {
      "version": "2.10.0",
      "date": "2026-04-29",
      "summary": "MINOR: upstream 5 rule sub-clauses from kailash-py codify (2026-04-29 — kailash-ml-1.5.x-followup workspace; SDK 2.12.0). Five global, cross-SDK MUST clauses bundled with the in-flight 2.9.7 git.md clause. (1) rules/zero-tolerance.md Rule 2 § Extended BLOCKED Patterns gains 'Fake dispatch' bullet — accepted-in-Literal-but-no-branch dispatcher; same failure-mode class as fake encryption / fake transaction / fake health; /redteam MUST AST-walk every Literal/Enum dispatch parameter to confirm every accepted literal has a branch; Rust match-exhaustiveness covers enum case structurally, &str dispatch does not. Origin: kailash-ml _wrappers.py:474–485 accepted kind={'clustering','alignment','llm','agent'} as valid Literals — none had a dispatch branch (#701 bonus finding). (2) rules/zero-tolerance.md NEW Rule 3c — 'Documented Kwargs Accepted But Unused'. Every kwarg in the public signature documented in the spec MUST be consumed by ≥1 branch of the function body; silent drop is BLOCKED. 6 BLOCKED rationalizations enumerated. Detection: at every def boundary confirm each kwarg appears ≥1 time in body or is forwarded to a callee. Origin: diagnose(model, kind='dl', data=loader) accepted data= in spec §3.1 documented signature, silently dropped on kind='dl' branch (#701). (3) rules/zero-tolerance.md NEW Rule 6a — 'Remove Fully — Public-API Removal Requires Deprecation Cycle'. Public-API removal MUST land with DeprecationWarning shim covering ≥1 minor cycle + CHANGELOG migration section. Removal-without-shim is BLOCKED. Python and Rust DO/DO NOT examples. 7 BLOCKED rationalizations. Origin: kailash-ml 1.5.0 dropped InferenceServer(registry=, cache_size=) + warm_cache + load_model(name, model) without deprecation cycle; every 1.1.x callsite hard-broke on first import (#700). Cross-SDK applicable: same gap structurally for Rust pub fn removal without #[deprecated]. (4) rules/schema-migration.md NEW Rule 1a — '/redteam MUST Grep For Inline DDL Outside Migrations'. /redteam mechanical sweep MUST scan source trees for CREATE/ALTER/DROP TABLE|INDEX|SCHEMA literals outside migrations/ + dialect-helper layers. Concrete grep command shipped. 6 BLOCKED rationalizations. Origin: kailash-ml ModelRegistry._create_registry_tables() shipped CREATE TABLE IF NOT EXISTS for ~3 months while migration 0002 owned same table with different columns; three-way drift invisible at code-review level (#699). (5) rules/agents.md § Parallel Execution gains NEW MUST sub-clause — 'Parallel Brief-Claim Verification When Issue Count ≥ 3'. /analyze with ≥3-issue brief MUST launch parallel deep-dive verification agents (one per claim cluster) to independently re-grep / re-read every factual claim. Single-agent analysis BLOCKED for ≥3-issue briefs. Variant cascade: codex/rules/agents.md (codex_agent syntax), gemini/rules/agents.md (@specialist syntax), rs/rules/agents.md (Task tool syntax) all updated to maintain cross-CLI parity per cross-cli-parity.md MUST Rule 1. 7 BLOCKED rationalizations enumerated. Origin: kailash-ml-1.5.x-followup brief had THREE distinct factual inaccuracies (issue #699 root-cause framing, #700 file path, #701 silent-drop scope) — all caught only because three parallel deep-dive agents independently verified. (6) [in-flight from 2.9.7, included in 2.10.0 distribution] rules/git.md NEW MUST sub-clause '## git reset --hard MUST Verify Clean Working Tree' — mandates git status --porcelain empty-check before any --hard reset; mandates git reset --keep <ref> as preferred form. Pre-existing extraction debt: zero-tolerance.md grew 189 → ~270 LOC; agents.md grew 196 → ~245 LOC; schema-migration.md grew 203 → ~245 LOC; git.md at 295 LOC (carried from 2.9.7). All four files exceed rule-authoring.md ≤200 LOC guidance — same status as testing.md prior cycles; the new clauses are load-bearing MUST content, not extraction-eligible reference material. Multi-CLI baseline headroom check (per session-notes 2026-04-28): AGENTS.md was 14.51% / GEMINI.md 14.49% under 61,440 B block_cap; this cycle adds ~3kB to baseline (4 sub-clauses) — projected post-distribution headroom ~9–10%, still under cap but next cycle MUST extract before adding.",
      "changes": [
        "GLOBAL: .claude/rules/zero-tolerance.md — Rule 2 Extended BLOCKED Patterns gains 'Fake dispatch' bullet (kailash-ml _wrappers.py:474–485 evidence, #701 bonus); NEW Rule 3c 'Documented Kwargs Accepted But Unused' (#701 diagnose(data=) evidence); NEW Rule 6a 'Remove Fully — Public-API Removal Requires Deprecation Cycle' (#700 InferenceServer kwargs evidence). 189 → ~270 LOC.",
        "GLOBAL: .claude/rules/schema-migration.md — NEW Rule 1a '/redteam MUST Grep For Inline DDL Outside Migrations' inserted after Rule 1. Concrete grep command + path-exclusion clause + 6 BLOCKED rationalizations. Origin #699 ModelRegistry three-way drift evidence. 203 → ~245 LOC.",
        "GLOBAL: .claude/rules/agents.md — NEW MUST sub-clause 'Parallel Brief-Claim Verification When Issue Count ≥ 3' inserted in § Parallel Execution. 7 BLOCKED rationalizations. Origin #699/#700/#701 brief inaccuracies. 196 → ~245 LOC.",
        "VARIANT: .claude/variants/codex/rules/agents.md — mirror brief-verification clause with codex_agent(...) delegation syntax (cross-cli-parity preservation).",
        "VARIANT: .claude/variants/gemini/rules/agents.md — mirror brief-verification clause with @specialist invocation syntax.",
        "VARIANT: .claude/variants/rs/rules/agents.md — mirror brief-verification clause (rs neutral-body, no Agent() syntax in body — abbreviated form without DO/DO NOT example block).",
        "GLOBAL: .claude/rules/git.md — (carried from 2.9.7) NEW MUST sub-clause '## git reset --hard MUST Verify Clean Working Tree'. 245 → 295 LOC."
      ]
    },
    {
      "version": "2.9.7",
      "date": "2026-04-28",
      "rolled_into": "2.10.0",
      "summary": "PATCH: upstream `git reset --hard` discipline rule from kailash-py codify (2026-04-28). New MUST sub-clause in rules/git.md (`## git reset --hard MUST Verify Clean Working Tree`) inserted before the existing § Rules bullet list. Mandates `git status --porcelain` empty-check before any --hard reset; mandates `git reset --keep <ref>` as the structurally-safer preferred form for back-out / branch-reorganization workflows; `--keep` aborts loudly when local changes would be lost, converting silent data-loss into refused-with-actionable-error. Origin: kailash-py session 2026-04-28 (PR #691 sync-PR rebase) — `.session-notes` modifications wiped by `git reset --hard 7ce2d2eb`; recovery only possible because file content was in conversation context. Sibling structural-confirmation pattern of dataflow-identifier-safety.md Rule 4 (DROP requires force_drop=True) and schema-migration.md Rule 7 (downgrade requires force_downgrade=True) — same family, applied to local-workspace destruction surface. Classification: GLOBAL — git semantics are universal across every language and SDK; no variant overlay needed; rule lives inside rules/git.md slot:neutral-body wrapper so codex and gemini emissions inherit byte-identical content per cross-cli-parity.md MUST Rule 1. git.md grew 245 → 295 lines (exceeds the rule-authoring.md ≤200 LOC guidance; pre-existing extraction debt — same status as agents.md and testing.md prior cycles; the new clause is load-bearing MUST content, not extraction-eligible reference material).",
      "changes": [
        "GLOBAL: .claude/rules/git.md — NEW MUST sub-clause `## git reset --hard MUST Verify Clean Working Tree` inserted before § Rules. DO/DO NOT examples cover four patterns: --keep (structurally-safest), --hard after porcelain-empty verification, stash-push-then-hard-then-pop, and committed-on-main-by-accident recovery via `git switch -c feat/<name> && git switch main && git reset --keep origin/main`. 7 BLOCKED rationalizations enumerated. Why-paragraph cross-references the structural-confirmation siblings."
      ]
    },
    {
      "version": "2.9.6",
      "date": "2026-04-28",
      "summary": "PATCH: fix downstream /sync skipping `.claude/hooks/` and `.claude/bin/`. Symptom reported: many downstream consumer repos (kaizen-cli-py, kz-engage, etc.) ran /sync, the obsoleted-purge for legacy `.claude/scripts/` and `scripts/hooks/` fired correctly, but the canonical v2.9.1+ `.claude/hooks/` directory was NEVER copied from the resolved USE template. Result: settings.json hook entries pointed at $CLAUDE_PROJECT_DIR/.claude/hooks/<name>.js paths that did not exist on disk; every CC session at affected downstreams failed SessionStart with MODULE_NOT_FOUND. Root cause: the /sync command spec (commands/sync.md) Step 4 of Downstream Sync said only 'Diff template's .claude/ against local' and Step 3 of Gate 2 enumerated 'Global files from .claude/ (agents/, commands/, rules/, skills/, guides/)' — hooks/ and bin/ were NOT in that list. The agent computing 'expected state' followed the explicit enumeration and silently skipped runtime infrastructure. Compounding factor: the manifest's `tiers:` block did not list `hooks/**` or `bin/**` under any tier (tier-aware tooling treated them as non-syncable). Fix: (1) commands/sync.md Step 4 (Downstream) and Step 3 (Gate 2) now enumerate hooks/**, hooks/lib/**, bin/**, .coc-obsoleted, plus top-level scripts/migrate.py, plus an explicit BLOCKED-rationalization clause that calls out the specific failure mode by name. (2) sync-manifest.yaml `tiers.coc:` gains hooks/**, hooks/lib/**, bin/** entries with a comment crediting v2.9.6 and the cross-consumer evidence. Affected downstream consumers MUST re-run /sync after the fix reaches their USE template (kailash-coc-claude-py 3.5.27 / kailash-coc-py 1.0.11 distribute the fix). Distribution is automatic; no manual hook-copy required.",
      "changes": [
        "GLOBAL: .claude/commands/sync.md — Step 4 (Downstream Sync) MUST-include list expanded with hooks/**, hooks/lib/**, bin/**, .coc-obsoleted, top-level variant_only items; explicit BLOCKED-rationalization clause; Step 3 (Gate 2 expected state) same expansion + BLOCKED-rationalization clause.",
        "GLOBAL: .claude/sync-manifest.yaml — tiers.coc adds hooks/**, hooks/lib/**, bin/**. Comment cross-references this v2.9.6 fix and the SessionStart MODULE_NOT_FOUND failure mode."
      ]
    },
    {
      "version": "2.9.5",
      "date": "2026-04-28",
      "summary": "PATCH: add /sweep generic project-scoped audit command to USE templates + downstream. (1) Authored .claude/commands/sweep.md (117 lines, ≤150 cap per cc-artifacts.md Rule 5) — 7 sweeps: active workspace todos, pending journal entries, GH open issues (current repo via `gh repo view`-detected nameWithOwner), open PRs + stale branches, redteam-vs-specs gaps across every workspace, workspace+worktree hygiene, process hygiene (uncommitted/divergence/zero-tolerance markers). Project-scoped: targets the CURRENT repo only — does NOT compare against sibling SDK repos, PyPI, or BUILD-only state. (2) Manifest: tiers.co adds commands/sweep.md (peer of /autonomize, /wrapup, /journal — repo-agnostic utility distributed to all USE templates including kailash-coc-claude-py, kailash-coc-py, kailash-coc-claude-rs, kailash-coc-rs, kailash-coc-claude-rb). (3) Coexistence with BUILD-local /sweep: kailash-py and kailash-rs BUILD repos already maintain richer LOCAL commands/sweep.md (11 + 12 sweeps respectively, including cross-SDK alignment, sibling-package drift vs PyPI, source-protection on rs). Per `feedback_variant_serves_use` and `rules/artifact-flow.md` BUILD-locally-maintained-adaptations carve-out, BUILD versions stay LOCAL — the global at loom is the GENERIC version that USE templates and downstream projects consume. BUILD versions are NOT overwritten by /sync (they live outside the manifest's distributable set; loom's global commands/sweep.md is additive at downstream destinations and does NOT touch BUILD-repo .claude/commands/). (4) USE templates kailash-coc-claude-rs (still on loom 2.9.3), kailash-coc-rs (1.0.4), and kailash-coc-claude-rb (1.7.1) will receive autonomize.md (renamed from decisive.md in 2.9.4) AND sweep.md in this 2.9.5 distribution.",
      "changes": [
        "GLOBAL: .claude/commands/sweep.md — NEW (117 lines). 7-sweep generic project-scoped audit; auto-detects current repo via `gh repo view --json nameWithOwner`.",
        "GLOBAL: .claude/sync-manifest.yaml — tiers.co adds commands/sweep.md."
      ]
    },
    {
      "version": "2.9.4",
      "date": "2026-04-28",
      "summary": "PATCH: 3 follow-ups from /sync py + rename /decisive → /autonomize + retire kailash-coc-claude-prism. (1) rules/agents.md slimmed 315→196 (≤200 cap per rule-authoring.md); content cuts: BLOCKED bullet lists collapsed to single-line slash-separated phrases, examples interleaved with MUST sections, full file wrapped in slot:neutral-body so codex/gemini can overlay. (2) rules/testing.md slimmed 293→200; same content cuts; the new __all__ AST sub-clause body widened to cross-language (Python ast.parse / Rust syn::parse_file, cargo doc) so the cross-SDK principle is visible to all USE template readers. (3) variants/codex/rules/agents.md and variants/gemini/rules/agents.md authored — full neutral-body replacements with codex_agent(...) and @specialist invocation syntax respectively, closing the cross-CLI examples-slot gap that shipped CC Agent(...) verbatim into Codex/Gemini AGENTS.md/GEMINI.md emissions. (4) variants/rs/rules/testing.md updated — added the new __all__ structural-enumeration sub-clause with paired Python (ast.parse) + Rust (syn::parse_file / cargo doc --document-private-items) examples for binding-consumer audiences. (5) /decisive renamed to /autonomize: 'autonomize' emphasises the autonomous-execution posture (permission envelope + execute-without-asking) the directive actually delivers and aligns the command surface with rules/autonomous-execution.md. Same body, kept § Prudence and § Rigor pairing intact. (6) Three obsoleted entries declared (.claude/commands/decisive.md, .codex/prompts/decisive.md, .gemini/commands/decisive.toml) so every USE template + Codex/Gemini emission purges the old name on next downstream sync. (7) Backing memory feedback_directive_recommendations.md refreshed with /autonomize naming + 2026-04-28 origin reinforcement note. (8) kailash-coc-claude-prism USE template RETIRED — frontend-generic artifacts (FE agents, design-system skills, conversation/widget UX) already ship to ALL USE templates via the global coc tier; the dedicated prism USE template had no unique consumer-facing payload. Manifest: repos.prism.templates emptied, top-level LANGUAGE MAPPING + PRISM DUAL DISTRIBUTION comments rewritten as PRISM CONTENT CLASSIFICATION reflecting the consolidation. Prism BUILD repo (kailash-prism) still receives /sync-to-build for working-tree updates; prism-internal compiler/specs/build artifacts stay BUILD-only (not COC-distributed).",
      "changes": [
        "GLOBAL: .claude/rules/agents.md — slimmed 315→196 (≤200 line cap); content cuts as described.",
        "GLOBAL: .claude/rules/testing.md — slimmed 293→200; new __all__ AST sub-clause widened cross-language; full body wrapped in slot:neutral-body for Rust variant overlay.",
        "GLOBAL: .claude/variants/codex/rules/agents.md — NEW; full neutral-body overlay with codex_agent(...) syntax.",
        "GLOBAL: .claude/variants/gemini/rules/agents.md — NEW; full neutral-body overlay with @specialist invocation syntax.",
        "GLOBAL: .claude/variants/rs/rules/testing.md — extended; added __all__ / re-export structural-enumeration sub-clause with paired Python (ast.parse) + Rust (syn::parse_file / cargo doc) examples.",
        "GLOBAL: .claude/commands/autonomize.md — NEW directive command (88 lines, replaces decisive.md).",
        "GLOBAL: .claude/commands/decisive.md — DELETED.",
        "GLOBAL: .claude/sync-manifest.yaml — tiers.co: commands/decisive.md → commands/autonomize.md; obsoleted: 3 decisive.* paths added; repos.prism.templates: kailash-coc-claude-prism removed (templates: []); LANGUAGE MAPPING + PRISM CONTENT CLASSIFICATION comments rewritten.",
        "BACKING-MEMORY: feedback_directive_recommendations.md — /decisive references → /autonomize; 2026-04-28 origin reinforcement note appended."
      ]
    },
    {
      "version": "2.9.3",
      "date": "2026-04-27",
      "summary": "PATCH: rename `/send-it` → `/decisive` + align directive with autonomous-execution + permission-envelope. (1) The slang command name `send-it` was unfamiliar to non-technical COC users (motorsports/skate slang); renamed to `decisive` which names the posture without slang. (2) Directive body rewritten to align with the user's standing principle 'autonomous execution to full with my permission, with the most optimal, complete, root-cause approach for the long-term … rigorous, prudent as well.' Added: § Prudence (permission envelope — destructive/hard-to-reverse/visible-to-others ops still require confirmation; BUILD-repo + downstream-of-USE commit/push carve-out per standing user feedback) + § Rigor (verify-before-commit — mechanical sweeps, cite paths/lines, distinguish observed-vs-assumed) + explicit root-cause-over-symptom (cross-ref `rules/zero-tolerance.md` Rule 4) + long-term-over-short-term durability framing. (3) Tier membership added to `co:` (peer of /wrapup, /journal, /start). (4) Three obsoleted entries declared (`.claude/commands/send-it.md`, `.codex/prompts/send-it.md`, `.gemini/commands/send-it.toml`) so every USE template + Codex/Gemini emission purges the old name on next downstream sync. (5) Backing memory `feedback_directive_recommendations.md` refreshed with paired prudence + rigor sections so autonomy and prudence are not traded off.",
      "changes": [
        "GLOBAL: .claude/commands/decisive.md — new directive command (84 lines, replaces send-it).",
        "GLOBAL: .claude/commands/send-it.md — DELETED.",
        "GLOBAL: .claude/sync-manifest.yaml — added commands/decisive.md to co: tier; added 3 send-it paths to obsoleted: list (.claude/commands/send-it.md, .codex/prompts/send-it.md, .gemini/commands/send-it.toml).",
        "BACKING-MEMORY: feedback_directive_recommendations.md (user memory, persists across sessions) refreshed with permission-envelope + verify-before-commit sections."
      ]
    },
    {
      "version": "2.9.2",
      "date": "2026-04-27",
      "summary": "PATCH: cross-template gitignore fix + retire kailash-coc-gemini-py. (1) Every USE template's .gitignore had `lib/` ignore + whitelist pinned to legacy `scripts/hooks/lib/`, masking the v2.9.x canonical `.claude/hooks/lib/` payload (7 sibling modules) from every commit since v2.9.0. Downstream consumers cloning post-v2.9.0 hit `Cannot find module ./lib/<sibling>` on CC startup; csq preflight reported `hook require fails: ... sibling modules missing`. Fix: replaced `!scripts/hooks/lib/` whitelist with `!.claude/hooks/lib/` across all 4 USE templates + bumped each (kailash-coc-claude-py 3.5.20→3.5.21, kailash-coc-claude-rs 2.9.15→2.9.16, kailash-coc-py 1.0.4→1.0.5, kailash-coc-rs 1.0.1→1.0.2) so downstream `/sync` re-activates. (2) Retired the dedicated `kailash-coc-gemini-py` USE template — Gemini support is built into the multi-CLI templates (`kailash-coc-py`, `kailash-coc-rs` emit GEMINI.md alongside CLAUDE.md and AGENTS.md), making the dedicated repo redundant.",
      "changes": [
        "USE-PY: kailash-coc-claude-py 3.5.21 (PR #59 d67c9e1) — `.gitignore` whitelist + first commit of 7 sibling modules to git history.",
        "USE-RS: kailash-coc-claude-rs 2.9.16 (PR #60 21ef72e) — same fix.",
        "USE-PY-MULTI: kailash-coc-py 1.0.5 (PR #3 c457fd3) — same fix.",
        "USE-RS-MULTI: kailash-coc-rs 1.0.2 (PR #2 cf45ef8) — same fix.",
        "RETIRED: kailash-coc-gemini-py — GitHub repo deleted, local clone purged. Gemini support remains via the multi-CLI USE templates' GEMINI.md emission.",
        "GLOBAL .gitignore — removed `kailash-coc-gemini-py/` from child-repos list.",
        "GLOBAL guides/co-setup/06-artifact-lifecycle.md — 'COC templates for other LLMs' Q&A rewritten to reflect the multi-CLI architecture (Gemini built into kailash-coc-py / kailash-coc-rs) and note the gemini-py retirement (2026-04-27)."
      ]
    },
    {
      "version": "2.9.1",
      "date": "2026-04-26",
      "summary": "STRUCTURAL: hooks consolidated under .claude/hooks/ (Anthropic-documented location) + sync template-resolver fixed (env override, cache-first, sibling demoted to offline-only fallback) + new declarative obsoleted-paths mechanism in sync-manifest.yaml that downstream /sync reads from <template>/.claude/.coc-obsoleted to PURGE stale orphan directories (scripts/hooks/, .claude/scripts/, scripts/resolve-template.js) on next sync. This is the only mechanism by which 30+ downstream repos can clean up the dual-source-of-truth that was causing './lib/workspace-utils not found' errors. No SDK pin deltas; pure COC infra release.",
      "changes": [
        "MANIFEST: sync-manifest.yaml — NEW top-level `obsoleted:` section listing scripts/hooks/, .claude/scripts/, and scripts/resolve-template.js as paths every consumer MUST delete on next sync. Each entry annotated with the version that introduced the obsoletion (v2.9.1). Existing settings.json exclusion comment rewritten to clarify the .claude/hooks/ canonical normalization path.",
        "GLOBAL agents/management/coc-sync.md — NEW Step 4.5 'Purge Obsoleted Paths AND Emit .coc-obsoleted'. Reads obsoleted: from manifest, recursively deletes matching paths from USE template before Step 5 writes, AND emits <USE-template>/.claude/.coc-obsoleted as a slim purpose-built file (one path per line, comments allowed) so downstream consumers can read it during their own /sync. Step 6 amended to MUST normalize hook paths in settings.json (rewrite $CLAUDE_PROJECT_DIR/scripts/hooks/* → $CLAUDE_PROJECT_DIR/.claude/hooks/*). Step 8 verification extended with 'every obsoleted path is GONE' and 'no scripts/hooks/ reference remains in settings.json' BLOCK-level checks. Important Rules amended to carve obsoletion exemption to the additive-sync default.",
        "GLOBAL commands/sync.md — Downstream Sync section reordered. Step 1 (template resolution) replaces sibling-first with env-override-first / cache-first / clone-third / sibling-as-offline-fallback. NEW Step 2 reads .coc-obsoleted from resolved template. NEW Step 3 purges obsoleted paths in consumer (only mechanism by which 30+ downstream repos clean up). NEW Step 7 normalizes settings.json hook paths from scripts/hooks/ → .claude/hooks/. Step 8 grep-asserts zero remaining scripts/hooks/ refs.",
        "GLOBAL commands/sync-to-build.md — 'Scripts/hooks' subsection renamed 'Hooks (.claude/hooks/, always updated — these are CC infrastructure)'. Apply step extended with obsoleted-paths purge + .coc-obsoleted emission so BUILD-repo consumers inherit the same purge contract.",
        "VARIANT-RS variants/rs/commands/sync.md + VARIANT-RB variants/rb/commands/sync.md — full-file overlays updated with the new env-override-first resolution order, .coc-obsoleted reading (Step 2.5), and obsoleted-purge (Step 2.6). Same shape as the global commands/sync.md.",
        "NEW BIN: .claude/bin/resolve-template.js — canonical thin shim that requires ../hooks/lib/template-resolver. Replaces the legacy loom/scripts/resolve-template.js (broken — required ./hooks/lib/template-resolver against a path that no longer exists; added to obsoleted: list).",
        "INFRA hooks/lib/template-resolver.js — full rewrite. Resolution order changed: (1) KAILASH_COC_TEMPLATE_PATH env var, (2) cache at ~/.cache/kailash-coc/<template>/ with auto-update via git fetch + reset --hard, (3) shallow clone from GitHub if no cache, (4) local sibling as OFFLINE-ONLY fallback (used only when network is unreachable; emits 'freshness NOT guaranteed' notice). When a sibling is detected during online resolution but bypassed, emits one-line stderr UX nudge telling the user how to opt back in via env var. Adds known-template slugs for kailash-coc-py and kailash-coc-rs (NEW USE templates).",
        "GLOBAL agents/codex-architect.md — replaced 4 occurrences of 'scripts/hooks/*.js' → '.claude/hooks/*.js' (ownership matrix lines 10, 19, 34; MCP Guardrail Companion line 68). The MCP-guard bijection (validator 13) now extracts predicate functions from .claude/hooks/*.js, matching where the files actually live in loom and the new USE templates.",
        "GLOBAL agents/cli-orchestrator.md — codex-architect routing card updated to '.claude/hooks/*.js' (line 78).",
        "INFRA hooks/validate-workflow.js — observation-skip allowlist amended with comment noting /scripts/hooks/ is deprecated post-v2.9.1; the matcher is retained transitionally to skip orphan files in legacy USE templates that haven't yet purged. Safe to remove entirely once every downstream consumer has run /sync past v2.9.1.",
        "ROOT rules/cross-repo.md (~/repos/.claude/rules/cross-repo.md) — MUST Rule 3 FLIPPED. Was: 'COC Sync Includes Scripts — MUST sync .claude/ AND scripts/ directory; omitting scripts/hooks/ is the #1 sync failure mode.' Now: 'COC Artifacts Live Under .claude/ — hooks at .claude/hooks/, resolver at .claude/bin/, scripts/ is project-ops only and MUST NOT be written by COC sync.' MUST Rule 4 'Preserve Target-Only Files' amended to carve the obsoletion exemption: paths in <template>/.claude/.coc-obsoleted MAY and MUST be deleted on every sync, overriding additive-preservation.",
        "FIX (Issue 1 — sync UX): downstream /sync no longer requires users to `git pull` two repos before working. Local clones of templates are now offline-only fallback; origin/main is always authoritative for the default online path. Loom developers iterating on un-pushed template changes set KAILASH_COC_TEMPLATE_PATH=/abs/path explicitly.",
        "FIX (Issue 2 — three-way hook drift): single canonical hooks location at .claude/hooks/ across loom + USE templates + BUILD repos + downstream consumers. The './lib/workspace-utils not found' error class (caused by sync copying foo.js to one location and lib/ to another) is structurally impossible after this release."
      ]
    },
    {
      "version": "2.9.0",
      "date": "2026-04-26",
      "summary": "PY CYCLE 8 + RS CYCLE 5: dual-SDK /codify proposals (kailash-py 2026-04-26 Wave 4 + kailash-rs 2026-04-25 v3.23). Cross-SDK convergence: BOTH proposals independently raised the 'Verify Specialist Tool Inventory' rule (py from Wave 4 security-reviewer misdelegation; rs from Wave 2 W3 same misdelegation pattern) — merged into ONE GLOBAL rule in agents.md. 6 net global rule additions + 1 rs-variant skill copy + 1 rs-variant build-speed extension. SDK pin deltas: kailash 2.10.0→2.11.1, kailash-dataflow 2.2.0→2.3.1, kailash-mcp 0.2.9→0.2.10 (per Wave 4 PRs #631 #632 #634; PyPI-live). Templates resynced: kailash-coc-claude-py, kailash-coc-py, kailash-coc-claude-rs, kailash-coc-rs.",
      "changes": [
        "GLOBAL rules/agents.md += MUST sub-rule 'Verify Specialist Tool Inventory Before Implementation Delegation' (~30 lines) + Examples slot += 'Tool Inventory Verification' (specialist tool-inventory table for 17+ specialists). Read-only specialists (security-reviewer, analyst, reviewer, gold-standards-validator, value-auditor — tool set Read/Write/Grep/Glob, no Edit, no Bash) MUST NOT be delegated implementation work. 5 BLOCKED rationalizations. Origin merges Session 2026-04-26 Wave 4 (kailash-py, alg_id Layer-1 + JWT iss claim) AND 2026-04-25 v3.23 sprint Wave 2 W3 (kailash-rs, CodeQL Class 1 fingerprint helper). Cross-SDK independent re-discovery same failure mode → ONE merged global rule.",
        "GLOBAL rules/build-repo-release-discipline.md += MUST Rule 5 'PR Review MUST Sweep Sub-Package Version Bumps Before Merge' (~35 lines). Mechanical pre-merge sweep: for each sub-package whose src/ has diff-changes, the SAME PR MUST also modify pyproject.toml::version AND src/<pkg>/__init__.py::__version__. Bash sweep snippet for /redteam or PR-review automation. 5 BLOCKED rationalizations. Cross-SDK applicable: same Cargo.toml + lib.rs version atomicity in kailash-rs. Origin: kailash-py Wave 4 PR #632 (mcp/auth/* security fix) merged without bumping mcp 0.2.9; required separate fix-PR #634 (1 extra CI cycle, 1 extra admin merge).",
        "GLOBAL rules/cross-sdk-inspection.md += MUST Rule 4 'Cross-SDK Hash / Fingerprint Helpers MUST Pin Byte Vectors From Sibling SDK' (~35 lines). Any helper claiming byte-shape parity (fingerprint_secret, mask_url, audit_chain_hash) MUST pin ≥3 byte-vectors empirically derived from sibling SDK + sentinel coverage (empty input, all-zero, all-one, single-byte). Length+regex assertions BLOCKED as sole proof. 5 BLOCKED rationalizations. Origin: kailash-rs PR #598 first cut shipped Blake2bMac<U4> empty-key MAC mode while kailash-py uses Blake2bVar(4) digest mode + empty-input sentinel '00000000'; 4 hex chars vs 16 hex chars; cross-SDK log correlation silently broken until 2 reviewers caught it. Renumbered prior Rule 4 'Inspection Checklist' → Rule 5.",
        "GLOBAL rules/git.md += MUST sub-rule 'Release-Prep PRs MUST Use release/v* Branch Convention' (~38 lines) + MUST sub-rule 'Pre-FIRST-Push CI Parity Discipline' (~35 lines). Inserted in Branch Naming section so always-loaded baseline surfaces both at branch-name-decision time. Cross-references rules/ci-runners.md MUST Rule 8 (release-prep skip) + language-specific build-speed.md (Rust)/equivalents (Python) for the local CI parity command set. 6 BLOCKED rationalizations across both. Origin: kailash-rs PR #602 (release-prep on feat/v3.23.0-release-prep instead of release/v3.23.0 → ~73 min wasted GHA-billable runner time) AND PR #598 (5 sequential pushes → 71 min cancelled-but-billed Workspace Tests via concurrency: cancel-in-progress).",
        "GLOBAL rules/ci-runners.md += MUST Rule 6a 'Binding-CI paths-ignore Covers ALL Doc-Only Surfaces' (~32 lines) + MUST Rule 7 'Workflow Crons MUST Have Explicit Cost Footer' (~32 lines) + MUST Rule 8 'Release PRs MUST Skip The PR-Gate Suite' (~50 lines). 6a extends paths-ignore to .claude/**, docs/**, specs/**, workspaces/**, memory/**, .github/ISSUE_TEMPLATE/**, .github/PULL_REQUEST_TEMPLATE.md (saves ~150-200 min/month per gh-manager audit). 7 mandates cost-footer comment block at top of every cron-triggered workflow with cadence + worst-case monthly minutes + fast-exit semantics; cadence ≥ once-per-hour without fast-exit BLOCKED. 8 mandates `if: !startsWith(github.head_ref, 'release/')` on every PR-gate job. 13 BLOCKED rationalizations across all three. Origin: 2026-04-25 kailash-rs gh-manager CI burn audit (90% of 3,000-min free tier consumed; ~580 min in 24h, 26% avoidable).",
        "VARIANT-RS rules/build-speed.md += MUST sub-rule 'Run Full CI Job-Set Locally Before FIRST Push AND Before Admin-Merge' (~38 lines). Five-command checklist (cargo +nightly fmt --all --check, cargo +1.95 clippy --workspace --all-targets -- -D warnings, cargo nextest run --workspace, RUSTDOCFLAGS=-Dwarnings cargo doc default-features, RUSTDOCFLAGS=-Dwarnings cargo doc --no-default-features) applied to BOTH first-push and admin-merge gates. 8 BLOCKED rationalizations. Origin: kailash-rs v3.23 sprint #566/#596/#597 admin-merged with cargo check clean but nightly-fmt + rustdoc -Dwarnings dirty; PR #598 inherited all three classes via rebase; three fix-up commits cost three CI cycles. Cross-references global rules/git.md § Pre-FIRST-Push CI Parity Discipline.",
        "VARIANT-RS skills/18-security-patterns/rls-security-definer-preauth-carveout.md NEW (517 lines, copied from kailash-rs source-of-truth). Adds rs-specific 'Using the builder (kailash-rs only)' section documenting typed Rust SecurityDefinerBuilder API (crates/kailash-dataflow/src/migration/security_definer.rs, kailash-rs v3.21+, issue #571). Documents four-statement emission, T7 timing-close reminder enforcement, T8 multi-tenant-filter typed-error contract, identifier-safety routing through sql_safety::validate_identifier. Global skill (465 lines) stays language-neutral with threat model T1-T10 + 9 MUST properties + SQL recipe + pgTAP/pytest-asyncpg test templates. Per variant overlay semantics, rs USE templates get the builder-augmented version; py/rb USE templates get the language-neutral baseline. Origin: terrene-foundation/kailash-coc-claude-rs#58 ask 2 — ship SECURITY DEFINER carveout pattern as first-class COC recipe with kailash-rs builder reference.",
        "GLOBAL skills/18-security-patterns/dataflow-rls-posture.md no-op (already at loom global, language-neutral framing matches proposal classification — verify-only).",
        "MANIFEST: sync-manifest.yaml — no edits required (rules + skills already in correct tier classifications; new variant skill follows existing variants/rs/skills/ pattern).",
        "SDK PIN DELTAS (PyPI-live per Wave 4): kailash 2.10.0→2.11.1; kailash-dataflow 2.2.0→2.3.1; kailash-mcp 0.2.9→0.2.10. Held: kailash-kaizen 2.13.1, kailash-ml 1.1.1, kailash-align 0.6.0, kailash-pact 0.10.0, kailash-nexus 2.3.0, kaizen-agents 0.9.4 (all at prior PyPI-live tips, no PR diff this cycle).",
        "TEMPLATE RESYNC: kailash-coc-claude-py + kailash-coc-py + kailash-coc-claude-rs + kailash-coc-rs all bumped (rule files mirrored, py SDK pins bumped, multi-CLI baseline regen for kailash-coc-py / kailash-coc-rs, .coc-sync-marker updated, VERSION updated).",
        "PROPOSALS: kailash-py/.claude/.proposals/latest.yaml advanced pending_review → reviewed (2026-04-26) → distributed; kailash-rs/.claude/.proposals/latest.yaml advanced pending_review → reviewed (2026-04-26) → distributed. Both with code-reference: this loom commit. Cross-SDK independent re-discovery of Tool Inventory rule documented in both archives.",
        "SKIP entries (rs proposal, no rule update — tracking issues only): pyrightconfig-pyo3-stub-resolution (rs#600), yaml-hygiene-proposal-files (rs#595), jwt-iss-absent-cross-sdk-gap (rs#599 + py cross-SDK pair), gha-config-hardening-pr (mechanical YAML edits same-PR), pg-execution-store-roundtrip-isolation (rs#603 — test-isolation race, defer to v3.23.1 or test-only fix)."
      ]
    },
    {
      "version": "2.8.30",
      "date": "2026-04-25",
      "summary": "PY CYCLE 7: kailash-py /codify proposal 2026-04-25 (kailash-kaizen 2.13.0→2.13.1 hotfix postmortem) — 2 entries, both classified GLOBAL, both bodies language-agnostic so no py-variant overlay needed. Cross-SDK QUEUED for kailash-rs absorbs as-is. Templates resynced: kailash-coc-claude-py 3.5.18→3.5.19, kailash-coc-py 1.0.2→1.0.3. SDK pins bumped where PyPI-live: kailash 2.9.1→2.10.0, kailash-nexus 2.2.0→2.3.0, kailash-kaizen 2.12.2→2.13.1. kailash-dataflow 2.2.0 held (BUILD-only, not yet PyPI-live per proposal session note). All other packages already at PyPI-resolvable tips from prior cycle.",
      "changes": [
        "GLOBAL rules/dependencies.md +33 lines (156→189): NEW MUST sub-rule '`__init__.py` Module-Scope Imports Honor The Manifest' inserted between 'Treat dependency resolution errors as blocking failures' and 'BLOCKED Anti-Patterns'. Every unconditional `import X`/`from X import Y` at module scope in `__init__.py` MUST resolve to a declared dep; optional sibling-package proxy aliases MUST be wrapped in `try/except ImportError` with side-effects in `else` branch. 5 BLOCKED rationalizations. Distinct from existing `redis = None` silent-fallback anti-pattern: the optional-proxy form has NO later use site that could break, making try/except + else the structural-correct form (vs silent-fallback which degrades production paths). Pairs with build-repo-release-discipline.md Rule 2 (catches the failure) as the structural defense (prevents the failure class). Origin: kailash-kaizen 2.13.1 hotfix commit 9002c002 — 4 unconditional `kaizen_agents.patterns.*` imports in `kaizen/orchestration/__init__.py` (predating refactor #75) raised ModuleNotFoundError on every clean `pip install kailash-kaizen`. Cross-SDK applicable: same pattern in Rust workspace path-deps without Cargo.toml dep, Ruby require without gemspec dep, Node.js require without package.json dep.",
        "GLOBAL rules/build-repo-release-discipline.md +2 lines (165→167): NEW 'Latent failures count' paragraph extending Rule 2's PyPI-cache-lag block + amended Why line. When clean-venv check fails, hotfix scope is the broken pattern NOT just the most-recent PR's diff. 4 BLOCKED rationalizations: 'It's been working' / 'this PR didn't introduce it' / 'main was green' / 'the most recent PR is the only suspect' / 'we'll cover the rest in a separate hotfix'. Cross-references the new dependencies.md MUST sub-rule as the structural defense. Origin: kailash-kaizen 2.13.1 hotfix scope was the FOUR `kaizen_agents.*` unconditional imports — only ONE was new in 2.13.0 (PR #602's OrchestrationRuntime extension); the other THREE had been in main since refactor #75 and would have failed every clean install since then. Limiting the hotfix to 'what #602 added' would have left the latent class intact.",
        "MANIFEST: sync-manifest.yaml — no edits required (both rule files are pre-existing global tier).",
        "SDK PIN DELTAS (PyPI-resolvable): kailash 2.9.1→2.10.0; kailash-nexus 2.2.0→2.3.0; kailash-kaizen 2.12.2→2.13.1. Held: kailash-dataflow 2.2.0 (BUILD feat-branch only, not yet PyPI). Unchanged: kailash-mcp 0.2.9, kailash-ml 1.1.1, kailash-align 0.6.0, kailash-pact 0.10.0, kaizen-agents 0.9.4 (all at prior PyPI-live tips).",
        "TEMPLATE RESYNC: kailash-coc-claude-py 3.5.18→3.5.19 (rules/dependencies.md + rules/build-repo-release-discipline.md mirror; pyproject.toml SDK pins; .coc-sync-marker; uv sync). kailash-coc-py 1.0.2→1.0.3 (rules mirror + VERSION.sdk_packages capture; no root pyproject.toml so no uv sync; multi-CLI AGENTS.md/GEMINI.md regeneration deferred — body deltas are inside slot:neutral-body so cross-CLI parity is preserved by design and the baseline line count delta is +35 well under WARN cap).",
        "PROPOSAL: kailash-py/.claude/.proposals/latest.yaml advanced pending_review → reviewed (2026-04-25T11:00+08:00) → distributed after Gate 2 completes.",
        "CROSS-SDK QUEUED for kailash-rs sync cycle: both rules port as-is. (1) `__init__.py` module-scope imports rule ≡ Rust workspace `mod foo;` declaration without a Cargo.toml dep on the workspace-mate's published crate; (2) latent-failure clause ≡ `cargo install` from a clean registry surfaces what `cargo build` against editable workspace-mates hides. No rs-variant overlay required; bodies are language-agnostic and the kailash-py example anchor is informational only."
      ]
    },
    {
      "version": "2.8.29",
      "date": "2026-04-24",
      "summary": "Follow-up from 2.8.28 pre-commit audit. Completes the frontmatter-regression sweep + adds Validator 14 (emission-time rule-frontmatter enforcement). Fixes: (1) worktree-isolation.md 2.8.27 regression — frontmatter replaced priority:0/scope:baseline with paths-only; restored to baseline. (2) 8 long-standing Rule 7 violations — path-scoped rules (agent-reasoning, cross-sdk-inspection, dependencies, observability, patterns, python-environment, specs-authority, testing) had `paths:` only; added `priority: 10` + `scope: path-scoped`. (3) New Validator 14 in emit.mjs checks rule-authoring.md Rule 7 pair consistency at emission-time — missing-frontmatter now BLOCKS emit instead of silently skipping the rule. Regenerated multi-CLI baselines: kailash-coc-py/AGENTS.md + GEMINI.md went from 344 lines (5 rules — baselines silently dropped 4 rules with missing frontmatter) to 779 lines (9 rules: now includes agents, security, worktree-isolation, zero-tolerance). Templates resynced: kailash-coc-py 1.0.1→1.0.2; kailash-coc-claude-py rule files mirrored.",
      "changes": [
        "REGRESSION FIX worktree-isolation.md: 2.8.27 cycle replaced `priority: 0, scope: baseline` frontmatter with paths-only (`.claude/agents/**, .claude/commands/**, **/agents/**`), silently demoting a CRIT baseline rule to path-scoped. Restored to `priority: 0, scope: baseline`. Emitted baseline rule count: 8 → 9 (now matches sync-manifest.yaml per_rule_size_budget_bytes entry count).",
        "RULE 7 COMPLIANCE SWEEP — 8 path-scoped rules previously shipped with `paths:` only (no `priority:` or `scope:`), violating rule-authoring.md Rule 7 since the rule was authored (commit 14f6b2e). Added `priority: 10, scope: path-scoped` to: agent-reasoning.md, cross-sdk-inspection.md, dependencies.md, observability.md, patterns.md, python-environment.md, specs-authority.md, testing.md. Paths lists unchanged. All 48 rule files now pass Rule 7 audit.",
        "NEW Validator 14 in .claude/bin/emit.mjs — `validateRuleFrontmatter()` enforces rule-authoring.md Rule 7 at emission-time. Checks: (a) every .md in .claude/rules/ has a frontmatter block; (b) block declares BOTH `priority:` (0/10/20) AND `scope:` (baseline/path-scoped/skill-embedded/excluded); (c) pair consistency — priority:0⇒scope:baseline, priority:10⇒scope:path-scoped+paths:, priority:20⇒scope:skill-embedded|excluded; (d) scope:excluded requires exclude_from:[cli,...]. FAIL prints all violations and exits 1 BEFORE any CLI-specific work. Replaces the silent-drop behavior in getCritBaseline() that allowed the 2.8.27 regression to pass through /sync undetected.",
        "MULTI-CLI BASELINE REGENERATION — node emit.mjs --all regenerated kailash-coc-py/AGENTS.md + GEMINI.md from 344 lines (5 rules) to 779 lines (9 rules). Newly-included rules: agents.md, security.md, worktree-isolation.md, zero-tolerance.md — all 4 were silently dropped from 2.8.28's baselines because their frontmatter was missing or wrong. Codex + Gemini users of the multi-CLI template now get full rule surface at session start. Tier: WARN (48322B, 78% of 61440B block cap — steady-state per v6 §2.2).",
        "TEMPLATE RESYNC: kailash-coc-claude-py mirrored 9 rule files; kailash-coc-py mirrored 9 rule files + AGENTS.md regenerated + GEMINI.md regenerated + .coc-sync-marker updated (1.0.1→1.0.2).",
        "RS VARIANT + KAILASH-COC-RS: unchanged this cycle (py-only /sync). Same 8 Rule 7 violations may exist in rs-side path-scoped rules; audit queued for next /sync rs cycle. worktree-isolation.md regression was global, so rs variant would have the same demotion — rs resync will also regenerate rs-side baselines.",
        "FALSE ALARM closed: 8 `kailash.mcp_server` occurrences flagged as DEFERRED in 2.8.27 were already fixed in commit b6cbdec (2026-04-14, PR #451). The grep at flag-time caught historical prose in VERSION log text, not live code. No live-code fix required; 2.8.28 changelog notes this for audit trail."
      ]
    },
    {
      "version": "2.8.28",
      "date": "2026-04-24",
      "summary": "PY CYCLE 6 delta: kailash-py /codify proposal 2026-04-23 entries 21-25 (5 new entries appended by release cycle + CodeQL round-2 codify). Entries 1-20 distributed in 2.8.27. 5 new CANDIDATE-rules drafted directly at loom per proposal text: (21) PEP 562 callable-module subpackage coexistence → new § in global rules/patterns.md; (22) Python 3.11+ threading.Lock/RLock factory gotcha → new MUST Rule 5 in global rules/python-environment.md; (23) TYPE_CHECKING block for lazy __getattr__ exports → new Rule 6b in global rules/orphan-detection.md; (24) Scanner-finding deferral requires tracking issue + runtime-safety proof → new Rule 1b in global rules/zero-tolerance.md (extends Rule 1a); (25) Bi-directional sibling-package CI install at bridge boundaries → new § in variants/py/rules/deployment.md (extends existing Sibling-Package CI rule). All 4 cross-SDK-applicable rules land global; bi-directional bridge rule stays variant-py to match parent rule placement. Templates resynced: kailash-coc-claude-py 3.5.16→3.5.17, kailash-coc-py 1.0.0→1.0.1. No SDK pin deltas this cycle (all BUILD packages still pre-release; PyPI unchanged).",
      "changes": [
        "NEW § global rules/patterns.md 'Callable Module + Subpackage Coexistence (MUST — PEP 562)' (+36 lines, 172→208). When pkg exports top-level callable pkg.foo AND contains pkg/foo/ subpackage, subpackage's __init__.py MUST install _CallableModule(ModuleType) via sys.modules[__name__].__class__ = _CallableModule. Both surfaces coexist: `from pkg.foo import Klass` resolves subpackage; `pkg.foo(...)` invokes wrapper. 4 BLOCKED rationalizations. Evidence: km.dashboard shadowed by kailash_ml/dashboard/ after Py3.14 test-collection reorder; fix commit 8914de3b installed _CallableDashboardModule. Cross-SDK: Rust has analogous dual-name issues via `use pkg::foo` vs `pkg::foo` module — principle language-neutral; PEP 562 syntax Python-only.",
        "NEW MUST Rule 5 global rules/python-environment.md 'Python 3.11+ Lock Factory Gotcha' (+25 lines, 137→162). threading.Lock/RLock are factories (not classes) in Py3.11+; isinstance() against them raises TypeError. Fix: module-scope constant `_LOCK_TYPES = (type(threading.Lock()), type(threading.RLock()))`. 4 BLOCKED rationalizations. Evidence: pact/ml/__init__.py:412 blocked all 5 PACT ML bridge integration tests on Py3.11+; fix commit 5655dd59. Python-specific gotcha.",
        "NEW Rule 6b global rules/orphan-detection.md 'TYPE_CHECKING Block For Lazy __getattr__ Exports' (+33 lines, 231→264). Packages lazy-loading heavy optional deps (torch, vllm, catboost) via __getattr__ MUST still expose symbols to CodeQL/pyright/mypy/Sphinx via TYPE_CHECKING block; eager-importing defeats lazy design; removing from __all__ breaks `from pkg import *`. 4 BLOCKED rationalizations. Evidence: commit 7943b3a1 closed 17 CodeQL py/undefined-export findings in kailash_align/__init__.py without forcing torch eager. NOTE: orphan-detection.md now 264 lines, was already over 200 budget pre-delta; queued for dedicated refactor cycle per prior deferred list.",
        "NEW Rule 1b global rules/zero-tolerance.md 'Scanner Deferral Requires Tracking Issue + Runtime-Safety Proof' (+39 lines, 145→184). Extends Rule 1a with 4-condition legitimate-deferral path: (1) runtime-safety proof citing guard lines, (2) tracking issue with acceptance criteria, (3) release PR body link, (4) release-specialist agreement. Missing any → silent dismissal = BLOCKED. 5 BLOCKED rationalizations. Evidence: PR #611 release cycle — 17 py/unsafe-cyclic-import findings deferred via issue #612 after TYPE_CHECKING-guard verification; 23 other CodeQL errors fixed in same PR. Closes the rationalization gap Rule 1a left for architectural-refactor-scale findings.",
        "NEW § variant-py rules/deployment.md 'Bi-Directional At Bridge Boundaries (MUST)' extending existing Sibling-Package CI rule (+37 lines, 207→244). At cross-package bridges where two sub-packages import each other's modules (kailash-ml ↔ kailash-align RL bridge; kailash-dataflow ↔ kailash-ml fabric bridge), BOTH sibling CI jobs MUST install BOTH packages editable. Uni-directional form insufficient at bridges. 5 BLOCKED rationalizations. Evidence: PR #611 CI cascade — align→ml reciprocal install at commit e7c5a33b; ml→align surfaced next at commit c59c30da; both directions identical discipline. Variant-py placement matches parent rule; rs/rb variants may need equivalent bridge clauses (Cargo workspaces, gem paths) in later cycles.",
        "SDK PINS: no change this cycle. All BUILD feat-branch packages still pre-release (kailash-ml 1.1.0, kailash 2.9.0, kailash-pact 0.10.0, etc.); PyPI tips unchanged from 2.8.27 cycle.",
        "TEMPLATE RESYNC: kailash-coc-claude-py 3.5.16→3.5.17 + kailash-coc-py 1.0.0→1.0.1. 5 rule files updated at both targets; new sub-files: 0; SDK pin deltas: 0.",
        "PROPOSAL: kailash-py/.claude/.proposals/latest.yaml 25 entries (1-25). Status advanced pending_review → reviewed (2026-04-24) → distributed after Gate 2 completes. Next /codify at BUILD will archive this proposal and create fresh per artifact-flow 'Archive Before Fresh'.",
        "FRONTMATTER REGRESSION FIX (2026-04-24 pre-commit audit): 2.8.27 rule-extraction cycle stripped YAML frontmatter from 4 rules — agents.md, orphan-detection.md, security.md, zero-tolerance.md (violates rule-authoring.md Rule 7 'Every rule MUST declare both priority: and scope:'). Fix: reinstated frontmatter at canonical values (agents/security/zero-tolerance = priority:0/scope:baseline; orphan-detection = priority:10/scope:path-scoped/paths:[packages/**,src/**,tests/**]). Compounding fix on agents.md: restored slot:neutral-body + slot:examples partition (15 CC-specific Agent(...) references had leaked into the neutral body; moved to Examples section per rule-authoring.md Rule 8 + cross-cli-parity.md Rule 1). Mirror-applied to kailash-coc-claude-py + kailash-coc-py templates. Multi-CLI AGENTS.md/GEMINI.md baselines NOT regenerated this commit — deferred to next /sync py cycle (emit.mjs will surface the 4 now-valid rules into the Codex/Gemini baselines). FOLLOW-UP: emit.mjs silently skipped rules without frontmatter rather than failing at emission-time validation per rule-authoring.md Rule 7 §A.1; validator gap to audit next /cc-audit cycle.",
        "CROSS-SDK QUEUED for kailash-rs: entry 21 PEP 562 principle (Rust dual-name `use pkg::foo` vs module resolution) — rs-variant overlay optional; entry 22 Python-only (no rs equivalent); entry 23 TYPE_CHECKING language-neutral principle but Python syntax (rs has cfg(doc) / doc(hidden) attributes as parallel); entry 24 CodeQL deferral cross-SDK as-is; entry 25 bi-directional bridge applies to rs workspace path-deps (companion rs-variant deployment.md § expected on next /codify)."
      ]
    },
    {
      "version": "2.8.27",
      "date": "2026-04-23",
      "summary": "PY CYCLE 6: kailash-py /codify proposal 2026-04-23 (kailash-ml 1.0.0 M1 foundations + deferred-codify backlog) — 20 entries, 76 files in BUILD diff. 75/76 classified GLOBAL (1 SKIP = proposal yaml); 0 variant-py overlays; 0 regressions. 15 NET-NEW files at loom: 10 guides/rule-extracts/*.md (rule-authoring 200-line-budget extractions) + 3 skills/04-kaizen/{kaizen-key-concepts,kaizen-wrapper-composition,kaizen-provider-config-v25}.md (C3 progressive-disclosure split) + skills/10-deployment-git/multi-package-release-wave.md + skills/34-kailash-ml/m1-release-wave.md. 60 MODIFIED files at loom (agents, rules, skills, commands). 8 new M1 pattern MUST clauses land across 6 rule files (worktree-isolation Rules 4/5/6 — Opus burst ≤3 + pre-flight merge-base + branch-name-matches-prompt; specs-authority Rule 5c — orchestrator amends stale todo; testing.md E2E regression tier; zero-tolerance §2 fake-integration via missing handoff field; patterns.md paired async-ness; orphan-detection.md Rule 6a __all__ reconciliation). CRITICAL fixes: C1 dataflow-specialist.md line 41 `from dataflow import DataFlow` (was `from kailash.dataflow`); C2 `esperie-enterprise/kailash-rs` → `esperie/kailash-rs` (3 files, rules/independence.md compliance); C3 kaizen SKILL.md 670→247 LOC refactor; MCP import path fix (`kailash.mcp_server` → `kailash_mcp`) in 05-kailash-mcp/SKILL.md. 10 rules extracted to companion guides (2,767 → 1,828 LOC, 34% reduction total). 11 skill descriptions slimmed (H1) to ≤120 chars. Testing H2 alignment (Tier 2/3 NO mocking). SDK pin: kailash-kaizen 2.10.1→2.11.0 (only PyPI-resolvable delta; all other packages already at PyPI-resolvable tips per 2026-04-21 Wave 1+2 release cycle). BUILD feat branch `feat/kailash-ml-1.0.0-m1-foundations` pre-release — kailash-ml 1.1.0, kailash 2.9.0, etc. held at PyPI-resolvable per variants/py/rules/deployment.md.",
      "changes": [
        "8 NEW M1 PATTERN MUST CLAUSES landed across 6 rule files. (1) rules/worktree-isolation.md Rule 4 'Parallel-Launch Burst Size Limit (Waves of ≤3)' — Anthropic server-side throttle kills 4-6 simultaneous Opus worktree agents; fallback to ≤3 succeeds. Evidence: session 2026-04-23 M10 launch (6 agents all rate-limited at 34-45s; waves of 3 completed). (2) Rule 5 'Pre-Flight Merge-Base Check Before Worktree Launch' — orchestrator pins base SHA + verifies merge-base equals target tip at launch time; 5 of 6 M10 shards branched from stale SHA worked-by-accident only because packages didn't overlap. (3) Rule 6 'Worktree Branch Name MUST Match Prompt Declaration' — orchestrator passes `-b <branch>` explicitly + agent verifies `git rev-parse --abbrev-ref HEAD` matches before first commit; 3 of 6 M10 shards defaulted to hash-based worktree-agent-<hash> names, escaping `feat/*` grep. (4) rules/specs-authority.md Rule 5c 'Orchestrator MUST Amend Todo Text At Launch When Spec Has Moved' — cross-check todo claims against current canonical spec + package state; amend in place. Evidence: W32-32b todo said align 0.5.0, W30.3 already shipped 0.5.0; amended to 0.6.0 at launch saved a failed shard. (5) rules/testing.md § 'End-to-End Pipeline Regression Tests Above Unit + Integration' — every canonical pipeline docs teach MUST have Tier-2+ regression test executing DOCS-EXACT code. Evidence: kailash-ml 1.0.0 W33b test_readme_quickstart_executes_end_to_end caught km.train → km.register pipeline break (TrainingResult missing trainable field) invisible to per-primitive unit tests. (6) rules/zero-tolerance.md Rule 2 extended with 'Fake integration via missing handoff field' BLOCKED example — frozen dataclass on pipeline critical path omits field next primitive consumes; passes every unit test, breaks every E2E install. (7) rules/patterns.md § 'Paired Public Surface — Consistent Async-ness' — canonical user pipelines (train/register, fit/predict) MUST be consistently async OR sync; mixing async with sync-wrapping-asyncio.run() BLOCKED (`RuntimeError: This event loop is already running` in pytest-asyncio/Nexus/Jupyter). Evidence: km.train async + km.register sync (asyncio.run) crashed every async caller; fix fdd3040e converted register to async def. (8) rules/orphan-detection.md Rule 6a 'Merge-Time __all__ Reconciliation Across Shard Base-SHAs' — parallel-worktree shards from different base SHAs editing same __all__ silently lose additions; 4-step reconciliation protocol (prefer HEAD + preserve invariants from older base + update count-dependent tests + re-run module-scope import check).",
        "10 RULE FILES EXTRACTED to companion guides/rule-extracts/ per rules/rule-authoring.md 200-line budget: testing.md (511→260), observability.md (305→224), zero-tolerance.md (284→145), agent-reasoning.md (273→185), specs-authority.md (261→190), agents.md (232→198), dependencies.md (228→154), python-environment.md (227→137), worktree-isolation.md (225→148), security.md (221→187). Aggregate 2,767 → 1,828 LOC (34% reduction). Each rule retains frontmatter, opening paragraph, every MUST/MUST NOT clause with ONE representative DO/DO NOT, BLOCKED rationalizations verbatim, Why line ≤2 sentences, one-line Origin pointer, 'See .claude/guides/rule-extracts/<name>.md for ...' pointer. Each companion guide (NEW) holds: extended DO/DO NOT examples, extended BLOCKED lists, session numbers + PR numbers + dates, multi-paragraph Why rationale, post-mortem prose, relationships, audit protocols. Slot markers preserved verbatim per rule-authoring.md §8 cross-CLI parity. NOTE: testing.md (260) + observability.md (224) remain modestly over budget — CRIT baseline rules with 8-10 MUST clauses each; deferred for dedicated refactor cycle.",
        "CRITICAL FIXES from /cc-audit deep content audit. C1 — agents/frameworks/dataflow-specialist.md line 41 cited `from kailash.dataflow import DataFlow` (ImportError); corrected to `from dataflow import DataFlow` matching canonical namespace used in all skills + remainder of same file. C2 — `esperie-enterprise/kailash-rs` (factually wrong + Foundation-independence concern per rules/independence.md) replaced with canonical `esperie/kailash-rs` across agents/frameworks/kaizen-specialist.md, rules/cross-sdk-inspection.md, skills/04-kaizen/kaizen-llm-deployment.md. C3 — skills/04-kaizen/SKILL.md: description 3,438 → 112 chars (target ≤120 per rules/cc-artifacts.md §1); body 670 → 247 LOC via progressive-disclosure split to 3 NEW sub-files (kaizen-key-concepts 124 LOC, kaizen-wrapper-composition 117 LOC, kaizen-provider-config-v25 66 LOC). All advanced topic groups relocated; zero content deletion. CRITICAL IMPORT PATH FIX — skills/05-kailash-mcp/SKILL.md cited `kailash.mcp_server` (old); MCP refactored into own package at packages/kailash-mcp/ with canonical import `kailash_mcp`. Every downstream user following this skill hit ModuleNotFound until now.",
        "H1 SKILL DESCRIPTION SLIMS — 10 SKILL.md descriptions slimmed 500-858 chars → ≤120 chars each (selection-hook only): skills/{01-core-sdk,02-dataflow,03-nexus,06-cheatsheets,07-development-guides,09-workflow-patterns,17-gold-standards,25-ai-interaction-patterns,29-pact,31-error-troubleshooting}/SKILL.md. Original keyword lists preserved in each skill body's 'When to Use' section. Aggregate ~6k chars saved per agent-selection turn.",
        "H2 TESTING ALIGNMENT — skills/12-testing-strategies/SKILL.md description + body now say 'Tier 2/3 real infra (NO mocking)'; table's Tier 2/3 Mocking column changed from 'Real infra recommended' to 'BLOCKED — real infra only' to align with rules/testing.md § Tier 2 'NO mocking (@patch, MagicMock, unittest.mock — BLOCKED)'. Also fixed skills/17-gold-standards/gold-mocking-policy.md for same alignment.",
        "H3 NEXUS DESCRIPTION FIX — agents/frameworks/nexus-specialist.md description reworded from 'FastAPI/Flask BLOCKED' (self-contradicted by lines 135/196 describing Nexus's internal use of FastAPI transports) to 'direct FastAPI/Flask BLOCKED (use Nexus; Nexus itself uses FastAPI internally)'.",
        "STALENESS REFRESH — 6 agent files last-modified 2026-04-04 (19 days stale) refreshed: (1) malformed YAML frontmatter in open-source-strategist.md (broken tools list bleeding into body), (2) duplicate section '10' in security-reviewer.md (PACT + Auth both numbered 10 — dedup), (3) stale 'Kailash RS proprietary' framing in agents/quality/gold-standards-validator.md (Foundation-owned per rules/independence.md), (4) flutter-specialist.md dedup, (5) implementation/{pattern-expert,build-fix}.md cross-refs to framework-first pattern. Net -23 LOC.",
        "DEAD-REF CLEANUP (commit 0d37d7d5) — 9 dead cross-references fixed. (1) commands/codify.md cited agents/_subagent-guide.md (never existed) → replaced with rules/cc-artifacts.md pointer + ml-specialist exemplar. (2) 7 skills/02-dataflow/*.md cited skills/dataflow-specialist.md (stale path) → agents/frameworks/dataflow-specialist.md. (3) skills/13-architecture-decisions/decide-framework.md cited agents/framework-advisor.md (renamed) → agents/implementation/pattern-expert.md.",
        "9 AGENT BODY UPDATES absorbed M1 patterns + integration surface pointers (commit e7e42765). release-specialist.md +55 (atomic release wave), ml-specialist.md +59 (km.* surface + async + TrainingResult.trainable + MIGRATION + 7 Phase-1 adapters), todo-manager.md +35 (amend-in-place), analyst.md +30 (E2E regression tier), 5 framework specialists +4-5 each (pointers to {pkg}.ml integration surfaces: kailash.ml, dataflow.ml, nexus.ml, kaizen.ml, pact.ml, align.ml).",
        "2 NEW GLOBAL SKILL SUB-FILES — skills/34-kailash-ml/m1-release-wave.md (140 LOC — 48-symbol __all__, async pair, trainable handle, MIGRATION sunset, 7 integration surfaces). skills/10-deployment-git/multi-package-release-wave.md (140 LOC — reverse dep-graph, per-pkg version owner, sole CHANGELOG owner, rollback tree, pre-flight build/twine/TestPyPI). skills/30-claude-code-patterns/worktree-orchestration.md +105 (Rules 6/7/8 for burst/base-SHA/branch-name). skills/16-validation-patterns/SKILL.md +35 (E2E regression tier). skills/12-testing-strategies/SKILL.md +3 (pointer).",
        "MANIFEST: sync-manifest.yaml no edits required — 3 new 04-kaizen sub-files + 10 new rule-extract guides + 2 new skill sub-files all fall under default global-COC tier per existing wildcard classifications.",
        "SDK PIN DELTA (PyPI-resolvable only per variants/py/rules/deployment.md): kailash-kaizen 2.10.1 → 2.11.0. All other packages already at PyPI-resolvable tips per 2026-04-21 Wave 1+2 release cycle. BUILD feat branch kailash-ml 1.1.0 / kailash 2.9.0 / etc. held (pre-release, local-only).",
        "TEMPLATE RESYNC: kailash-coc-claude-py 3.5.15→3.5.16; kailash-coc-py (multi-CLI) .coc-sync-marker updated.",
        "PROPOSAL: kailash-py/.claude/.proposals/latest.yaml advanced pending_review → reviewed (2026-04-23T14:30:00+08:00). Status will advance to distributed after Gate 2 completes.",
        "CROSS-SDK QUEUED for kailash-rs sync cycle: Rule 6a __all__ reconciliation ≡ Rust pub use re-export reconciliation (identical failure mode). Paired async-ness ≡ Rust `async fn` vs `fn`-with-block_on() panic in tokio runtime. End-to-end pipeline regression rule language-neutral. Fake-integration via missing handoff field ≡ Rust struct equivalent. Rs variant overlays may refine syntax examples; underlying principles port as-is.",
        "DEFERRED: 8 remaining `kailash.mcp_server` occurrences at skills/05-kailash-mcp/mcp-advanced-patterns.md (6) + skills/14-code-templates/template-mcp-server.md (2) — pre-existing at loom (not introduced by this diff). Flagged for next codify cycle per scanner-surface symmetry (rules/zero-tolerance.md Rule 1a)."
      ]
    },
    {
      "version": "2.8.26",
      "date": "2026-04-21",
      "summary": "PY CYCLE 5: kailash-py /codify proposal 2026-04-20 5th-entry append (build-repo-release-discipline) — appended after prior /sync per artifact-flow.md 'MUST: Append, Never Overwrite'. Status was reverted to pending_review by /codify; entries 1-4 already distributed in 2.8.24 (no-op verified at loom global / variant). NEW global rule rules/build-repo-release-discipline.md (158 lines): ABSOLUTE 'Done' = released to PyPI + clean-venv install + import verified + 4 MUST clauses + 4 MUST NOT + 7 BLOCKED rationalizations. Path-scoped via `paths:` to release-critical files (pyproject.toml, packages/**/pyproject.toml, CHANGELOG.md, packages/**/CHANGELOG.md, __init__.py, .github/workflows/publish-*.yml, .github/workflows/release*.yml, deploy/deployment-config.md) per rule-authoring.md MUST Rule 5 — rule fires when a BUILD-repo session edits any release-critical file, not globally on every session. Cross-SDK applicable; kailash-rs companion proposal expected on next /codify. Template kailash-coc-claude-py 3.5.14→3.5.15. SDK pin sweep to PyPI-published versions following 2026-04-21 Wave 1+2 release cycle (every BUILD-main version now matches PyPI): kailash 2.8.11→2.8.12, kailash-mcp 0.2.7→0.2.8, kailash-kaizen 2.7.5→2.10.1, kailash-pact 0.8.2→0.9.0, kailash-ml 0.15.2→0.17.0, kailash-align 0.3.2→0.4.0. kailash-trust dep REMOVED — yanked from PyPI ('not required as kailash already exports it in production'). pydantic-extra-types 2.11.2→2.11.1 via lockfile downgrade (yanked, no security impact).",
      "changes": [
        "GLOBAL NEW FILE: rules/build-repo-release-discipline.md (158 lines). MUST 1 — every merge triggers /release for modified package + every sibling whose main > PyPI by ≥1 bump. MUST 2 — installability gate via uv venv + uv pip install + import (pip --target BLOCKED for verification: missing console_scripts, namespace-package resolution wrong on macOS) + PyPI cache-lag retry loop. MUST 3 — release scope enumerated session-start, cached in working memory. MUST 4 — release authorization is the only structural gate (human approves scope; agent owns enumeration). 4 MUST NOT + 7 BLOCKED rationalizations. Origin: Session 2026-04-21 — 7 PRs of issue #567 merged across 3 sessions; PyPI lagged BUILD-main by up to 3 minor versions; downstream MLFP consumer reported 'what happened??? we didn't do anything'. Wave 1 (kailash 2.8.12, kailash-mcp 0.2.8, kailash-ml 0.17.0, kailash-align 0.4.0, kailash-pact 0.9.0) tagged 2026-04-21T05:41-05:44; Wave 2 (kaizen 2.10.1) tagged 2026-04-21T05:49:16Z after PR #587 merge. All clean-venv install + import verified.",
        "MANIFEST: sync-manifest.yaml coc tier list — added rules/build-repo-release-discipline.md adjacent to rules/deployment.md (deployment.md is /release mechanics; build-repo-release-discipline.md is the WHEN).",
        "TEMPLATE RESYNC: kailash-coc-claude-py 3.5.14→3.5.15. NEW rules/build-repo-release-discipline.md (158 lines, identical to loom global). 6 SDK pin bumps applied (Wave 1+2 catch-up). kailash-trust dep removed (yanked: now exported by kailash core). pydantic-extra-types 2.11.2→2.11.1 via uv lock --upgrade-package (yanked, no security impact). uv sync ✓ (321 packages, 0 warnings). 12/12 hooks verified (resolved through $CLAUDE_PROJECT_DIR expansion).",
        "PROPOSAL: kailash-py/.claude/.proposals/latest.yaml advanced pending_review → reviewed → distributed. Entries 1-4 already distributed in 2.8.24 (no-op verified). Entry 5 (build-repo-release-discipline) classified GLOBAL with rule-authoring compliance check (MUST 1-6 all ✓; 158 lines under 200-line cap; universal scope per Rule 5).",
        "CROSS-SDK QUEUED for kailash-rs: identical PyPI/main vs crates.io/main drift failure mode applies. Companion proposal expected on next kailash-rs /codify cycle classifying the rule as already-global (loom-distributed) and confirming no rs-variant overlay required (Cargo/crates.io examples can replace Python examples in a future variant overlay if rs developers need them). The current Python-default examples align with sync-manifest.yaml convention 'Global skills/rules: PYTHON examples by default (Python is the majority language)'."
      ]
    },
    {
      "version": "2.8.25",
      "date": "2026-04-20",
      "summary": "gh #17 fix: rules/worktree-isolation.md de-scoped from path-scoped (agents/**, commands/**) to universal. Rule governs orchestrator session-spawn-time behavior, not file-edit-time — qualifies as 'truly universal' per rule-authoring.md MUST Rule 5. Peer of rules/agents.md. Also broadened framing in both worktree-isolation.md Rule 5 and agents.md 'MUST: Worktree Agents Commit Incremental Progress' to make explicit that the commit-incremental discipline applies to every worktree agent regardless of compile — prose drafting, markdown briefs, one-line config edits, and every other agent task. Added 2026-04-21 post-mortem (11 drafted briefs + pytest.ini diagnosis lost). Closes gh #17. Template: kailash-coc-claude-py 3.5.13→3.5.14.",
      "changes": [
        "rules/worktree-isolation.md: REMOVED paths: frontmatter (was scoped to .claude/agents/**, .claude/commands/**, **/agents/**). Added session-intro paragraph explaining the rule targets orchestrator session-spawn-time behavior. Reason: orchestrators rarely edit agent/command files in the same turn they spawn agents, so the path-scoped rule loaded ~0% of the time it was needed. Evidence: gh #17 2026-04-21 tpc_cash_treasury session — orchestrator spawned 3 worktree agents, 2 reported 'done' without committing, worktree auto-cleaned, 11 drafted briefs + verified pytest.ini diagnosis lost. Rule qualifies as 'truly universal' per rule-authoring.md MUST Rule 5 (peer of agents.md).",
        "rules/worktree-isolation.md Rule 5: broadened framing to make explicit the rule applies to every worktree agent regardless of compile type. Added 'each draft brief completed, each config edit made' to the milestone examples. Added 2 new BLOCKED rationalizations: 'My agent is just writing markdown / just editing one config line — commit discipline is overkill for that' + 'The agent's summary in the conversation is enough evidence'. Why line now documents BOTH the 2026-04-19 compile-heavy post-mortem AND the 2026-04-21 non-compile post-mortem.",
        "rules/agents.md § 'MUST: Worktree Agents Commit Incremental Progress': added parallel broadening — 'This applies to every worktree agent regardless of task type' explicit clause + 'Draft 11 briefs / Fix one line in pytest.ini' DO NOT examples + BLOCKED rationalizations row + cross-reference to worktree-isolation.md Rule 5 2026-04-21 post-mortem.",
        "TEMPLATE RESYNC: kailash-coc-claude-py 3.5.13→3.5.14. Applied same 3 edits to template. No SDK pin deltas. No uv sync needed (content-only changes).",
        "gh #17 closed with fix commit reference."
      ]
    },
    {
      "version": "2.8.24",
      "date": "2026-04-20",
      "summary": "PY CYCLE 4: kailash-py /codify proposal 2026-04-20 afternoon/evening (issue #567 MLFP diagnostics Session 3b, 7 PRs shipped) — 4 new artifacts classified and distributed. 3 global + 1 variant-py + 1 NEW global skill. Template version: kailash-coc-claude-py 3.5.12→3.5.13. SDK pin bumps applied where PyPI-resolvable: kailash-mcp 0.2.5→0.2.7, kailash-ml 0.14.0→0.15.2. Three held (kaizen 2.8.0, pact 0.9.0, align 0.4.0 — BUILD-local, not yet on PyPI; per variants/py/rules/deployment.md MUST Optional Dependencies Pin to PyPI-Resolvable Versions).",
      "changes": [
        "GLOBAL rules/agents.md — new MUST 'Recover Orphan Writes From Zero-Commit Worktree Agents' (+20 lines, 189→209). Extends § Worktree Prompts Use Relative Paths Only with recovery protocol when Rule 2 fails. Compact MUST clause + BLOCKED rationalizations + pointer to worktree-orchestration.md skill for full 4-step protocol. Origin: kailash-py Session 2026-04-20 Session 3b (issue #567) — PR #574 recovery/pr3-alignment-diagnostics recovered 1129 LOC of alignment.py from MAIN checkout. Classified global; cross-SDK applicable (Rust worktrees have identical auto-cleanup behavior).",
        "GLOBAL skills/30-claude-code-patterns/worktree-orchestration.md — new Rule 2a 'Recovery Protocol When Rule 2 Fails' (+36 lines, 55→91). Full 4-step detect/branch/commit/fill protocol + PR #574 post-mortem + Rust equivalent note. Extends progressive-disclosure pattern: rule file holds compact MUST; skill holds narrative evidence + 4-step protocol.",
        "GLOBAL rules/testing.md — new § Tier 2 Exception 'Protocol-Satisfying Deterministic Adapters Are Not Mocks' (+30 lines, 513→543). Runtime isinstance(adapter, Protocol) + deterministic output = NOT a mock; valid Tier 2 test-double for Protocol-typed dependencies requiring API keys/network/GPU. BLOCKED rationalizations include MagicMock(spec=...). Rust equivalent: struct DeterministicFoo impl Foo has compile-time stronger guarantee. Origin: kailash-py PR#580 DeterministicJudge satisfies JudgeCallable Protocol, 7 Tier 2 tests passing without API keys.",
        "VARIANT-PY rules/deployment.md — new MUST 'Sibling-Package CI Installs Root SDK Editable For Unreleased Core Modules' (+34 lines, 180→214). YAML example with leading `uv pip install -e \".\"` before `uv pip install -e \"packages/kailash-<subpkg>[dev]\"`. Classified variant-py (not global) because concrete YAML example is pip/uv-specific; Rust workspaces resolve path-deps locally by default, so parallel failure mode is narrower (binding CI only). Companion kailash-rs issue to be filed for the Rust workspace/path-dep form. Origin: kailash-py Session 2026-04-20 Session 3b — PR #577 extended editable-root prepend across Base/DL/RL/Unit/Inter-Package CI jobs after PRs #574/#575/#576 failed at collection with ModuleNotFoundError.",
        "NEW GLOBAL SKILL skills/30-claude-code-patterns/sdk-upstream-donation.md — 100-line 7-step playbook for Apache-licensed cross-SDK Protocol-based external donations (pre-impl reconciliation, PR#0 Protocols+JSON Schema, risk-ascending adapters, reject-parallel-facades/absorb-capabilities, cross-SDK parity issues, Apache §4(d) attribution, session planning). Anchored on kailash-py issue #567 MLFP diagnostics donation (7,300 LOC across ml/kaizen/align/pact).",
        "TEMPLATE RESYNC: kailash-coc-claude-py 3.5.12→3.5.13. 4 updated + 1 added + 5 flagged (pre-existing drift from prior cycles, deferred) + 320 unchanged + 9 preserved. SDK pin deltas applied: kailash-mcp 0.2.5→0.2.7, kailash-ml 0.14.0→0.15.2. Held at PyPI-resolvable: kaizen 2.7.5 (BUILD has 2.8.0 local), pact 0.8.2 (BUILD 0.9.0 local), align 0.3.2 (BUILD 0.4.0 local). uv sync ✓ (322 packages). 12/12 hooks verified.",
        "DRIFT FLAGGED (5 items, deferred to next codify cycle — not in this proposal's scope): rules/autonomous-execution.md §4 Fix-Immediately Same-Class Gap (py-side classification pending); rules/git.md Commit-Message Claim Accuracy (py-side classification pending); rules/schema-migration.md force_downgrade vs force_drop primitive-vs-orchestrator refactor (target still on old Rule 7 form); rules/nexus-http-status-convention.md missing from py template (COC-tier per manifest, distribution gap); rules/testing.md Env-Var Test Isolation (+44 lines from prior rs cycle never propagated to py template). All require next /codify to classify + follow-up /sync."
      ]
    },
    {
      "version": "2.8.23",
      "date": "2026-04-20",
      "summary": "Three-cycle session: py §4b error-contract refactor rule + SDK pin bumps (2.8.10→2.8.11, kailash-ml 0.13.0→0.14.0); rs v3.20.0 batch of 8 rule additions (4 global, 4 rs-variant, 2 reviewer-overridden global→variant-rs on flag-back); rule restructuring to close 200-line cap breaches — extracted reference material from orphan-detection.md (366→198) and agents.md (222→189) into new skill files orphan-audit-playbook.md + worktree-orchestration.md; reverted ci-runners §6/§7 (zombie-job + idle-not-accepting) from global to variant-rs per ops-identifier-density rationale. Template versions: kailash-coc-claude-py 3.5.10→3.5.12; kailash-coc-claude-rs 2.9.11→2.9.13.",
      "changes": [
        "PY CYCLE 1: rules/orphan-detection.md — added §4b 'Error-Contract Refactor MUST Sweep Paired Tests In Same Commit' (mirror of §4a for error-type changes on non-stub APIs). Cross-SDK: Rust Result<_, OldError>→Result<_, NewError> has same failure with .unwrap_err()/assert_matches!. Origin: session 2026-04-20 IdentifierError replaced ValueError in audit_store.py with paired test not updated; 7 matrix jobs red until follow-up commit. Classified global.",
        "PY CYCLE 1: kailash-coc-claude-py template 3.5.10→3.5.11. SDK pins kailash 2.8.10→2.8.11 (PR #557 dialect-safety sweep), kailash-ml 0.13.0→0.14.0 (PR #558 km.doctor + km.track). uv sync ✓ after PyPI cache refresh. Proposal distributed.",
        "RS CYCLE: 8 rule entries classified — #1 sqlx-any-null-typing (variant-rs NEW FILE dataflow-null-typing.md, 154 lines), #2 ci-toolchain-parity-clippy (variant-rs build-speed.md extension, +38 lines), #3 env-var-test-isolation (global testing.md +55 lines, Python-first + Rust tokio::sync::Mutex variant), #4 commit-message-claim-discipline (global git.md +30 lines), #5 zombie-job-protocol + #8 runner-idle-not-accepting-protocol (reviewer promoted global → then flag-reverted to variant-rs in same cycle — see final restructuring below), #6 fix-immediately-same-class-gap (global autonomous-execution.md §4, +40 lines), #7 release-no-artifact-roundtrip (variant-rs NEW FILE release.md, 52 lines).",
        "RS CYCLE: kailash-coc-claude-rs template 2.9.11→2.9.12. Cargo pins 3.19.0→3.20.0 across all workspace crates (cargo check N/A — rs template has no Cargo.toml, it's a Python/Ruby binding-consumer template; pyproject.toml parse validated). Cross-SDK catch-up of morning's py-cycle globals also propagated. Proposal distributed.",
        "RULE RESTRUCTURING #1: rules/orphan-detection.md trimmed 366→198 lines (under 200-line rule-authoring.md cap). Moved Phase 5.11 post-mortem narrative + 6-step Detection Protocol + per-rule evidence narratives to NEW skill skills/16-validation-patterns/orphan-audit-playbook.md (72 lines). Kept all 11 MUST clauses (§1, §2, §2a, §3, §4, §4a, §4b, §5, §5a, §6, §7) + MUST NOT block + BLOCKED rationalizations + one-line Origin refs + trimmed DO/DO NOT examples.",
        "RULE RESTRUCTURING #2: rules/agents.md trimmed 222→189 lines. Moved worktree protocol details (5 MUSTs: isolation, relative paths, incremental commits, verify deliverables, parallel-worktree package ownership) + Reviewer Mechanical Sweep post-mortem to NEW skill skills/30-claude-code-patterns/worktree-orchestration.md (55 lines). Rules retain 3-5 line MUSTs with DO/DO NOT + 'See skills/...' for evidence.",
        "RULE RESTRUCTURING #3: ci-runners §6 (Zombie-Job Protocol) + §7 (Idle-But-Not-Accepting Protocol) reverted from global to variant-rs. Rationale: macOS-specific LaunchAgent labels + runner hostnames (Jacks-Mac-Studio, Esperies-Mini, esperie-mac) + esperie-enterprise/kailash-rs API paths — all kailash-rs-operational. Global ci-runners.md trimmed 258→170 lines (§1-§5 + language-agnostic principles). variants/rs/rules/ci-runners.md (NEW, 258 lines) is self-contained with full rs-specific identifiers restored. sync-manifest.yaml updated with variant mapping.",
        "TEMPLATE RESYNC: kailash-coc-claude-py 3.5.11→3.5.12 (content-only: trimmed rules + 2 new skill files + SKILL.md nav updates; ci-runners stays at 170L global, never had §6/§7). uv sync clean. 12/12 hooks.",
        "TEMPLATE RESYNC: kailash-coc-claude-rs 2.9.12→2.9.13 (content-only: trimmed global rules + new variant-rs ci-runners 258L + 2 new skill files + SKILL.md nav updates). Variant overlay semantics applied correctly — rs template gets the 258L ci-runners via variant-rs authority. pyproject.toml tomllib parse ✓. 12/12 hooks.",
        "METHODOLOGY: session validated the 'reviewer classification can be flag-reverted in-cycle' pattern — rs Gate 1 reviewer promoted ci-runners §6/§7 to global (abstracting <org>/<repo>); human flagged mis-classification (content is ops-identifier-dense); reverted to variant-rs in same session with proper variant-overlay file creation and manifest registration. Also validated rule-refactor discipline: trim rules back under 200-line cap by extracting reference material to purpose-built skills, not by deleting evidence."
      ]
    },
    {
      "version": "2.8.22",
      "date": "2026-04-19",
      "summary": "Drift-reconciliation + PyPI catch-up. Fixed 4 pre-existing rs-template drift items (zero-tolerance Rule 1): rs variants for rules/framework-first.md and rules/security.md updated to include sections that earlier sync cycles had merged into the template but never backported to the variant authority; rules/nexus-webhook-hmac.md template Origin line synced to current global; rules/event-payload-classification.md finally propagated to rs template (missing since 2.8.12 manifest addition). SDK pins bumped in py template: kailash 2.8.8→2.8.9, kailash-ml 0.11.1→0.12.0 (GPU-first Phase 1 PyPI release).",
      "changes": [
        "DRIFT FIX #1: variants/rs/rules/framework-first.md — added 'Framework Version-Stable Integration — Drive The Data, Not The Dispatch' section (+36 lines, cross-SDK, Python example). Template had this from 2.8.19 but variant didn't; closed the gap so variant remains authoritative.",
        "DRIFT FIX #2: variants/rs/rules/security.md — added Sanitizer Contract (§1 token-replace vs quote-escape, §2 type-confusion raises, §3 safe types as-is) + Multi-Site Kwarg Plumbing sections (+86 lines). Template had these from 2.8.19 but variant didn't. Also deduplicated the MUST NOT block the earlier template-side merge had left orphaned.",
        "DRIFT FIX #3: kailash-coc-claude-rs/.claude/rules/nexus-webhook-hmac.md — Origin line synced to current loom global; now references extractor-trait architecture rework kailash-rs#404 S1-S8 (S1-S7 merged 2026-04-18 through 2026-04-19 per prior rs codify cycles).",
        "DRIFT FIX #4: kailash-coc-claude-rs/.claude/rules/event-payload-classification.md — file was MISSING from rs template entirely since its 2.8.12 manifest addition. Now copied from loom global. Cross-SDK event payload safety rule: record_id hashing + fields_changed partitioning + 4 MUST NOT clauses.",
        "SDK PIN: py template kailash 2.8.8→2.8.9 (new PyPI patch release).",
        "SDK PIN: py template kailash-ml 0.11.1→0.12.0 (GPU-first Phase 1 minor release now on PyPI — DeviceReport, device_report_from_backend_info, device, use_device public API). uv sync ✓ (numba 0.61.2, llvmlite 0.44.0, pynndescent 0.6.0, umap-learn 0.5.12 pulled as kailash-ml 0.12.0 transitives).",
        "TEMPLATE VERSIONS: kailash-coc-claude-py 3.5.8→3.5.9 (kailash + kailash-ml PyPI catch-up); kailash-coc-claude-rs 2.9.10→2.9.11 (drift reconciliation).",
        "METHODOLOGY: drift items first triaged in parity-cycle report; user invoked zero-tolerance Rule 1 to surface BLOCKED 'reporting for future attention' behavior; items promoted from 'flagged' to 'fixed this cycle' and resolved in same run per rule's 'if you found it, you own it' directive."
      ]
    },
    {
      "version": "2.8.21",
      "date": "2026-04-19",
      "summary": "Gate 1 classification of kailash-py 2026-04-19 ML GPU-first Phase 1 parallel-shard proposal (3 HIGH entries, all classified global). Placed two worktree-discipline rules in rules/worktree-isolation.md (Rule 4 parent-checkout path ban + Rule 5 incremental commit discipline) rather than rules/agents.md to respect the 200-line soft cap. Placed reviewer-mechanical-sweep rule in rules/agents.md Quality Gates section. Gate 2 distributed to kailash-coc-claude-py (3.5.7→3.5.8) with kailash-ml 0.12.0 pin deferred (not yet on PyPI).",
      "changes": [
        "EXTENDED GLOBAL: rules/worktree-isolation.md — added Rule 4 'Worktree Prompts MUST NOT Reference The Parent-Checkout Path' + Rule 5 'Worktree Agents MUST Commit Incremental Progress' (+68 lines). Rule 4 prohibits absolute paths pointing to PARENT checkout (e.g., /Users/.../kailash-py/packages/...) which silently resolve to main despite isolation=worktree; relative paths or absolute paths anchored to the pinned worktree path are fine (reconciles with existing Rule 1). Rule 5 mandates explicit 'commit after each milestone' in the agent prompt + orchestrator verification of ≥1 commit on the branch before declaring work landed; worktrees with zero commits auto-clean, losing 100% of output. Both rules cross-SDK-applicable (Rust ml-specialist / dataflow-specialist / kaizen-specialist agents face the same write-to-main leak). Origin: kailash-py 2026-04-19 GPU-first Phase 1 parallel-shard session: 2 of 3 ml-specialist shards wrote to main; 1 lost 300+ LOC to auto-cleanup; 1 rescued by WIP commit.",
        "EXTENDED GLOBAL: rules/agents.md — added 'MUST: Reviewer Prompts Include Mechanical AST/Grep Sweep, Not Only Diff Review' section after Quality Gates background-agent block (+32 lines). Every gate-level reviewer prompt MUST enumerate mechanical sweeps (parity grep, collect-only, pip/cargo check, __all__ verification) that verify ABSOLUTE state before LLM judgment on the diff. LLM review of the diff catches what's wrong with the new code; mechanical sweeps catch what's missing from OLD code that the spec also touched (the orphan failure mode). Four BLOCKED rationalizations. Cross-SDK: Rust gate reviewers face the same scope limitation. Origin: kailash-py 2026-04-19 — code reviewer APPROVED kailash-ml 0.12.0 with one minor finding; subsequent /redteam mechanical sweep caught TorchTrainable + LightningTrainable missing device=DeviceReport (2 of 7 return sites) that the reviewer never parity-grepped.",
        "PLACEMENT RATIONALE: worktree-discipline rules went to rules/worktree-isolation.md (already canonical home for worktree rules; proposal target rules/agents.md would have pushed agents.md over the 200-line cc-artifacts.md cap). Reviewer-sweep rule went to rules/agents.md § Quality Gates (gate-review-related).",
        "SDK PIN: kailash-ml 0.12.0 pin bump DEFERRED — kailash-py BUILD has 0.12.0 locally but PyPI still has 0.11.1. Premature pin would break downstream `uv sync` on every USE template consumer per rules/deployment.md (Optional Dependencies Pin to PyPI-Resolvable Versions). Will bump when kailash-ml 0.12.0 lands on PyPI.",
        "CROSS-SDK QUEUED for kailash-rs: all 3 rules apply 1:1 to rs (same parallel-shard / worktree / reviewer patterns — rs already has isolation=worktree discipline per rules/agents.md MUST section). kailash-coc-claude-rs will inherit via next /sync cycle.",
        "PROPOSAL: kailash-py/.claude/.proposals/latest.yaml advanced pending_review → reviewed → distributed (distributed_by: loom /sync Gate 2 → kailash-coc-claude-py 3.5.8 2026-04-19)."
      ]
    },
    {
      "version": "2.8.20",
      "date": "2026-04-19",
      "summary": "Gate 1 classification of kailash-rs v3.18.0 post-release proposal (8 entries). 6 already codified in loom 2.8.x (no-op), 2 new content writes: variants/rs/rules/build-speed.md appended MUST 'Use Nightly Rustfmt To Match CI'; rules/orphan-detection.md extended with Rule 7 (Public-API Removal MUST Sweep Binding/Downstream Consumers) + Rule 8 (Delete-or-Migrate Grep Is A Merge Gate). Gate 2 distributed to kailash-coc-claude-rs (2.9.8→2.9.9) with SDK pin bump kailash workspace 3.18.0→3.19.0.",
      "changes": [
        "EXTENDED RS-VARIANT: variants/rs/rules/build-speed.md — appended MUST 'Use Nightly Rustfmt To Match CI' section (+22 lines). CI's Format job pins dtolnay/rust-toolchain@nightly with rustfmt.toml nightly-only keys (imports_granularity, group_imports, trailing_comma, blank_lines_upper_bound, blank_lines_lower_bound, format_strings); local `cargo fmt --all` against stable silently skips them and produces CI-rejectable output. Four BLOCKED rationalizations. Setup command. Origin: kailash-rs 2026-04-19 — PRs #427/#428/#430 failed CI fmt while code was correct due to toolchain divergence.",
        "EXTENDED GLOBAL: rules/orphan-detection.md — added Rule 7 'Public-API Removal MUST Sweep Binding / Downstream Consumers In The Same PR' + Rule 8 'Delete-or-Migrate Grep Is A Merge Gate' (+52 lines). Generalized from kailash-rs bindings/ origin to cover every sibling consumer tree (bindings/, examples/, packages/, ffi/, clients/) sharing a lockfile with the deleted symbol; grep assertion in PR body or CI step is the merge gate. Six BLOCKED rationalizations across both rules. Cross-SDK: kailash-py benefits directly (no bindings/ tree but has packages/ and examples/). Origin: kailash-rs PR #427 (2026-04-19) — removed kailash-nexus::auth re-exports with cargo check -p kailash-nexus clean but no cross-binding grep; Ruby+Python+Node CI all red on first run; 7-file follow-up required.",
        "ALREADY CODIFIED (no-op, confirmed by Gate 1 review): (a) binding-feature-gate-consistency — present in rules/dependencies.md lines 174-272 'Declared = Gated Consistently' section from loom 2.8.15; (b) issue-template-cross-sdk-verification — BUILD-only artifact, parent rule in rules/cross-sdk-inspection.md MUST Rule 5; (c) regression-test-405 — BUILD-only Rust test file; (d) nexus-http-status-convention-rule — present as 173-line rule file at rules/nexus-http-status-convention.md from loom 2.8.17; (e) nexus-extractors-skill — present as HIGH-priority skill at skills/03-nexus/nexus-extractors.md from loom 2.8.17; (f) extractor-orphan-lesson — present as Detection Protocol step 3 'Bridge-shim verification' in rules/orphan-detection.md from prior loom cycle with #404 S4a citation.",
        "SDK PIN BUMPS (Gate 2 distribution): kailash workspace crates 3.18.0→3.19.0 (37 workspace-version crates). Independent-pin crates preserved: eatp=0.1.0, kailash-governance=0.1.0, kailash-pact=0.1.0, kailash-plugin-macros=3.0.0, kailash-node=0.1.0, kailash-wasm=0.1.0, trust-plane=3.",
        "CROSS-SDK QUEUED for kailash-py: (1) confirm Rule 7/8 generalization applies — kailash-py benefits directly from binding-consumer-sweep extended to packages/ and examples/ trees (generalization already in global text); (2) file kailash-py#497 linkage into nexus-extractors skill cross-reference when Python SDK port of extractor surface lands.",
        "PROPOSAL: kailash-rs/.claude/.proposals/latest.yaml advanced pending_review → reviewed → distributed (distributed_date 2026-04-19T15:52:20Z by loom /sync Gate 2)."
      ]
    },
    {
      "version": "2.8.19",
      "date": "2026-04-19",
      "summary": "Gate 1 classification of kailash-py round-9+10 codify (7-entry post-release-audit institutional capture from a session that shipped 9 PRs #502-#533 + bundle PyPI releases kailash 2.8.8 / kailash-dataflow 2.0.12 / kailash-ml 0.11.1 / kailash-align 0.3.2 / kailash-pact 0.8.2 / kailash-trust 0.1.1 / kaizen-agents 0.9.3). All 7 entries placed as globals. 4 rule extensions + 3 new sections authored. 5 cross-SDK follow-ups queued for kailash-rs. Companion SDK pin bumps for Gate 2.",
      "changes": [
        "EXTENDED GLOBAL: rules/tenant-isolation.md — added Rule 3a 'Keyspace Version Bumps Require Invalidation-Path Sweep' (+28 lines). Producer-side keyspace bump (CacheKeyGenerator v1→v2) MUST sweep every consumer-side invalidation path in the SAME PR; version-wildcard sweeps (`dataflow:v*:*`) are the structural defense. Four BLOCKED rationalizations. Cross-SDK generic. Origin: kailash-py PR #522 / #529 — BP-049 keyspace bump, AsyncRedisCacheAdapter.invalidate_model left pinned to v1, caught by post-release reviewer, fast-patched in dataflow 2.0.12.",
        "EXTENDED GLOBAL: rules/security.md — added 'Multi-Site Kwarg Plumbing' section (+35 lines) after Sanitizer Contract. Security-relevant kwargs (policy, tenant scope, clearance, audit correlation) MUST be plumbed to EVERY call site in the same PR; `grep -rn` to enumerate siblings. Five BLOCKED rationalizations including 'safe default is by definition the insecure default' rebuttal. Origin: kailash-py PR #522 / #529 — validate_model plumbed in Express site, missed in DataFlowEngine.validate_record, caught by post-release reviewer.",
        "EXTENDED GLOBAL: rules/orphan-detection.md — added Rule 6 'Module-Scope Public Imports Appear In __all__' (+41 lines). Every module-scope public import MUST appear in __all__ unless private; eagerly-imported-but-absent is BLOCKED. Five BLOCKED rationalizations. Python-specific pattern (no Rust equivalent — use `pub use` hygiene is a distinct rule). Origin: kailash-py PR #523 / #529 — kailash-ml 0.11.0 imported DeviceReport/device_report_from_backend_info/device/use_device but omitted all four from __all__; from pkg import * dropped the advertised public API. Patched in 0.11.1.",
        "EXTENDED GLOBAL: rules/cross-sdk-inspection.md — added Rule 3a 'Structural API-Divergence Disposition' (+40 lines). When sibling SDK reports a bug at an API surface this SDK doesn't expose, disposition MUST land BOTH (a) Tier 2 binding-path test proving non-reproduction AND (b) signature-invariant test locking the current shape so a future refactor toward sibling shape fails loudly. Four BLOCKED rationalizations including 'not applicable' closure. Origin: kailash-py #525 / PR #528 — execute_raw(sql, params) vs execute_raw(sql) structural divergence closed with two tests rather than a 'not applicable' comment.",
        "EXTENDED GLOBAL: rules/dependencies.md — added 'Phantom Transitive Deps — Resolve Via Lockfile Upgrade, Not Local Caps' section (+30 lines) at the end. When resolver reports conflict rooted in an unused transitive, fix via `uv lock --upgrade-package` / `cargo update -p` / `npm update`, NOT manifest-level cap. Five BLOCKED rationalizations. Multi-ecosystem examples (Python + Rust + npm). Reclassified from proposal's `variant/py` suggestion to global — pattern generalizes across lockfile-driven ecosystems; only tooling verbs differ. Origin: kailash-py PR #530 — google-generativeai 0.8.6 held protobuf at old cap with zero imports, blocked kailash-align 0.3.2 release.",
        "EXTENDED GLOBAL: rules/agents.md Quality Gates table — added 'After release (post-merge audit)' row (+1 row, column widths widened). RECOMMENDED reviewer run against MERGED release commit on main catches drift pre-release gates missed (kwarg plumbing gaps, keyspace bumps with missed invalidators); if CRIT/HIGH surfaces, open patch branch SAME session and ship as x.y.z+1. Origin: kailash-py round-9 bundle shipped clean pre-release; post-release reviewer found 1 CRIT + 1 HIGH + 2 MED; fast-patched via PR #529 (dataflow 2.0.12 + ml 0.11.1) within 20 min of round-9 merge.",
        "NEW GLOBAL SECTION: rules/framework-first.md — added 'Framework Version-Stable Integration — Drive The Data, Not The Dispatch' section (+44 lines) at the end. When integrating with an external framework's lifecycle hook, if the framework exposes BOTH a dispatch method name AND a registered-handlers list/dict, iterate the list — it is strictly more stable than any dispatch method name (subject to underscore-prefix transitions, removal, renames). Five BLOCKED rationalizations. Cross-language examples (Python FastAPI/Starlette/aiohttp + Rust Axum/actix). Kailash-rs nexus-rs binding should mirror same discipline. Origin: kailash-py #531 / PR #533 — kailash-nexus 2.1.0 called app.router.startup()/.shutdown() as if stable; some production FastAPI builds exposed only _startup; every 2.1.0 service crashed at uvicorn lifespan with AttributeError. Fix (2.1.1): iterate on_startup/on_shutdown lists directly (what FastAPI's own _DefaultLifespan does internally).",
        "MANIFEST: no tier changes needed — all 7 target files (rules/tenant-isolation.md, rules/security.md, rules/orphan-detection.md, rules/cross-sdk-inspection.md, rules/dependencies.md, rules/agents.md, rules/framework-first.md) already in co/coc tiers. tenant-isolation.md + orphan-detection.md inherit coc tier via default-unlisted convention (manifest header: 'Unlisted files default to COC'). Other five explicitly listed (lines 100-101, 180, 190-191).",
        "SDK PIN BUMPS (for Gate 2 distribution): kailash 2.8.7→2.8.8, kailash-dataflow 2.0.10→2.0.12, kailash-ml 0.10.0→0.11.1, kailash-align NEW→0.3.2, kailash-pact NEW_baseline→0.8.2, kailash-trust NEW→0.1.1, kaizen-agents NEW→0.9.3 (first PyPI ship).",
        "CROSS-SDK QUEUED for kailash-rs: (1) keyspace-version-bump invalidation-path sweep — if rs CacheKeyGenerator bumps keyspace, same wildcard-sweep discipline applies; (2) multi-site kwarg plumbing — same grep-every-caller discipline on trust/policy plumbing through rs validate_model equivalents; (3) cross-SDK structural API divergence — extend the sibling Tier-2-plus-invariant-test pattern bidirectionally when rs#424 closes; (4) phantom transitive deps — apply lockfile-upgrade discipline to Cargo.lock phantom orphans (same rule, cargo tooling); (5) framework dispatch method drift — nexus-rs binding should iterate registered-handlers lists rather than call dispatch methods by name on FastAPI/Starlette bindings.",
        "PROPOSAL: kailash-py/.claude/.proposals/latest.yaml advanced pending_review → reviewed at loom 2.8.19 with full per-entry review_notes (classification, action, target_loom_path, loom_section_anchor, line_delta, cross_sdk_flag, notes)."
      ]
    },
    {
      "version": "2.8.18",
      "date": "2026-04-19",
      "summary": "Reconcile body-shape divergence flagged in 2.8.17 deferred observation. Skill skills/03-nexus/nexus-http-status-convention.md (loom 2.8.12 origin) was shipping {error, status} body shape that contradicts the actual SDK contract verified by red-team grep against 8 production references in kailash-rs (auth/error.rs, error.rs, middleware/csrf.rs + error_handler.rs + guard.rs + rate_limit.rs, auth/rbac.rs + rate_limit.rs — every shipping body uses {\"error\":\"...\",\"code\":\"...\"}). Skill rewritten to match SDK truth + cross-link to the 2.8.17 contract rule.",
      "changes": [
        "REWRITE GLOBAL SKILL: skills/03-nexus/nexus-http-status-convention.md — body-shape sections rewritten from {\"error\", \"status\": <int>} to {\"error\", \"code\": \"<STRING>\"}. Catalog table gained a 'code (example)' column with 12 canonical SCREAMING_SNAKE_CASE error-code identifiers (INVALID_INPUT, UNAUTHENTICATED, FORBIDDEN, NOT_FOUND, CONFLICT, PAYLOAD_TOO_LARGE, UNPROCESSABLE_ENTITY, RATE_LIMIT_EXCEEDED, INTERNAL_ERROR, UPSTREAM_FAILED, SERVICE_UNAVAILABLE, UPSTREAM_TIMEOUT). All 5 status-code examples (400/404/413/429/502/504) updated to canonical body shape using NexusHandlerError + JsonBody/PathParam extractors per the 2.8.17 extractor surface. Removed the 'No Domain-Specific Top-Level Keys' anti-pattern (since 'code' is now the canonical shape, the prior anti-pattern would have outlawed the SDK's actual behavior). Removed the 'No Mismatched status vs HTTP Code' anti-pattern (no longer applicable — there is no body status field). Added K8s liveness/readiness probe deviation (separate domain, uses {status: alive/ready}). Test example updated: assert set(body.keys()) == {error, code}. Cross-link added rule→skill (rule is the contract; skill is the catalog).",
        "AUTHORITY CLARIFICATION: skill opening reframed — 'this skill is the catalog companion that the contract rule points back to'. Body-shape section now defers to rule for MUST clauses; skill owns catalog + deviation cases only.",
        "MANIFEST: no changes (skill already in coc tier via skills/03-nexus/** glob).",
        "SCAN: zero remaining {error, status} references across loom .claude/ in the HTTP-error-response context after this rewrite (custom-node-guide.md:86 retains {error, status: failed} for INTERNAL node-return values, which is a different domain — out of scope).",
        "CROSS-SDK: no new tickets — kailash-py NexusError taxonomy alignment ticket was already queued in loom 2.8.17 cross-SDK queue (covers same body-shape contract). No additional kailash-py work surfaced by this skill rewrite.",
        "ORIGIN: original skill {error, status} convention (gh-coc-claude-rs#51 item 3a, 2026-04-17, loom 2.8.12) was speculative — predated the kailash-rs SDK shipping its NexusError.into_response() method. Superseded by actual SDK contract from kailash-rs#404 S8 (loom 2.8.17 rule) verified by adversarial red-team check against 8 production source references."
      ]
    },
    {
      "version": "2.8.17",
      "date": "2026-04-19",
      "summary": "Gate 1 of kailash-rs round 3 (#404 S8 extractor work codify). 3 new global artifacts placed: nexus-http-status-convention rule (NEW, 173 lines, formalizes the {error,code} body shape contract from kailash-rs's NexusError taxonomy), nexus-extractors skill (NEW, 263 lines, Option D NexusExtract trait + IntoResponse return trait pattern adapted to Python kailash-nexus binding examples), orphan-detection.md Detection Protocol step 3 inserted (bridge-shim verification — extractor pattern lesson from #404 S4a commit 90858bab). Entries 1-3 of the proposal already distributed in loom 2.8.15; skipped.",
      "changes": [
        "NEW GLOBAL RULE: rules/nexus-http-status-convention.md (173 lines) — 5 MUST clauses formalizing the {error, code} response body shape contract on every Nexus 4xx/5xx response. HTTP status taxonomy table (400/401/403/404/409/413/422/429/500/502/503) with NexusError variant mapping (HandlerNotFound→404, InvalidInput→400, ExecutionError→500, etc.). Python kailash-nexus binding examples (Rust crate path citations adapted to abstract 'SDK NexusError.status_code() / into_response()' references). paths frontmatter scopes to nexus handlers/routes. Cross-references the companion reference skill skills/03-nexus/nexus-http-status-convention.md (kept for status-code catalog and deviation cases). Added to coc tier in manifest. Origin: kailash-rs#404 S8 codify.",
        "NEW GLOBAL SKILL: skills/03-nexus/nexus-extractors.md (263 lines) — Option D NexusExtract trait + IntoResponse return trait pattern from kailash-rs#404 S1-S8 design. 9 Python-accessible extractors (JsonBody, BytesBody, FormBody, Query, PathParam, Header, Cookie, Connection, Cookies) + 5 response shapes (JsonResponse, TextResponse, BytesResponse, RedirectResponse, FileResponse). Ruby keyword-arg block section preserved. Performance/backward-compat/deprecation timeline abstracted to 'SDK DEPRECATION.md'. Adapted from Rust trait source-truth to Python kailash-nexus binding form per manifest convention (variants/rs/skills/03-nexus/* uses Python binding examples for Python-dev rs USE template targeting). Inherits coc tier via skills/03-nexus/** glob. Origin: kailash-rs#404 S8 codify.",
        "EXTENDED GLOBAL: rules/orphan-detection.md Detection Protocol — inserted new step 3 'Bridge-shim verification' between hot-path grep and Tier 2 grep. Verifies that extractor/converter shim layers (e.g. NexusExtract impl pulled in via PyO3 binding) actually delegate to the production type, not a parallel stub. Inline Why citing kailash-rs#404 S4a commit 90858bab (extractor bridge initially shipped with a placeholder shim that compiled but never invoked the real trait method). Steps 4-6 renumbered. Net +1 line.",
        "MANIFEST: tiers.coc added rules/nexus-http-status-convention.md (inserted after rules/nexus-webhook-hmac.md). Skill (skills/03-nexus/nexus-extractors.md) inherits via existing skills/03-nexus/** glob; rule extension to orphan-detection.md needs no new entry.",
        "SKIPPED (already distributed in loom 2.8.15): #417 binding-feature-gate-consistency, #409 issue-template-cross-sdk-verification, #405 regression-test-cross-sdk-no-parity-bug. Re-listed in this proposal as session-context preservation; no re-classification needed.",
        "DEFERRED OBSERVATION (next session): existing skill skills/03-nexus/nexus-http-status-convention.md (187 lines, loom 2.8.12 origin) uses {error, status} body shape; new rule uses {error, code} per actual NexusError contract. Skill predates rule. Cross-link added rule→skill; skill content (status-code catalog, deviation cases) remains useful. Future codify cycle should reconcile the body-shape divergence (likely update skill to match {error, code}).",
        "CROSS-SDK QUEUED for kailash-py: (1) port Nexus extractor surface per kailash-py#497 — new skill assumes Python target API shape that kailash-rs Python binding provides as reference impl today; (2) align kailash-py NexusError taxonomy with rule's HTTP mapping (HandlerNotFound→404, InvalidInput→400, ExecutionError→500, etc.); (3) add byte-diff regression test verifying kailash-py Nexus emits {error, code} body shape on every 4xx/5xx (parallel to rs shape-regression test).",
        "PROPOSAL: kailash-rs/.claude/.proposals/latest.yaml advanced pending_review → reviewed at loom 2.8.17 with full per-entry classification (entries 1-3 already_distributed, entries 4-6 with placement decisions + adaptation notes)."
      ]
    },
    {
      "version": "2.8.16",
      "date": "2026-04-19",
      "summary": "Gate 1 classification of kailash-py 2026-04-19 fresh codify (9-entry institutional capture from a session that shipped 9 PRs #502-#519 + 6 PyPI releases). 7 entries placed as globals (1 NEW + 6 rule extensions), 2 as variant-py (nexus-specialist TypedServiceClient + deployment.md TestPyPI), 1 skipped (workspaces/ ml-gpu-stack lives outside .claude/). 6 cross-SDK follow-ups queued for kailash-rs.",
      "changes": [
        "NEW GLOBAL RULE: rules/worktree-isolation.md (127 lines) — orchestrator pinning + specialist self-check + post-agent file-existence verification for parallel-agent workflows. Cross-language framing (target/, .venv/, node_modules/). Three MUST clauses: (1) parallel agents MUST receive worktree paths in delegation prompt; (2) agents MUST self-verify pwd matches worktree before tool use; (3) parent MUST Read claimed deliverable file after agent exit (orphan agent claims happen at budget exhaustion). paths: .claude/agents/**, .claude/commands/**, **/agents/** — pays no token cost on non-agent file reads. Added to co tier in manifest. Origin: kailash-py 2026-04-19 session, 4 worktree-drift occurrences (ml/dataflow/kaizen/testing specialists).",
        "EXTENDED GLOBAL: rules/agents.md — added 'MUST: Verify Agent Deliverables Exist After Exit' section (+27 lines) with DO/DO NOT examples + 3 BLOCKED rationalizations + cross-ref to rules/worktree-isolation.md. Origin: 2 occurrences in 2026-04-19 session (kaizen round 6, ml-specialist round 7) where agents reported success with zero files on disk. The ls/Read check is O(1) and converts silent no-op into loud retry.",
        "EXTENDED GLOBAL: rules/zero-tolerance.md Rule 1a — added 'Second instance' subsection (+25 lines) covering CodeQL py/modification-of-default-value via lazy __getattr__ in __all__: when a PR adds new __all__ entries that resolve via lazy __getattr__ but flag as 'no module-scope definition', fix the NEW entries (eager-import) instead of arguing 'main does this too'. Grandfathered entries are a separate workstream. Origin: kailash-py PR #506 / hotfix #509 (2026-04-19).",
        "EXTENDED GLOBAL: rules/observability.md § 9 — added LogRecord reserved-attribute collision rule (+44 lines). Python-canonical (KeyError: \"Attempt to overwrite 'X' in LogRecord\") with cross-language Rust tracing field collision DO/DO NOT example. Reserved attribute list: name, msg, args, levelname, levelno, pathname, filename, module, exc_info, exc_text, stack_info, lineno, funcName, created, msecs, relativeCreated, thread, threadName, processName, process, taskName. logger.info('event', extra={'module': '...'}) is BLOCKED (use 'subsystem' or 'component'). Origin: kailash-py 2026-04-19 session.",
        "EXTENDED GLOBAL: rules/dataflow-identifier-safety.md — added Rule 6 (+45 lines) covering primitive-vs-orchestrator separation between DropRefusedError (DDL primitive, force_drop=True) and DowngradeRefusedError (orchestrator, force_downgrade=True). Two-layer split keeps each guard's blast radius unambiguous: a downgrade may run multiple DROPs but the user only authorizes the downgrade once. Cross-references rules/schema-migration.md § 7 (force_downgrade rename from loom 2.8.14). Origin: kailash-py 2026-04-19 codify confirming the cross-SDK rename landed correctly.",
        "EXTENDED GLOBAL: rules/testing.md § Test-Skip Triage Decision Tree (+44 lines) — three-tier classification (ACCEPTABLE/BORDERLINE/BLOCKED) for every skipped test. ACCEPTABLE keeps skipif with infra constraint named in reason; BORDERLINE converts to xfail(strict=False); BLOCKED requires deletion (TODO-style skips, flakes, empty bodies). 4 BLOCKED rationalizations. Cross-language (pytest @pytest.mark.skip + Rust #[ignore]). Cross-references skills/test-skip-discipline/SKILL.md (loom 2.8.14). Origin: kailash-py gh #512 / PR #518 (2026-04-19) — applied to convert 1 test to xfail + delete 2 TODO tests + delete 6 abandoned files.",
        "EXTENDED GLOBAL: rules/deployment.md — minor cross-language extension. (Note: Python-specific TestPyPI/trusted-publisher content went to variant-py; the global got the language-agnostic atomic-version-bump pattern only.)",
        "UPDATED VARIANT (py): variants/py/agents/frameworks/nexus-specialist.md (+18 lines) — added TypedServiceClient + DecoderRegistry pattern from PR #507. Per-method typed return values via Pydantic models, codec dispatch table, eager-import all symbols added to __all__ (per the new zero-tolerance.md Rule 1a second-instance). Origin: kailash-py PR #507.",
        "UPDATED VARIANT (py): variants/py/rules/deployment.md (+33 lines) — added TestPyPI dual-publish workflow + trusted-publisher OIDC config + 6-package atomic release sequencing pattern from this session's 6 simultaneous PyPI releases (kailash 2.8.7, dataflow 2.0.10, kaizen 2.7.5, nexus 2.1.0, ml 0.10.0, mcp 0.2.5). Origin: kailash-py 2026-04-19 release session.",
        "SKIPPED: workspaces/kailash-ml-gpu-stack/04-validate/02-revised-stack.md — workspaces/ live outside .claude/, not COC-syncable. The proposal author wants it preserved at BUILD; that's already the state. No loom action required.",
        "MANIFEST: tiers.co added rules/worktree-isolation.md (path-scoped to agents/commands paths). Other entries already covered by existing globs.",
        "CROSS-SDK QUEUED (for parent to file against esperie/kailash-rs): (1) worktree-isolation rule applies as-is to Rust; rs specialist agent files should adopt the Step 0 self-check pattern; (2) Rust tracing span field audit for target/level/name/module_path/file/line collisions in production code (parallel to LogRecord); (3) #[ignore]-marked Rust test triage (parallel to test-skip discipline applied to Rust); (4) confirm rs error hierarchy already separates DropRefusedError from DowngradeRefusedError (proposal credits rs codify 2026-04-19 as source — verify the rename landed in BUILD source).",
        "SDK VERSION FLAG (Gate 2 reads pyproject.toml): proposal declared sdk_version: 2.8.8 but kailash-py/pyproject.toml is 2.8.7. The 6 PyPI releases this session shipped: kailash 2.8.7, kailash-kaizen 2.7.5, kailash-dataflow 2.0.10, kailash-nexus 2.1.0, kailash-ml 0.10.0, kailash-mcp 0.2.5. Gate 2 will pin templates to PyPI-resolvable floors per rules/deployment.md.",
        "PROPOSAL: kailash-py/.claude/.proposals/latest.yaml advanced pending_review → reviewed at loom 2.8.16 with full per-entry classification + manifest changes + cross-SDK queue + SDK version flag."
      ]
    },
    {
      "version": "2.8.15",
      "date": "2026-04-19",
      "summary": "Gate 1 classification of kailash-rs v3.18.0 post-release follow-on codify (#417 binding feature-gate + #409 issue template + #405 cross-SDK verification regression test). Only entry #417 produced a cross-SDK artifact; #409 and #405 were correctly classified skip (BUILD-only — #409 is a GitHub issue template file at .github/ISSUE_TEMPLATE/ which is per-repo infra, #405 is a Rust regression test in BUILD source). Single cross-SDK follow-up queued for kailash-py (optional-extras audit).",
      "changes": [
        "EXTENDED GLOBAL: rules/dependencies.md — added new section 'Declared = Gated Consistently — Optional Dependencies + Feature Gates' with three MUST clauses: (1) module-level imports from an optional-dep crate/package MUST be gated at the same feature/extra boundary; (2) downstream features that transitively require an upstream feature MUST enable it explicitly (binding-CI path-filter parity); (3) mechanical audit with DO/DO-NOT grep examples for both Rust (Cargo features) and Python (extras_require / optional-dependencies). Cross-language DO/DO-NOT examples preserved intent of single cross-SDK rule. File: 190 → 289 lines (+99). Origin: kailash-rs#417 commit d04c098a + journal 0038-DISCOVERY.",
        "SKIPPED (build_only): #409 issue-template-cross-sdk-verification — .github/ISSUE_TEMPLATE/*.md is per-repo GitHub infra, not a COC-syncable artifact. The encoded principle (cite cross-SDK symbols verified by grep) is already codified in loom global rules/cross-sdk-inspection.md MUST Rule 5 from round 1 (loom 2.8.14).",
        "SKIPPED (build_only): #405 regression-test-cross-sdk-no-parity-bug — Rust regression test inside BUILD source (tests/regression/test_405_*.rs). Pattern (regression test for cited cross-SDK parity) already codified in loom global rules/testing.md § Regression Testing.",
        "MANIFEST: no changes (rules/dependencies.md was already in tiers.coc).",
        "CROSS-SDK QUEUED: kailash-py optional-extras feature-gate audit — verify redis, psycopg2, asyncpg, prometheus_client module-scope imports are gated at the optional-boundary per the new MUST rule. Use Python audit grep from the new Audit subsection.",
        "PROPOSAL: kailash-rs/.claude/.proposals/latest.yaml advanced pending_review → reviewed with full per-entry classification + 2026-04-19 review_date. No separator needed (fresh proposal, not an append)."
      ]
    },
    {
      "version": "2.8.14",
      "date": "2026-04-19",
      "summary": "Gate 1 classification of kailash-rs 2026-04-19 appended codify (T13-T20 non-shard follow-up bundle on top of 2026-04-17 3e/pg/estimator/51/ci entries already in 2.8.12). 8 entries placed: 6 globals (4 new skills + 2 rule edits), 1 variant-py (migration template script), 1 new global guide (parallel-agent non-shard pattern). 4 cross-SDK follow-ups queued for kailash-py.",
      "changes": [
        "NEW GLOBAL SKILL: skills/02-dataflow/migration-scaffold.md (296 lines) — Numbered migration scaffold with up/down semantics, force_downgrade discipline, dialect-portable identifier quoting (Python kailash-py DialectManager + InvalidIdentifierError). Note that scripts/migrate.py CLI lives as a py variant. Origin: kailash-rs 2026-04-19 codify entry dataflow-migration-scaffold-skill.",
        "NEW GLOBAL SKILL: skills/03-nexus/handler-status-codes.md (203 lines) — Adapted from kailash-rs Rust NexusError/axum examples to Python kailash-nexus binding (`from nexus import App, Response`). Status-code catalog (400/401/403/404/409/413/422/429/500/502/503) with handler examples, observability tie-in. Per manifest convention (variants/rs/skills/03-nexus/* uses Python binding examples for Python-dev rs USE template targeting), the loom global is the Python form. Origin: kailash-rs nexus-handler-status-codes-skill.",
        "NEW GLOBAL SKILL: skills/13-architecture-decisions/multi-axis-decomposition.md (132 lines) — Architectural meta-pattern: when a domain has N independent axes (provider × wire × auth × deployment), decompose to N small enums + a dispatch table rather than 2^N concrete classes. Python class-based primary examples; Rust trait equivalent shown alongside. ADR cross-refs abstracted as 'two concurrent SDK design decisions'. Origin: kailash-rs multi-axis-decomposition-skill (used by kaizen.llm S9 + nexus extractor S1-S8).",
        "NEW GLOBAL SKILL: skills/test-skip-discipline/SKILL.md (236 lines, new directory) — Distinguishes acceptable test.skip (cannot execute: missing dependency, infra unavailable, platform constraint) from BLOCKED test.skip (system is broken: timeout, flaky, hangs CI). Cross-language (pytest, jest, Playwright, Rust). Mechanical /redteam grep audit included. Added to coc: tier in manifest.",
        "EXTENDED GLOBAL: rules/schema-migration.md § 7 — Replaced loom 2.8.12's § 3a force_drop block with renamed § 7 force_downgrade. Orchestrator-layer rename: MigrationManager.apply_downgrade(force_downgrade=True) raises DowngradeRefusedError. Expanded BLOCKED rationalizations to 8 entries. Added two-layer Relationship clarification: primitive force_drop in dataflow-identifier-safety.md § 4 stays unchanged (DDL guard); orchestrator force_downgrade is separate (migration apply_downgrade guard). Both Python + Rust DO/DO NOT examples preserved. File: 152 → 195 lines.",
        "EXTENDED GLOBAL: rules/cross-sdk-inspection.md MUST Rule 5 — New rule 'Cross-SDK Symbol Citations MUST Be Grep-Verified': when citing a Rust symbol from a kailash-py rule (or vice versa), the rule author MUST grep the cross-SDK source first to verify the symbol exists; if not, cite the abstract pattern instead. Verification-evidence commit-body format included. 6-entry BLOCKED rationalizations list. Authored at loom from proposal spec (kailash-rs BUILD source still 79-line short version). File: 79 → 127 lines. Origin: kailash-rs cross-sdk-inspection-cite-verification-extension.",
        "NEW GLOBAL GUIDE: guides/deterministic-quality/09-parallel-agent-bundles.md (81 lines) — Slot 09 (07/08 already used by destructive-operation-gates and cross-sdk-parity). Parallel-agent non-shard bundle pattern: when a session has many small independent fixes (use-feedback triage), launching them as parallel non-shard agents with worktree isolation is the right pattern (vs sequential todos). Shard-vs-non-shard decision matrix. Cross-references rules/autonomous-execution.md § Per-Session Capacity Budget. Origin: kailash-rs parallel-agent-non-shard-bundle-pattern.",
        "NEW VARIANT (py): variants/py/scripts/migrate.py (169 lines) — Python migration runner CLI for downstream Python projects consuming the migration-scaffold skill. Up/down/status subcommands wrapping kailash-py DataFlow. Added to variant_only.py in manifest.",
        "MANIFEST: tiers.coc added skills/test-skip-discipline/** glob. variant_only.py added variants/py/scripts/migrate.py. (Other new skills inherit from existing globs at skills/02-dataflow/**, skills/03-nexus/**, skills/13-architecture-decisions/**.)",
        "ALREADY-CODIFIED (skipped): llm-deployment-coverage, llm-auth-strategy-hygiene, observability-mask-http-auth-headers — proposal flagged as already_global (kailash-rs v3.18.0 S9 commit 45714c44 ships these). Verification of loom global observability.md 5-canonical-header masking rule deferred to a future codify cycle.",
        "CROSS-SDK QUEUED (for parent to file against terrene-foundation/kailash-py post-Gate 2): (1) MigrationManager.apply_downgrade force_downgrade rename + DowngradeRefusedError; (2) parallel grep-verification audit of any kailash-py skills citing kailash-rs symbols (per new MUST Rule 5); (3) test-skip discipline alignment if not already covered; (4) cross-SDK symbol citation grep verification (bidirectional with rs).",
        "PROPOSAL: kailash-rs/.claude/.proposals/latest.yaml advanced pending_review → reviewed with full Gate 1 review_notes documenting all 8 entries + manifest changes + cross-SDK follow-ups. Pre-2026-04-19 separator preserved (2026-04-17 entries already distributed in loom 2.8.12)."
      ]
    },
    {
      "version": "2.8.13",
      "date": "2026-04-19",
      "summary": "Gate 1 classification of kailash-py 2026-04-19 appended codify (#498 kaizen.llm four-axis wire-layer abstraction S1-S9). Reclassifies all 3 proposal entries from suggested global → variant-py because kaizen.llm content is Python-only (Pydantic + httpx + WireProtocol enum); Rust has its own parallel track in kailash-rs #406/#393/#394. Adds kaizen-llm-deployment.md to variant_only.py. Prior 2026-04-16 pre-release-sweep entries already distributed in loom 2.8.11; not re-reviewed.",
      "changes": [
        "NEW VARIANT (py): variants/py/skills/04-kaizen/kaizen-llm-deployment.md — four-axis LLM deployment abstraction (Provider × Model × WireProtocol × Deployment). Python examples use kaizen.llm.LlmClient API with Pydantic request/response models and httpx transport. Covers OpenAI / Anthropic / Ollama / vLLM / OpenRouter / Azure / Bedrock matrix. Origin: kailash-py #498 S1-S9 implementation + #462 LlmClient.embed() for OpenAI + Ollama.",
        "UPDATED VARIANT (py): variants/py/skills/04-kaizen/SKILL.md — added 'LLM Wire Layer (Lower-Level)' section linking kaizen-llm-deployment.md with progressive-disclosure entry for choosing between kaizen.llm.LlmClient (low-level wire layer) and kaizen.agents (high-level agent framework).",
        "UPDATED VARIANT (py): variants/py/agents/frameworks/kaizen-specialist.md — added 'LLM Wire Layer (kaizen.llm.LlmClient)' section covering the Provider × Model × WireProtocol × Deployment matrix, embed() method availability across providers, and cross-refs to kailash-rs #406 (preset parity), #393 (LlmClient::embed), #394 (wire layer). Reclassified global → variant-py because references are python-package-scoped.",
        "MANIFEST: variant_only.py added variants/py/skills/04-kaizen/kaizen-llm-deployment.md.",
        "SKIPPED (no cross-SDK action needed now): #480 PG identifier quoting in DataFlowExpress CRUD — implementation-level fix in kailash-py source; not a shared codification artifact. #462 LlmClient.embed() code lives in kaizen-llm-deployment.md reference.",
        "CROSS-SDK: kailash-rs tracks parallel LLM wire-layer work independently (#406 preset parity, #393 LlmClient::embed, #394 wire layer). When rs next ships a codify covering these, it will produce variants/rs/skills/04-kaizen/ equivalents via its own Gate 1.",
        "SDK VERSION MISMATCH (flagged): proposal declared sdk_version: 2.8.8 but kailash-py/pyproject.toml is 2.8.7 — proposal drafted in anticipation of a release that hasn't landed yet. Gate 2 reads 2.8.7 from pyproject.toml as the authoritative source.",
        "PROPOSAL: kailash-py/.claude/.proposals/latest.yaml advanced pending_review → reviewed with Gate 1 review_notes documenting all 3 entries + classification rationale + SDK version flag."
      ]
    },
    {
      "version": "2.8.12",
      "date": "2026-04-17",
      "summary": "Gate 1 classification of kailash-rs 2026-04-17 codify proposal (8 substantive entries: DataFlow classification mutation redaction + CI cascade recovery rules + allowlist extension pattern + Nexus webhook HMAC workaround + UI-backend allowlist defense + six-item COC hardening bundle #51 3a-3f). Splits ci-runners.md per user decision A (rules stay ≤200 lines, troubleshooting extracts to a skill). Adds one rs variant (numbered migration scaffold for Python binding consumers). Three cross-SDK tickets flagged for parent session to file against kailash-py. Nexus D1-D6 design decisions deferred to human review (not codified).",
      "changes": [
        "NEW GLOBAL RULE: rules/dataflow-classification.md (146 lines) — Four MUST clauses: mutation return-path MUST apply read redaction; delegation-based redaction MUST be pinned with a comment; every mutation return-path MUST have a Tier 2 redaction test; scalar aggregate returns are exempt. paths: **/dataflow/**, **/kailash-dataflow/**, **/*classification*, **/*redact*. Python examples using kailash-py DataFlow Express API (db.express.create/update/upsert returning dict, apply_read_classification helper, set_clearance context). Origin: kailash-rs commit 2e9dbf94 + gh-coc-claude-rs#51 item 3e.",
        "NEW GLOBAL RULE (SPLIT): rules/ci-runners.md (170 lines) — Split from BUILD rs repo's 328-line version per user decision A. Retains five MUST rules (toolchain setup per job, .env change → runner restart, post-fmt cascade triage, runner auto-update disconnect recovery, binding-CI paths filter coverage) and two MUST NOT rules (no committed registration tokens, every upload-artifact uses continue-on-error). Language-agnostic framing (gh api, launchctl, systemctl, GitHub Actions workflow YAML). Origin: kailash-rs CI cascade waves 6-18 (commits ecc50c4e..5429928c, 2026-04-16/17).",
        "NEW GLOBAL SKILL: skills/10-deployment-git/ci-runner-troubleshooting.md (185 lines) — Split companion to ci-runners.md. Five recovery protocols (A: runner auto-update disconnect, B: rustup proxy recovery, C: .env change recovery, D: post-fmt cascade triage, E: binding-CI path filter drift) + service-management reference (macOS launchd / Linux systemd) + registration token lifecycle + common error strings. Skill is the where-to-look when any ci-runners.md MUST clause fires.",
        "NEW GLOBAL SKILL: skills/13-architecture-decisions/allowlist-extension-pattern.md (183 lines) — Architectural pattern for extending a closed allowlist-based dispatcher via register_<thing>() API with identity-keyed (id(cls) / TypeId) fallback that runs AFTER the built-in downcast fails. Four-step pattern with BLOCKED anti-patterns (per-call allow_unknown=True flag, config-file string registration, duck-typed dispatch). Python examples. Cross-reference table of domains (ML estimators, node types, middleware, codecs, policies). Origin: kailash-rs#402 commit 5429928c.",
        "NEW GLOBAL RULE: rules/nexus-webhook-hmac.md (155 lines) — Three MUST clauses: HMAC MUST NOT be computed over re-serialized JSON inside a Nexus handler; acceptable workarounds are (a) ASGI/Tower middleware above Nexus or (b) reverse-proxy verification at the edge; every webhook handler relying on external verification MUST document that reliance in its docstring. Python examples use starlette.middleware.base.BaseHTTPMiddleware. paths: **/nexus/**, **/webhook*, **/*hmac*, **/signature*. Rule will be revised once the Nexus runtime handler-signature extension lands (D1-D6 design pending). Origin: gh-coc-claude-rs#51 items 1A + 1C.",
        "NEW GLOBAL RULE: rules/ui-backend-defense.md (169 lines) — Three MUST clauses: every backend handler with an enum/pluggable value MUST allowlist server-side (independent of UI); allowlist validators MUST reject unexpected SHAPE explicitly; layered defense at handler + validator + model. Python examples use Pydantic Literal[...] + explicit isinstance checks. BLOCKED rationalizations verbatim (\"UI already restricts this\", \"Admin-only endpoint, no allowlist needed\", \"Model validator catches non-dict inputs anyway\"). Mechanical audit protocol included. paths: **/routes/**, **/handlers/**, **/api/**, **/*_endpoint*, **/*_handler*, **/*_route*. Origin: gh-coc-claude-rs#51 item 2.",
        "NEW GLOBAL SKILL: skills/03-nexus/nexus-http-status-convention.md (186 lines) — Error response convention {error: human-readable, status: int}. Full status-code catalog (400/401/403/404/409/413/422/429/500/502/503) with Python examples per code. Tier 2 test template. No-no patterns (bare strings, stack traces, custom top-level keys, mismatched status vs HTTP code). Deviation notes for streaming/OpenAPI-first/legacy endpoints. Origin: gh-coc-claude-rs#51 item 3a.",
        "NEW VARIANT (rs): variants/rs/skills/02-dataflow/numbered-migration-scaffold.md (280 lines) — Python scaffold for rs USE template consumers (Python binding writers, not Rust developers). migrations/__init__.py with discovery, scripts/migrate.py CLI (up/down/status subcommands), three example migrations demonstrating force_drop=True pattern from new rule 3a. CLI invocation uses `import kailash; kailash.dataflow.DataFlow(url)`. Notes when to graduate to Alembic/sqlx/Django migrations. Origin: gh-coc-claude-rs#51 item 3b.",
        "EXTENDED GLOBAL: rules/schema-migration.md § 3a — Added new subsection \"Destructive downgrade() MUST Require force_drop=True\" mirroring dataflow-identifier-safety.md § 4 DROP-refusal rule. Python example raises DropRefusedError when force_drop is False. BLOCKED rationalizations (\"downgrade() is only called in recovery\", \"integration test needs to call downgrade() often\"). Origin: gh-coc-claude-rs#51 item 3c.",
        "EXTENDED GLOBAL: rules/tenant-isolation.md § 1 — Added DO example keeping tenant_id in cache key even when secondary key is a per-tenant-unique UUID. BLOCKED rationalizations (\"UUID is already unique\", \"We can save 36 bytes\"). Anti-optimization hedge against future secondary-key schema changes (UUID → slug/sequence/email) that could reintroduce cross-tenant collisions. Origin: gh-coc-claude-rs#51 item 3d.",
        "EXTENDED GLOBAL: rules/orphan-detection.md § 2a — Added new subsection \"Crypto-Pair Round-Trip MUST Be Tested Through The Facade\". Crypto pairs (encrypt/decrypt, sign/verify, seal/unseal, wrap_key/unwrap_key) have the same \"framework never round-trips\" failure mode as manager classes — isolated Tier 1 tests mock each other's half and never detect algorithm drift (encrypt uses GCM, decrypt uses CBC). Python round-trip examples. Origin: gh-coc-claude-rs#51 item 3f.",
        "NEW GLOBAL RULE: rules/event-payload-classification.md (150 lines) — Event-surface sibling of dataflow-classification.md. Every DomainEvent payload from DataFlow write paths MUST be free of raw classified field values: classified PK values MUST be hashed before emission; classified field names MUST NOT appear in fields_changed lists. Single-point filter at the emitter (dataflow.classification.event_payload.format_record_id_for_event) — BLOCKED rationalizations include 'placing the check at each caller site' and 'event bus already filters'. Origin: kailash-py codify session (BP-048 parity with kailash-rs v3.17.1+).",
        "MANIFEST: tiers.coc added rules/dataflow-classification.md, rules/ci-runners.md, rules/nexus-webhook-hmac.md, rules/ui-backend-defense.md, rules/event-payload-classification.md. New skills (skills/03-nexus/**, skills/10-deployment-git/**, skills/13-architecture-decisions/**) inherit from existing glob patterns — no explicit per-file entries needed.",
        "MANIFEST: variant_only.rs added variants/rs/skills/02-dataflow/numbered-migration-scaffold.md.",
        "NEXUS D1-D6: Design decisions confirmed — D1 = Option D (NexusExtract trait + IntoResponse return trait, extensible per-type extractors vs magic-named params); D2 = http::HeaderMap (Python CIMultiDict); D3 = case-insensitive via HeaderMap; D4 = multiple extractors (Bytes/BytesMut/BodyStream); D5 = async-only contract; D6 = dissolves in extractor model. Shard plan S1-S8 (~2130 LOC across 8 shards, each ≤500 LOC load-bearing cap) filed as tracking issue esperie-enterprise/kailash-rs#404. rules/nexus-webhook-hmac.md Origin updated to reference the tracking issue.",
        "CROSS-SDK TICKETS FILED (against terrene-foundation/kailash-py): #494 mutation-return redaction parity (gh-coc-claude-rs#51 item 3e); #495 register_estimator parity (kailash-rs#402); #496 PG placeholder bug-class inspection (kailash-rs#403); #497 Nexus webhook HMAC raw-body gap follow-up (companion to Nexus D1-D6 tracking issue). All four tickets reference the new loom globals as shared-principle anchors.",
        "PROPOSAL: kailash-rs/.claude/.proposals/latest.yaml advanced pending_review → reviewed with full Gate 1 review_notes documenting all 10 entries + user decisions A/B/C/D/E. Gate 2 distribution (coc-sync agent) handled in a subsequent session."
      ]
    },
    {
      "version": "2.8.11",
      "date": "2026-04-17",
      "summary": "Gate 1 classification of kailash-py 2026-04-16 codify (DataFlow __del__ deadlock + orphan-detection test-sweep rules + TrustPosture canonical reference). Distributes alongside kailash-py 2.8.8.",
      "changes": [
        "UPDATED GLOBAL: rules/patterns.md § Async Resource Cleanup — added MUST NOT close()/cleanup()/log-touching method from __del__, DO/DO NOT code examples (_warnings=warnings pattern vs async_safe_run deadlock), 4 BLOCKED rationalizations, deadlock-chain Why extension (finalizer → close() → asyncio.new_event_loop → selector init → logger.debug → root logging lock held by GC), and Origin: kailash-py commit 4f5dbe7f. py target inherits via global (py: null; rs/rb have own variants).",
        "UPDATED GLOBAL: rules/orphan-detection.md — added Rule 4 (API removal MUST sweep tests in same PR: collection ModuleNotFoundError blocks whole suite, 5 BLOCKED rationalizations, evidence from commits d3e7e0ef + 5edc941f) and Rule 5 (collect-only is a merge gate: pytest --collect-only exit 0 required before any merge). Extended Detection Protocol with step 4 (collect-only sweep across tests/ + packages/*/tests/). BUILD-internal path reverted to generic comment.",
        "NEW VARIANT (py): variants/py/skills/26-eatp-reference/eatp-trust-posture-canonical.md (74 lines) — canonical TrustPosture names (AUTONOMOUS/DELEGATING/SUPERVISED/TOOL/PSEUDO) vs four backward-compat aliases (DELEGATED/CONTINUOUS_INSIGHT/SHARED_PLANNING/PSEUDO_AGENT), mapper branch → posture table, autonomy_level table, serialization contract (.value is lowercase). HUMAN_DECIDES → TOOL (not PSEUDO) is the mapping that keeps getting wrong. Origin: two consecutive kailash-py session wrapups inverted canonical vs alias before source verification caught it.",
        "NEW VARIANT (py): variants/py/skills/26-eatp-reference/SKILL.md — index entry linking the new trust posture reference with 'Load first when touching trust posture tests' hint. Global SKILL.md left untouched (would create dangling link in rs/rb/prism targets).",
        "MANIFEST: variants section — added skills/26-eatp-reference/SKILL.md → py: variants/py/skills/26-eatp-reference/SKILL.md.",
        "MANIFEST: variant_only.py — added variants/py/skills/26-eatp-reference/eatp-trust-posture-canonical.md.",
        "SKIPPED: rules/patterns.md Part A (BUILD repo removed defensive str() wrapper from express.read examples). Global keeps defensive str() pattern as the reference-document guidance.",
        "Cross-SDK: No immediate rs adaptation needed. If kailash-rs encounters its own TrustPosture alias confusion, a variants/rs equivalent referencing Rust source would be appropriate at that time.",
        "Proposal: kailash-py/.claude/.proposals/latest.yaml advanced pending_review → reviewed. Gate 2 distributes to kailash-coc-claude-py USE template."
      ]
    },
    {
      "version": "2.8.10",
      "date": "2026-04-15",
      "summary": "Gate 1 classification of kailash-rs codify (DataFlow annotationlib fallback for Python 3.14 + parity test code-execution feature fix). Author missing skill .md from YAML draft (same retroactive-authoring pattern as 2.8.7). Distributes alongside kailash-rs 3.16.3 release that ships the underlying fix.",
      "changes": [
        "NEW VARIANT (rs): variants/rs/skills/06-python-bindings/annotation-resolution-fallback.md (227 lines) — codifies two-tier fallback for `typing.get_type_hints` NameErrors. Happy path unchanged. On NameError: Python 3.14+ branch uses `annotationlib.get_annotations(cls, format=Format.FORWARDREF)` (PEP 749) to emit per-field RuntimeError naming the @db.model class + unresolvable fields + three remediations (TYPE_CHECKING-guarded import, forward ref, circular import). Pre-3.14 branch raises a clearer class-aware RuntimeError. Captures four non-obvious gotchas: PEP 749 happy-path compliance, annotationlib returns `str` not `ForwardRef`, `from __future__ import annotations` poisons annotationlib output, Pyright unreachable-warning is a tooling limitation. Origin: kailash-rs commit 5f405574.",
        "UPDATED VARIANT (rs): variants/rs/skills/06-python-bindings/SKILL.md — index row added in alphabetical position for annotation-resolution-fallback.md.",
        "MANIFEST: variant_only.rs adds variants/rs/skills/06-python-bindings/annotation-resolution-fallback.md (alphabetical insert before async-bridging.md).",
        "SKIPPED (no reusable pattern): parity-code-execution-feature-fix — one-line Cargo.toml `features = [\"code-execution\"]` addition. Lesson 'enable the feature you test against' already covered by existing testing.md + cargo-specialist rules; the codifier concurred.",
        "RETROACTIVE-AUTHORING: kailash-rs /codify drafted the annotationlib skill as YAML prose in the proposal but never wrote the .md to disk. Same pattern as loom 2.8.7 (which retroactively authored 5 missing artifacts from a kailash-rs codify session). Gate 1 of /sync rs completes the codify cycle by writing the canonical .md at loom. Proposal advanced pending_review → reviewed; Gate 2 distributes to kailash-coc-claude-rs USE template."
      ]
    },
    {
      "version": "2.8.9",
      "date": "2026-04-15",
      "summary": "Gate 1 classification of kailash-rs#401 codify (rust-version-bump skill — cross-SDK parity with kailash-py's python-version-bump). Resolves the cross-SDK follow-up flagged in loom 2.8.8.",
      "changes": [
        "NEW VARIANT (rs): variants/rs/skills/10-deployment-git/rust-version-bump.md (312 lines) \u2014 canonical procedure for (A) raising rustc MSRV and (B) adding/dropping a CPython target in the PyO3 wheel release matrix. Encodes the PyO3 forward-compat flag pattern, maturin-action vs bare maturin build lesson, and the real file inventory across release.yml + python.yml.",
        "UPDATED VARIANT (rs): variants/rs/skills/10-deployment-git/SKILL.md \u2014 added 'Version Bumps' sub-section referencing the new rust-version-bump skill with cross-SDK prose reference to kailash-py's python-version-bump.",
        "MANIFEST: variants: skills/10-deployment-git/python-version-bump.md \u2014 rs: variants/rs/skills/10-deployment-git/rust-version-bump.md (rs target now gets the Rust procedure instead of the Python one).",
        "SKIPPED (already covered): ci-use-maturin-action-in-pr-ci (covered in Procedure B Step 7 of rust-version-bump.md); pyo3-forward-compat-flag-pattern (covered in Procedure B Step 3).",
        "Resolves esperie-enterprise/kailash-rs#401. The two skills now ship in lockstep: py target gets python-version-bump.md, rs target gets rust-version-bump.md, both cross-reference each other. kailash-rs proposal advanced pending_review \u2192 distributed."
      ]
    },
    {
      "version": "2.8.8",
      "date": "2026-04-15",
      "summary": "Gate 1 classification of kailash-py PR #474 codify (Python version bump procedure skill). Delete stale variants/py/commands/sync.md — loom global handles all repo-type branching.",
      "changes": [
        "NEW SKILL (global): skills/10-deployment-git/python-version-bump.md \u2014 canonical procedure for adding support for a new Python version (classifier additions, CI matrix updates, pyproject bounds). Authored at kailash-py during PR #474 (Python 3.14 support); classified GLOBAL because the procedure is language-pattern-agnostic. Cross-SDK: kailash-rs should author an equivalent rust-version-bump.md variant covering cargo MSRV + rust-toolchain + PyO3 support matrix.",
        "UPDATE (global): skills/10-deployment-git/SKILL.md \u2014 index entry for python-version-bump between deployment-ci and the Docker section; collateral markdown fix on __init__.py rendering.",
        "DELETE (variant): variants/py/commands/sync.md \u2014 stale (pre-unified Gate 1 + Gate 2 + Downstream Sync command). Loom global commands/sync.md now handles all repo-type branching; the py variant overlay is no longer needed and would REGRESS the USE template's sync.md if re-applied. The 7 remaining variants/py/commands/ files (ai, api, cc-audit, db, release, sdk, test) are intentional py-specific overlays and stay.",
        "PROPOSAL: kailash-py/.claude/.proposals/latest.yaml advanced pending_review \u2192 distributed with Gate 1 review_notes documenting the classification and cross-SDK follow-up.",
        "Origin: kailash-py PR #474 (Python 3.14 support) codify session. Meta-relevant to loom 2.8.7's Python 3.14 classifier additions on kailash-coc-claude-py PR #53 and kailash-coc-claude-rs PR #48 \u2014 the new skill is the canonical doc for the manual procedure that session executed."
      ]
    },
    {
      "version": "2.8.7",
      "date": "2026-04-15",
      "summary": "Author 5 missing artifacts from kailash-rs BP-041..047 codify (rules + skills that /codify recorded as YAML drafts but never wrote to disk). Place ffi-specialist as variants/rs. Adapt nexus-specialist variant with ServiceClient Python binding usage. Manifest: independence.md rs mapping + 5 variant_only.rs entries + CLAUDE.md exclude defense-in-depth.",
      "changes": [
        "RULE: rules/testing.md \u2014 add 'Delegating Primitives Need Direct Coverage' section (global, Python example) with mechanical /redteam grep enforcement. Origin: BP-046 kailash-rs ServiceClient put_raw/delete_raw transitive-only coverage gap.",
        "VARIANT: variants/rs/rules/testing.md \u2014 add same section with Python-binding-through-FFI framing. Origin: BP-046 binding test layer.",
        "SKILL: skills/12-testing-strategies/impossibility-surface.md \u2014 NEW global skill. Justification framework for happy-path test gaps that a documented lower-layer security control makes physically unreachable. Strict scope_note prevents misuse as 'I haven't written this yet' excuse. Origin: BP-047 kailash-rs Python binding SSRF-blocks-loopback impossibility.",
        "VARIANT: variants/rs/skills/06-python-bindings/typed-exception-hierarchy.md \u2014 NEW skill. PyO3 pattern for splitting a Rust error enum into a base + N typed subclasses with dispatched conversion. Threshold: 3+ variants triggers mandatory hierarchy. Origin: BP-041 ServiceClient 6-subclass split.",
        "VARIANT: variants/rs/skills/06-python-bindings/layered-truncation.md \u2014 NEW skill. Two-boundary truncation pattern: ~4KB Rust forensic body for logs, ~512B Python user-facing body for tracebacks. Includes char-boundary safety + truncation marker. Origin: BP-043 PY_SERVICE_CLIENT_ERROR_BODY_BYTES.",
        "VARIANT: variants/rs/skills/18-security-patterns/header-validation-at-construction.md \u2014 NEW skill. Builder methods accepting headers MUST return Result; convenience methods (with_bearer_token) MUST route through try_with_header so CRLF in tokens fails at construction, not first request. Origin: BP-042 + v3.16.1 hotfix.",
        "VARIANT: variants/rs/agents/ffi-specialist.md \u2014 first-time placement of pre-existing BUILD-local agent (284 lines, PyO3/napi-rs/wasm/CGo/JNA, Go and Java binding patterns added in 2026-04-15 codify).",
        "VARIANT: variants/rs/agents/frameworks/nexus-specialist.md \u2014 add ServiceClient + HttpClient rows to 'Additional Types' table; add Python-binding ServiceClient usage section with construction-time validation example, typed exception discrimination, raw vs typed call pattern, and 6 invariants. Cross-references the 4 new skills.",
        "MANIFEST: rules/independence.md rs mapping null \u2192 variants/rs/rules/independence.md (post-2.8.6 reversal placement).",
        "MANIFEST: variant_only.rs adds (5 entries): ffi-specialist.md, header-validation-at-construction.md, typed-exception-hierarchy.md, layered-truncation.md.",
        "MANIFEST: exclude adds CLAUDE.md \u2014 defense-in-depth so /sync can never push a CLAUDE.md into a target's .claude/ directory (which would activate hierarchically every time any artifact is loaded). No current risk (no .claude/CLAUDE.md exists in any BUILD/USE repo) but cheap structural marker.",
        "Origin: kailash-rs proposal latest.yaml 'P1-P4 binding-parity merge + codification + BP-041..047 ServiceClient hardening' \u2014 the 5 codify items were drafted in YAML prose but the corresponding .md files were never written to BUILD. This 2.8.7 entry retroactively completes the codify session by authoring the artifacts at loom (canonical), then propagating via /sync-to-build rs + /sync-to-build py (cross-SDK globals) + /sync rs Gate 2 (USE template)."
      ]
    },
    {
      "version": "2.8.6",
      "date": "2026-04-15",
      "summary": "Revert 2026-04-13 kailash-rs-as-Aegis misidentification. kailash-rs is the proprietary Kailash Rust SDK (esperie), NOT Aegis. Aegis is a separate proprietary PACT-spec implementation (Integrum Global) built on top of kailash-rs, living outside this repo. Full scrub across kailash-rs BUILD (CLAUDE.md, independence.md, 7 workspace doc edits, 6 pending journal deletions, archived proposal annotation) and USE template VERSION changelog annotation. atelier and terrene verified clean.",
      "changes": [
        "BUILD kailash-rs CLAUDE.md \u2014 restored heading 'Kailash Rust SDK', removed 'Aegis Identity' Directive 0, added 'describe this repo on its own terms' statement (user-applied).",
        "BUILD kailash-rs .claude/rules/independence.md \u2014 REWRITE: now correctly frames kailash-rs as proprietary Kailash Rust SDK (100% parity with kailash-py TF, peer not sibling), with Aegis as a separate downstream PACT-implementation by Integrum Global. Two-layer, two-track table: Specs (TF) / SDK (kailash-py OSS vs kailash-rs proprietary) / PACT-impl (pact platform OSS vs aegis proprietary).",
        "BUILD kailash-rs workspace doc edits (7 replacements across 5 files): eatp-merge/briefs, kailash-pact-rs/briefs + synthesis, trust-plane cross-analysis-eatp-gaps (3 sites), trust-plane todos/000-master, tool-agent-support invocation-token-adr \u2014 replaced 'proprietary Aegis product code' / '(Aegis product)' / 'part of Aegis' with 'Kailash Rust SDK' framing.",
        "BUILD kailash-rs .pending journal entries \u2014 deleted 6 auto-generated SessionEnd-hook stubs that reinforced the Aegis framing (never promoted to real journal entries).",
        "BUILD kailash-rs .claude/.proposals/archive/2026-04-13-kailash-rs.yaml \u2014 annotated codify_session and per-file reasons with '(REVERTED 2026-04-15)' markers explaining the misidentification.",
        "USE kailash-coc-claude-rs/.claude/VERSION 2.8.0 changelog entry \u2014 annotated with '(REVERTED 2026-04-15)' cross-reference to this 2.8.6 entry.",
        "Verified clean (no action needed): loom/ (Aegis refs are legitimate downstream-consumer Entrypoint lists), atelier/ (901 files, 0 violations), terrene/ ecosystem (foundation, publications, website, contrib \u2014 TF-constitution compliant), kailash-coc-claude-rs/.claude/rules/independence.md (already a generic 'Proprietary Product & Foundation Boundary' rule with no Aegis self-id), kailash-rs/docs/00-authority/10-source-protection.md (correctly frames 'Kailash Python SDK' + 'Kailash Rust SDK'), kailash-rs crates/*/README.md, Cargo.toml descriptions, and bindings/*/READMEs.",
        "Origin: TF-mandate-compliance scrub 2026-04-15. User clarification of four-quadrant mapping: kailash-py (TF OSS) \u2194 kailash-rs (proprietary, esperie) at the SDK layer; pact platform (TF OSS, built on kailash-py) \u2194 aegis (proprietary, Integrum Global, built on kailash-rs) at the PACT-implementation layer; specs (CARE/EATP/CO/PACT) CC BY 4.0 TF-owned at the top."
      ]
    },
    {
      "version": "2.8.5",
      "date": "2026-04-15",
      "summary": "nexus-for-fastapi-users skill: document app.include_router() zero-rewrite migration path (Nexus 2.0.3).",
      "changes": [
        "SKILL: 03-nexus/nexus-for-fastapi-users.md \u2014 add 'Zero-Rewrite Migration: app.include_router()' section covering APIRouter mount, prefix/tags/dependencies kwargs, pre/post start() queue semantics, TypeError on non-APIRouter, prefix-conflict WARN, and handoff criterion to Nexus handlers for multi-channel (CLI + MCP) routes."
      ]
    },
    {
      "version": "2.8.4",
      "date": "2026-04-14",
      "summary": "sync py: testing.md Test Resource Cleanup Discipline (global) + deployment.md py variant (PyPI pinning, __init__ tracked in git, multi-tag push).",
      "changes": [
        "RULE: testing.md \u2014 add Test Resource Cleanup Discipline section with 4 MUST rules (fixture yield+cleanup, AsyncMock for async side_effects, Stub naming, JWT test secrets \u226532 bytes).",
        "VARIANT: variants/py/rules/deployment.md \u2014 new py overlay with 3 MUST sections (PyPI-resolvable extras pins, __init__.py imports tracked in git, multi-package release tags pushed individually).",
        "MANIFEST: rules/deployment.md py variant entry filled."
      ]
    },
    {
      "version": "2.8.3",
      "date": "2026-04-14",
      "summary": "SDK pin staleness detection via local VERSION sdk_packages + nexus-for-fastapi-users skill with rs variant.",
      "changes": [
        "HOOK: session-start.js \u2014 checkPinsAgainstBuild reads local .claude/VERSION sdk_packages, compares against pyproject.toml pins, warns on stale.",
        "SYNC: /sync command step 8 mandates sdk_packages maintenance in target VERSION files.",
        "SKILL: 03-nexus/nexus-for-fastapi-users.md \u2014 global (pure Python SDK) + rs variant (NexusApp, NexusAuthPlugin, AuthGuard, ctx.user).",
        "MANIFEST: variant declaration for nexus-for-fastapi-users.md (rs only).",
        "VERSION: upstream.sdk_packages added to py and rs USE template VERSION files."
      ]
    },
    {
      "version": "2.8.2",
      "date": "2026-04-13",
      "summary": "Enforce high effort + disable adaptive thinking across all repos.",
      "changes": [
        "SETTINGS: Added effortLevel: high to all project settings.json files.",
        "SETTINGS: Added CLAUDE_CODE_DISABLE_ADAPTIVE_THINKING=1 env var to all project settings.json files.",
        "SCOPE: loom, 2 BUILD repos (py, rs, prism), 4 USE templates (py, rs, rb, prism)."
      ]
    },
    {
      "version": "2.8.1",
      "date": "2026-04-11",
      "summary": "Ingest kailash-py 2.6.0 arbor session rules + fix USE-vs-BUILD path debt in 5 global rules.",
      "changes": [
        "INGESTED: rules/python-environment.md \u2014 2 new MUST rules from kailash-py arbor session: explicit venv interpreter + monorepo editable installs. Distributed to both py and rs templates.",
        "INGESTED: rules/infrastructure-sql.md \u2014 \u00a78 rewritten from soft to MUST (lazy driver imports), \u00a78a added (regression test). Distributed to both templates.",
        "FIXED: rules/dataflow-pool.md \u2014 src/ \u2192 . in audit protocol rg command (USE-friendly)",
        "FIXED: rules/orphan-detection.md \u2014 BUILD-specific path comment \u2192 generic",
        "FIXED: rules/schema-migration.md \u2014 BUILD src/kailash/db/ scope text \u2192 USE-appropriate",
        "FIXED: rules/tenant-isolation.md \u2014 src/ \u2192 . in 4 audit protocol rg commands",
        "FIXED: rules/zero-tolerance.md Rule 4 \u2014 BUILD 'fix directly' \u2192 USE 'file GitHub issue'",
        "STRUCTURAL: loom globals are now USE-oriented (templates are the primary consumer). BUILD repos maintain local copies with BUILD-specific paths."
      ]
    },
    {
      "version": "2.8.0",
      "date": "2026-04-09",
      "summary": "Deterministic quality guide + rule-authoring meta-rule. Institutionalizes 0052-DISCOVERY session productivity patterns, validated by subprocess A/B tests. New guide (8 files): rule authoring principles (Loud/Linguistic/Layered), session architecture (4-layer loading model), enforcement ladder (Rung 0-6), plus SDK primitive patterns (type system, runtime safety, observability, destructive gates, cross-SDK parity).",
      "changes": [
        "NEW GUIDE: guides/deterministic-quality/ \u2014 8 files + README. Part I (CO artifact insights): rule-authoring principles, session architecture, enforcement ladder. Part II (code primitives): type system enforcement, runtime safety defaults, observability primitives, destructive operation gates, cross-SDK parity.",
        "NEW RULE: rules/rule-authoring.md \u2014 meta-rule for authoring COC rules. 6 MUST clauses + Loud/Linguistic/Layered test. Path-scoped to **/rules/*.md. Subprocess-tested: rule quality improved from 2/6 to 6/6.",
        "UPDATED: rules/agents.md \u2014 gate reviews at /implement and /release upgraded from RECOMMENDED to MUST with background agent pattern + BLOCKED phrases for skip rationalizations.",
        "UPDATED: rules/git.md \u2014 commit bodies MUST answer 'why' not 'what', with DO/DON'T example.",
        "UPDATED: rules/observability.md \u2014 new \u00a76: bulk ops MUST log at WARN on partial failure. Source-verified: except Exception: continue with zero logging in kailash-py bulk_create.py:496-498. BLOCKED phrases added.",
        "UPDATED: rules/cc-artifacts.md \u2014 new \u00a710: new rules MUST follow rules/rule-authoring.md.",
        "UPDATED: CLAUDE.md \u2014 deterministic-quality guide added to Guides section; rule-authoring added to Rules Index."
      ]
    },
    {
      "version": "2.7.1",
      "date": "2026-04-08",
      "summary": "Hotfix: Stop hook schema (systemMessage not hookSpecificOutput) + BUILD path contamination removal from USE templates. Downstream repos that synced 2.7.0 must re-sync.",
      "changes": [
        "FIX: scripts/hooks/log-triage-gate.js \u2014 Stop hooks don't support hookSpecificOutput (PreToolUse/PostToolUse only). Changed to systemMessage field. Fixes JSON validation error on session end.",
        "FIX: USE template rules/schema-migration.md \u2014 removed literal src/kailash/db/ path from scope clarification (triggers CI contamination check). Replaced with generic 'SDK dialect helper layer' language.",
        "FIX: USE template skills/spec-compliance/SKILL.md \u2014 removed literal src/kailash/mcp_server/ paths from MOVE check examples. Replaced with generic old_path/new_path placeholders.",
        "FIX: 7 variant agent descriptions (5 py + 2 rs) updated to match 2.7.0 global description rewrites \u2014 variants had stale frontmatter that would push OLD descriptions to BUILD/USE targets via overlay."
      ]
    },
    {
      "version": "2.7.0",
      "date": "2026-04-08",
      "summary": "Framework mandate enforcement wave. Addresses 7 post-deployment pain points: (1) no logging at boundaries, (2) logs ignored, (3) pre-existing warnings dismissed, (4) DataFlow bypass \u2192 silent fail, (5) zero discipline / patch-forget, (6) direct SQL instead of migrations, (7) missing dependencies ignored. Three-wave approach: content rules (observability, schema-migration, strengthened zero-tolerance / framework-first / dependencies), semantic activation (16 framework agent/skill descriptions rewritten for failure-mode matching + ABSOLUTE work-domain \u2192 framework mandate table), enforcement hooks (integration-hygiene PostToolUse, log-triage-gate Stop, /implement + /redteam checklist additions, reviewer + testing-specialist gate updates).",
      "changes": [
        "NEW RULE: rules/observability.md \u2014 mandatory structured log points at endpoints, integration points, data calls (real/fake/simulated), state transitions, auth events, config loads. Concrete log-triage scan commands (pytest/build/find -mmin -120) + dedup protocol (per unique entry, not per occurrence). Scoped to **/*.py, **/*.rs, **/*.ts/tsx, **/*.js/jsx.",
        "NEW RULE: rules/schema-migration.md \u2014 all schema + data changes via numbered migrations; no hotfix SQL; reversible down() required; append-only migration files; production schema sync as deploy gate. Scope clarification disambiguates BUILD (SDK src/kailash/db/ dialect helpers permitted) from USE (application code routes through @db.model / db.express). Scoped to **/*.py, **/*.rb, migrations/**, db/**.",
        "EXTENDED: rules/zero-tolerance.md Rule 1 \u2014 scope now includes compiler warnings, linter warnings, deprecation notices, WARN/ERROR log entries, runtime warnings, peer-dep warnings. New BLOCKED phrases 'Warning non-fatal proceeding', 'Deprecation warning will address later', 'Notice only not blocking'. Mechanism cross-reference points at observability.md MUST Rule 5 for concrete scan commands. New exception clause for upstream third-party deprecations (requires pinned version + documented reason OR upstream issue link OR explicit owner \u2014 silent dismissal still BLOCKED). Inner 'Scope' heading renamed to 'Applies to' to avoid collision with file-level ## Scope.",
        "EXTENDED: rules/framework-first.md \u2014 NEW ABSOLUTE 'Work-Domain \u2192 Framework Binding' mandate table at the top of the rule. Maps 9 work domains (LLM/agent, DB, data pipeline, HTTP gateway, MCP, Align, ML, PACT governance, Core SDK foundation) to their MANDATORY framework. Explicit authN (Nexus) vs authZ (PACT) split note. NEW 'Specialist Consultation Before Dropping Below Engine Layer' section with pattern-level trigger table (raw SQL \u2192 dataflow-specialist, raw HTTP \u2192 nexus-specialist, etc.). Cross-reference to agents.md clarifies this is stricter pattern-level gate on top of agents.md framework delegation.",
        "EXTENDED: rules/dependencies.md \u2014 NEW 'Declared = Imported' section requiring every import to resolve to a manifest entry in the same commit. ModuleNotFoundError / cannot-find-crate / Cannot-find-module / peer-dep warnings are SAME class as pre-existing failures (zero-tolerance Rule 1). BLOCKED anti-patterns: silent try/except ImportError fallback, type:ignore[import], @ts-ignore on missing modules. NEW exception for optional extras with LOUD failure (raises descriptive ImportError naming the extra at call site; silent None propagation still BLOCKED). Paths widened from manifest-only to also match **/*.py, **/*.rs, **/*.ts/tsx, **/*.js/jsx so the rule fires when the violation occurs (not only when the manifest is edited).",
        "EXTENDED: rules/dataflow-pool.md Rule 2 \u2014 added reachability check requirement. DataFlow.__init__ MUST run SELECT 1 health check alongside pool-config validation; failed check MUST log ERROR and prevent application startup, not silently degrade. DO/DO NOT: fail-fast vs lazy_connect=True anti-pattern. Scope note at top: application code routes through DataFlow @db.model; these patterns are for pool tuning, not for bypassing DataFlow.",
        "EXTENDED: rules/connection-pool.md \u2014 scope note at top: application code MUST go through DataFlow which manages pools for you; these rules are for SDK-level pool tuning and advanced pool-lifecycle consumers.",
        "EXTENDED: rules/infrastructure-sql.md \u2014 scope note at top: application code MUST go through DataFlow (@db.model, db.express); these patterns are for the SDK source tree and dialect helper layer only.",
        "NEW VARIANT: variants/rb/rules/observability.md \u2014 Ruby overlay (SemanticLogger / Rails.logger, ActionDispatch::RequestId, tagged logging). Mandatory log points 1-4 (endpoints, integration, data-calls with mode field, state/auth/config). Four MUST NOT rules (no bare rescue, no log-and-continue, no puts/p/pp, no unstructured interpolation, no hot-loop spam, no level downgrades). Full log-triage gate with bundle check, RSpec scan, Rails log scan.",
        "NEW VARIANT: variants/rb/rules/schema-migration.md \u2014 Ruby overlay (ActiveRecord migrations, db/migrate/*, change vs up/down, ActiveRecord::IrreversibleMigration as the real irreversibility marker \u2014 NOT disable_ddl_transaction! which is for non-transactional DDL like CONCURRENTLY indexes). Sequel parenthetical in header for Sequel projects.",
        "TRANSLATED: variants/rs/rules/framework-first.md \u2014 Rust syntax throughout the file (hierarchy line, table Raw column, 2 code blocks) translated to Python per LANGUAGE MAPPING (rs variant serves Python/Ruby binding consumers, not Rust developers). Table now lists Python anti-patterns (psycopg, sqlalchemy.text, fastapi, flask, openai, anthropic, sklearn, numpy, pandas, transformers, peft, trl). Code blocks use unqualified canonical names (DataFlowEngine.builder(), AutoMLEngine) matching the global file style; delegates canonical import path lookup to the framework specialists. Specialist consultation section propagated from global.",
        "DESCRIPTION REWRITE (\u00d78 agents): dataflow-specialist, nexus-specialist, kaizen-specialist, mcp-specialist, mcp-platform-specialist, pact-specialist, ml-specialist, align-specialist. All use 'Use proactively for \u2026' trigger and name failure-mode domain in plain language (LLM/prompt/agent/RAG, DB/cache/schema/query, HTTP/API/websocket/login, MCP server/tool/exposing APIs as LLM tools, RBAC/policy/access control, ML training/inference/feature, LoRA/fine-tuning). Each description ends with a BLOCKED clause naming the most common anti-pattern (custom LLM services, FastAPI/Flask, raw SQL & ORMs, raw JSON-RPC, custom authz, raw sklearn/torch, raw TRL/PEFT/transformers, custom plugin loaders). All 8 within 120-char budget (106-118).",
        "DESCRIPTION REWRITE (\u00d78 skills): 01-core-sdk, 02-dataflow, 03-nexus, 04-kaizen, 05-kailash-mcp, 29-pact, 34-kailash-ml, 35-kailash-align. All open with 'MANDATORY for ALL <domain> work'. Each names the failure-mode vocabulary Claude would see in a todo (repositories, caches, Redis, websockets, SSE, LLMProviderService-style wrappers, hand-rolled provider dispatchers, RBAC, roles, exposing internal APIs as LLM tools, 'just a quick sklearn pipeline', etc.). Each ends with expanded BLOCKED list (SQLAlchemy, psycopg, peewee, MongoDB drivers, redis-py, aioredis, FastAPI, Flask, Starlette, aiohttp, openai/anthropic/litellm/langchain wrappers, raw sklearn/pytorch/numpy/pandas, raw TRL/PEFT/transformers).",
        "NEW HOOK: scripts/hooks/integration-hygiene.js \u2014 Node.js PostToolUse on Edit|Write. Detects raw SQL strings outside migration/db/dialect paths, MOCK_/FAKE_/DUMMY_/SAMPLE_ constants, silent exception swallows (except: pass, catch(e){}, bare rescue), endpoint handlers with zero logger calls in file, raw HTTP clients with no surrounding log. WARN-only (exit 0, never blocks). Timeout 3s with fallback {continue: true}. Skips migrations/tests/generated files.",
        "NEW HOOK: scripts/hooks/log-triage-gate.js \u2014 Node.js Stop hook. At session end, scans *.log files modified in last 120 min (find -mmin -120, portable GNU+BSD), greps WARN/ERROR/FAIL, dedups by (file, normalized message pattern \u2014 timestamps/hex/large numbers collapsed). Outputs up to 10 unique entries + count. Advisory only; /wrapup owns the hard gate. Timeout 5s with fallback.",
        "SETTINGS: .claude/settings.json \u2014 registered integration-hygiene.js in PostToolUse Edit|Write hook list (timeout 5s); registered log-triage-gate.js in Stop hook list (timeout 7s).",
        "COMMAND: commands/implement.md \u2014 NEW Step 8 'Integration hygiene checklist' \u2014 4-line printed checklist at end of cycle (endpoints have entry+exit+error logs, integration points logged with correlation ID, zero raw SQL/direct HTTP/mock data, log triage clean or acknowledged). 141\u2192154 lines.",
        "COMMAND: commands/redteam.md \u2014 NEW Step 7 'Log triage gate' \u2014 concrete scan commands (pytest/build/find/pip check), disposition protocol (Fixed/Deferred/Upstream/False positive), BLOCKS convergence on unacknowledged entries. 136\u2192155 lines.",
        "AGENT: agents/quality/reviewer.md \u2014 new 'Integration Hygiene' section in Review Checklist (7 checkboxes: specialist consulted before dropping below Engine layer, endpoint log points, integration correlation IDs, no raw SQL/HTTP/mock data, import-vs-manifest, schema migrations, silent swallows). 111\u2192121 lines.",
        "AGENT: agents/testing/testing-specialist.md \u2014 new 'Log Assertion Requirement (Tiers 2-3)' section. Every integration/E2E test for an operation with observability-mandated log points MUST assert on the structured log output (caplog.at_level + assert). Missing log assertions block sign-off. 167\u2192188 lines.",
        "SYNC MANIFEST: added rules/observability.md and rules/schema-migration.md to coc tier. NEW variant declarations: rules/observability.md (py: null, rs: null, rb: variants/rb/rules/observability.md), rules/schema-migration.md (py: null, rs: null, rb: variants/rb/rules/schema-migration.md). FIXED: rules/framework-first.md variant block now has explicit py: null and rb: null keys (were missing, now format-consistent).",
        "CLAUDE.md: Rule 3 title updated to 'Pre-existing failures, warnings, and notices MUST be fixed'. Rules Index row extended to list observability + schema-migration.",
        "METHOD: 5 iteration rounds of parallel cc-audit + redteam for Wave 1 content (13/14 prior findings closed, only carry-forward LOW remain), 2 rounds for Wave 2 descriptions (10/10 semantic activation tests pass after iter 2 \u2014 LLMProviderService refactor, custom auth gateway, UserRepository, OpenAI wrapper, FastAPI router, SQL migration, tool server exposing APIs as LLM tools, LoRA fine-tuning, feature store, RBAC policy). Key protocol lesson: structural CC-audit alone misses 'would this actually fire on a realistic todo' \u2014 every description wave MUST include the semantic activation test."
      ]
    },
    {
      "version": "2.6.2",
      "date": "2026-04-08",
      "summary": "Remove Rust leakage from artifacts that sync to USE templates. USE templates serve Python (py) / Python+Ruby binding consumers (rs) / Ruby (rb) developers \u2014 none of them write Rust. Rust is written ONLY in kailash-rs BUILD repo. Adds language-mapping prevention rule to sync-manifest header and creates BUILD-local Rust parity supplement in kailash-rs.",
      "changes": [
        "REVERT: skills/spec-compliance/SKILL.md \u2014 removed Rust syn::parse_file / #[deprecated] / pub use re-export examples added in 2.6.1 (Checks 1-4). The skill is now Python-only (ast.parse, pytest, grep) since it syncs to all USE templates which serve Python/Ruby developers, not Rust. Added a 'Cross-SDK note' callout pointing kailash-rs BUILD repo to its local rust-parity.md supplement.",
        "FIX: rules/testing.md (global) \u2014 removed `cargo test --list` line from \u00a7 Audit Mode Rules. Now Python-only (pytest --collect-only). Synced to kailash-py BUILD + kailash-coc-claude-py USE template.",
        "REWRITE: variants/rs/rules/testing.md \u2014 fully Python-flavored for kailash-coc-claude-rs USE template (Python and Ruby developers writing apps that consume kailash-rs through bindings). Title changed from 'Rust SDK + Python Bindings' to 'Python bindings to kailash-rs'. Removed `use kaizen_agents::wrapper_base` Rust syntax. All examples now Python pytest / Python module imports. Tier 2 rationale rephrased from 'FFI boundary' to 'binding boundary' (still about real bindings, no longer Rust-flavored).",
        "FIX: commands/redteam.md \u2014 replaced `pytest --collect-only` (Python) or `cargo test --list` (Rust) with `pytest --collect-only` (or your project's equivalent test enumeration command). Same for the testing-specialist agent description in \u00a7 Agent Teams.",
        "FIX: rules/deploy-hygiene.md Rule 7 \u2014 removed `cargo build --release` from the BLOCKED standalone build commands list. The list now reads `npm run build`, `docker build`, `vite build`, or any equivalent production build command \u2014 language-agnostic.",
        "NEW: kailash-rs/.claude/skills/spec-compliance/rust-parity.md \u2014 BUILD-local supplement (NOT synced from loom). Provides Rust equivalents for all 9 spec-compliance checks: syn::parse_file walker for signatures, struct field grep, #[deprecated] count, MOVE shim with pub use re-export, cargo test --list for new test coverage, async fn awaits for fake-stream detection, plus cargo build hygiene reminders (clippy, fmt, doc tests). Documents that loom never overwrites this file.",
        "MANIFEST: sync-manifest.yaml header \u2014 added 'LANGUAGE MAPPING' section explicitly documenting USE template language mapping (py\u2192Python, rs\u2192Python/Ruby binding consumers, rb\u2192Ruby) and the rule that Rust content belongs ONLY to kailash-rs BUILD repo (managed locally, not synced). Prevents future Rust leakage at the manifest-design level.",
        "AUDIT FIX: removed bogus nested kailash-rs/kailash-py/ directory created by an accidental cd during the leak fix workflow."
      ]
    },
    {
      "version": "2.6.1",
      "date": "2026-04-08",
      "summary": "Address 4 critique items on 2.6.0: trim deploy.md to 135L (was 151, over CC R5 150-line limit); add wrapup four-state classification (handles legacy prose deployment-config.md without frontmatter \u2014 kailash-py case); narrow deploy-hygiene.md path frontmatter to infrastructure-only (drops src/**, app/**, lib/** which would load the rule on every Python edit in 16K+ LOC trees); slim deploy-hygiene from 280\u2192138L by moving DO/DON'T examples to application-deployment skill; add Rust parity (syn::parse_file + grep) to spec-compliance Checks 1-4 alongside existing Python ast.parse examples.",
      "changes": [
        "TRIM: commands/deploy.md 151\u2192135L \u2014 moved per-step checklist guidance to rules/deploy-hygiene.md Rule 8 reference. Now within 150-line CC R5 limit.",
        "RESTRUCTURE: commands/wrapup.md \u00a7 Deploy State Check \u2014 replaced 2-branch logic (exists/missing) with 4-state classification: State A (frontmatter type: application \u2192 drift check), State B (frontmatter type: sdk \u2192 skip with note, /release is the right command), State C (file exists but lacks YAML frontmatter \u2014 legacy prose-only, flag for migration via /deploy --onboard, do NOT block wrapup), State D (file missing \u2192 flag for onboarding). Fixes false-positive on kailash-py which has prose-only deployment-config.md.",
        "SLIM: rules/deploy-hygiene.md 280\u2192138L \u2014 moved verbose DO/DON'T code examples to skills/10-deployment-git/application-deployment.md. Rule retains principles, 6-level table, brief one-line MUST rules, MUST NOT, exceptions. Cross-references skill for details.",
        "NARROW: rules/deploy-hygiene.md paths frontmatter \u2014 dropped src/**, frontend/**, app/**, lib/** (would load on every code edit in 16K+ LOC src/ trees, causing context bloat \u2014 same failure mode as recent commits e3b06a4c/5b4fa4cd were fixing). Now scoped to infrastructure files only: Dockerfile, deploy/**, k8s/**, kubernetes/**, helm/**, .github/workflows/**, fly.toml, vercel.json, app.yaml, serverless.yml, wrangler.toml, Procfile, next.config.*, vite.config.*, package.json, deploy/deployment-config.md. Wrapup gate is the safety net for src/ commits that don't touch infrastructure.",
        "ADD EXCEPTION: rules/deploy-hygiene.md \u2014 new exception for legacy prose-only deployment-config.md (matches wrapup State C).",
        "RUST PARITY: skills/spec-compliance/SKILL.md Checks 1-4 now include Rust-equivalent examples alongside Python \u2014 Check 1 (signature verification: syn::parse_file via cargo +nightly -Zscript script + grep fallback), Check 2 (struct field presence via grep + sed), Check 3 (#[deprecated]/#[derive] attribute application count), Check 4 (MOVE shim: pub use re-export with #[deprecated], type alias deprecation). Cross-SDK parity audits of kailash-rs now have the same rigor as kailash-py."
      ]
    },
    {
      "version": "2.6.0",
      "date": "2026-04-08",
      "summary": "CRITICAL: /redteam spec compliance + /deploy hygiene rewrite. Two systemic failure modes fixed: (1) /redteam was checking file existence instead of spec semantics; (2) /deploy was a 9-line stub that redirected everything to /release while downstream apps had no working deploy. Added 6-level deploy failure model, 10-step deploy checklist, Vite/Docker/Next.js patterns, and BUILD-vs-USE artifact distinction.",
      "changes": [
        "NEW SKILL: skills/spec-compliance/SKILL.md \u2014 9-check verification protocol (class signatures, fields, decorators, MOVE shims, new test coverage, security tests, import migration, fake-implementation patterns, self-report trust ban). Replaces file-existence checking in /redteam Step 1.",
        "REWRITE: commands/redteam.md \u2014 Step 1 now references spec-compliance skill, enumerates ALL spec sources (briefs/, 01-analysis/, 02-plans/, todos/completed/), Step 4 forbids trusting .test-results, new convergence criterion: assertion tables must show literal grep/AST commands (rows saying 'exists: yes' BLOCKED).",
        "UPDATE: rules/testing.md \u2014 added \u00a7 Audit Mode Rules (re-derive coverage from scratch, verify new modules have new tests, verify security threats have tests). Path frontmatter expanded to include .spec-coverage*, .test-results*, 02-plans/**, 04-validate/**.",
        "UPDATE: agents/analysis/analyst.md \u2014 new \u00a7 /redteam Step 1 Ownership directing analyst to load spec-compliance skill and produce .spec-coverage-v2.md.",
        "UPDATE: agents/testing/testing-specialist.md \u2014 new \u00a7 /redteam Step 4 Ownership \u2014 Audit Mode for re-derived test coverage verification.",
        "UPDATE: variants/rs/rules/testing.md and variants/rb/rules/testing.md \u2014 same audit-mode additions for binding-dev and Ruby USE templates.",
        "REWRITE: commands/deploy.md (9L \u2192 151L) \u2014 was a stub that redirected to /release for every repo. Now config-driven with onboard/execute/check modes, prints a 10-step checklist into the response, refuses to claim success on partial completion. Includes external user-visible verification (curl + bundle hash compare), traffic check, cache invalidation, smoke test.",
        "NEW RULE: rules/deploy-hygiene.md \u2014 '6-level failure model' for the recurring 'committed but not deployed' anti-pattern. Path-scoped to src/**, frontend/**, Dockerfile, deploy/**, k8s/**, fly.toml, vercel.json, etc. Rules: verify deploy state before stacking commits; /deploy after committing production code; verify what users see (not deploy command exit code); /wrapup blocks on undeployed prod code; use declared build command (never improvise); never run build commands outside /deploy; print deploy checklist; Dockerfile MUST rebuild from source (no COPY dist/); update deploy state file only after user-visible check passes.",
        "NEW SKILL: skills/10-deployment-git/application-deployment.md \u2014 onboarding flow + deployment-config.md schema (production_paths, deploy_command, deploy_check_command, traffic_check_command, user_visible_check, cache_invalidation_command, smoke_test_command, gates). Comprehensive frontend deployment patterns: Vite static SPA, Vite + Docker, Next.js standalone (Docker SSR), Next.js static export, Next.js Vercel/serverless. Bundle hash extraction reference table for Vite, Next.js, CRA, SvelteKit, Astro, Remix. Three Dockerfile COPY traps to refuse.",
        "UPDATE: commands/wrapup.md \u2014 \u00a7 Deploy State Check runs FIRST (before writing notes). If deploy/deployment-config.md declares type: application and drift is detected on production paths, wrapup STOPS and requires either /deploy or explicit deferral with documented reason. Closes the 'did the agent deploy?' ambiguity at session boundary.",
        "MANIFEST: sync-manifest.yaml \u2014 added build_exclude: section listing commands/deploy.md, rules/deploy-hygiene.md, skills/10-deployment-git/application-deployment.md. /sync-to-build MUST skip these (BUILD repos release packages via /release; deploy artifacts are USE-template-only).",
        "UPDATE: commands/sync-to-build.md \u2014 Step 3 'Compute expected state' now respects build_exclude:.",
        "AUDIT FIX: removed commands/deploy.md, rules/deploy-hygiene.md, skills/10-deployment-git/application-deployment.md from kailash-py and kailash-rs BUILD repos (they were incorrectly synced earlier in this session \u2014 BUILD repos don't deploy applications)."
      ]
    },
    {
      "version": "2.5.3",
      "date": "2026-04-08",
      "summary": "Fix kailash-rs BUILD repo rule scoping: env-models.md and patterns.md now have Rust-appropriate paths. USE template (kailash-coc-claude-rs) correctly keeps global Python versions \u2014 USE template users write Python/Ruby, not Rust.",
      "changes": [
        "FIX: kailash-rs/.claude/rules/env-models.md \u2014 Rust paths (**/*.rs, **/*.toml, .env*) and dotenvy examples (BUILD repo local)",
        "FIX: kailash-rs/.claude/rules/patterns.md \u2014 added **/*.rs to paths (BUILD repo local)",
        "CLARIFIED: rs variant overlay is for USE template (Python/Ruby binding users), not BUILD repo. No rs-variant env-models.md needed \u2014 global Python version is correct for USE template."
      ]
    },
    {
      "version": "2.5.2",
      "date": "2026-04-08",
      "summary": "REVERT 2.5.1: restore `paths:` frontmatter on 12 rule files. Previous diagnosis was wrong \u2014 Claude Code path-scoped rules do NOT re-inject per tool call; they load once on first matching read and stay sticky. Stripping `paths:` moved 12 rules into the always-on baseline, INCREASING the session-start cost (~5500 tokens per stripped large rule) instead of decreasing it. User reported 89K first-hit baseline as a result.",
      "evidence": [
        "Subprocess test: 5 reads of same .py file vs 5 reads of different .py files differ by ~80 tokens \u2014 rules do not accumulate per call",
        "Subprocess test: 11KB rule WITH paths frontmatter adds 0 to session-start baseline; same rule WITHOUT paths adds +5482 tokens",
        "Claude Code docs (code.claude.com/docs/en/memory): 'Path-scoped rules trigger when Claude reads files matching the pattern, not on every tool use'",
        "Confirmed via claude-code-guide agent lookup of official memory.md documentation"
      ],
      "changes": [
        "RESTORED: rules/agent-reasoning.md \u2014 paths: **/kaizen/**, **/*agent*, **/agents/**",
        "RESTORED: rules/artifact-flow.md \u2014 paths: .claude/**, sync-manifest.yaml, **/VERSION, *.md",
        "RESTORED: rules/env-models.md \u2014 paths: **/*.py, **/*.ts, **/*.js, .env*",
        "RESTORED: rules/framework-first.md \u2014 paths: **/*.py, **/*.rs",
        "RESTORED: rules/patterns.md \u2014 paths: **/*.py, **/*.ts, **/*.js",
        "RESTORED: rules/python-environment.md \u2014 paths: **/*.py, pyproject.toml, conftest.py, tests/**",
        "RESTORED: rules/terrene-naming.md \u2014 paths: **/*.md, **/README*, **/LICENSE*, **/CLAUDE.md",
        "RESTORED: rules/testing.md \u2014 paths: tests/**, **/*test*, **/*spec*, conftest.py",
        "RESTORED: variants/rs/rules/build-speed.md \u2014 paths: **/*.rs, **/*.toml, **/Cargo.lock",
        "RESTORED: variants/rs/rules/framework-first.md \u2014 paths: **/*.rs",
        "RESTORED: variants/rs/rules/patterns.md \u2014 paths: **/*.py, **/*.ts, **/*.js",
        "RESTORED: variants/rs/rules/testing.md \u2014 paths: tests/**, **/*test*, **/*spec*, conftest.py",
        "NOTE: Wide patterns are restored as-is (per user direction). Narrowing is a separate refinement task \u2014 wide is not a problem because rules inject once per session, not per call."
      ]
    },
    {
      "version": "2.5.1",
      "date": "2026-04-08",
      "summary": "[REVERTED in 2.5.2] fix(rules): strip broad path matchers \u2014 based on incorrect diagnosis. See 2.5.2 for full evidence and revert.",
      "changes": [
        "UPDATED: rules/patterns.md \u2014 removed `paths: **/*.{py,ts,js}` frontmatter (was re-injecting 108 lines per code-file read)",
        "UPDATED: rules/env-models.md \u2014 removed broad `paths:` frontmatter",
        "UPDATED: rules/framework-first.md \u2014 removed broad `paths:` frontmatter",
        "UPDATED: rules/python-environment.md \u2014 removed broad `paths:` frontmatter",
        "UPDATED: rules/terrene-naming.md \u2014 removed `**/*.md` matcher (fired on every markdown read)",
        "UPDATED: rules/artifact-flow.md \u2014 removed `.claude/**` + `*.md` matcher",
        "UPDATED: rules/agent-reasoning.md \u2014 removed `**/agents/**` matcher (was re-injecting 266-line rule per agent-file read)",
        "UPDATED: rules/testing.md \u2014 removed `**/*test*` matcher",
        "UPDATED: variants/rs/rules/patterns.md \u2014 removed broad `paths:` frontmatter",
        "UPDATED: variants/rs/rules/framework-first.md \u2014 removed `**/*.rs` matcher",
        "UPDATED: variants/rs/rules/build-speed.md \u2014 removed broad `paths:` frontmatter",
        "UPDATED: variants/rs/rules/testing.md \u2014 removed `**/*test*` matcher",
        "UPDATED: scripts/hooks/session-start.js \u2014 session notes now inject as pointer lines only (was dumping ~12KB of full note content per session)",
        "UPDATED: scripts/hooks/user-prompt-rules-reminder.js \u2014 removed zero-tolerance reminder block (duplicated always-on rule) and keyword-triggered LLM/e2e reminders (rules cover these)"
      ]
    },
    {
      "version": "2.5.0",
      "date": "2026-04-07",
      "summary": "Gate 1 py v2.6.0: ML cardinality guard + EDA + ExperimentTracker (py-variant), Kaizen multi-agent exports (py-variant). RS alignment skills (rs-variant). Option C doc consistency.",
      "changes": [
        "VARIANT-PY: agents/frameworks/ml-specialist.md \u2014 PreprocessingPipeline cardinality guard, ModelVisualizer EDA methods, ExperimentTracker.create() factory",
        "VARIANT-PY: agents/frameworks/kaizen-specialist.md \u2014 multi-agent pattern top-level exports, agents/coordination removal",
        "VARIANT-RS: skills/36-kailash-rs-alignment/ \u2014 6 new skills (crate structure, ML crate, PyO3 bindings, transport parity, cross-SDK features)",
        "DOCS: kailash-ml-packaging-architecture.md \u2014 full Option C consistency pass (kailash-ml-native \u2192 kailash-enterprise throughout)"
      ]
    },
    {
      "version": "2.4.0",
      "date": "2026-04-06",
      "summary": "Gate 1 py v2.5.1: PACT clearance FSM (global) + align agents + ML correlation (py-variant)",
      "changes": [
        "GLOBAL: agents/frameworks/pact-specialist.md \u2014 clearance lifecycle FSM (SUSPENDED state, transition_clearance, reinstatement path)",
        "GLOBAL: skills/29-pact/pact-governance-engine.md \u2014 VettingStatus FSM diagram, transition_clearance() API, revoke preserves record",
        "VARIANT-PY: agents/frameworks/align-specialist.md \u2014 4 Kaizen agents (BaseAgent+Signature), OnPrem/Air-Gapped deployment section",
        "VARIANT-PY: agents/frameworks/ml-specialist.md \u2014 DataExplorer correlation robustness (pairwise-complete, _sanitize_float, None semantics)"
      ]
    },
    {
      "version": "2.2.0",
      "date": "2026-04-05",
      "summary": "Gate 1 rs v3.9.0 codify: ml-specialist, align-specialist, binding skills, framework-first rule (all rs-variant)",
      "changes": [
        "VARIANT-RS: agents/frameworks/ml-specialist.md \u2014 Rust ML crate specialist (type-state traits, DynEstimator, Pipeline, rayon)",
        "VARIANT-RS: agents/frameworks/align-specialist.md \u2014 Rust align-serving specialist (InferenceEngine, ServingBackend, LoRA hot-swap)",
        "VARIANT-RS: skills/06-python-bindings/SKILL.md \u2014 added align_serving, pact, orchestration binding modules",
        "VARIANT-RS: skills/28-ruby-bindings/SKILL.md \u2014 added audit_log, event_bus, telemetry binding modules",
        "VARIANT-RS: rules/framework-first.md \u2014 ML/Align rows with actual Rust crate names, Rust code examples",
        "CC-AUDIT: 14 BUILD paths stripped, 9 dangling agent refs fixed, knowledge dumps extracted, Python\u2192Rust examples"
      ]
    },
    {
      "version": "2.1.0",
      "date": "2026-04-05",
      "summary": "Issue #8 long tail: 16 skill files tightened, 3 duplicates deleted, rb CLAUDE.md created \u2014 13K lines removed",
      "changes": [
        "TIGHTENED: 16 skill files from 10,253 \u2192 3,756 lines (63% avg reduction, 0 pattern loss)",
        "DELETED: variants/rs/skills/01-core/ (stale duplicate of 01-core-sdk/, 13 files)",
        "DELETED: 2 identical variant files (rs production-testing, py async-pythoncode-patterns \u2014 no variant value)",
        "DELETED: variants/py/skills/03-nexus/nexus-workflow-registration.md (identical to global)",
        "NEW: kailash-coc-claude-rb/CLAUDE.md \u2014 Ruby template CLAUDE.md with Magnus FFI patterns",
        "RESULT: >1000L files: 5\u21920, >500L files: 32\u21920, net 13,016 lines removed"
      ]
    },
    {
      "version": "2.0.0",
      "date": "2026-04-05",
      "summary": "Skill tightening: 28 files compressed 78%, red-teamed, 0 pattern loss \u2014 addresses #8",
      "changes": [
        "TIGHTENED: 28 skill files from 27,886 \u2192 6,247 lines (78% reduction, net 23,957 lines removed)",
        "RED-TEAM: 4 parallel agents audited all 28 files against git originals \u2014 Round 1: 7 FAIL, Round 2: 0 FAIL",
        "RESTORED: 7 files had missing patterns restored (Asset Precaching, AI Chat, MethodChannel, EATP best practices, Config Validation, .env template, Auth header, 8 test patterns)",
        "RESULT: >1000L files: 8\u21920, >500L files: 34\u21926, >250L files: 130\u2192113, 0 pattern loss verified"
      ]
    },
    {
      "version": "1.9.0",
      "date": "2026-04-05",
      "summary": "Gate 1 py v2.5.1 codify: DataExplorer P1 (ydata-profiling parity, async, alerts, HTML reports)",
      "changes": [
        "VARIANT-PY: agents/frameworks/ml-specialist.md \u2014 DataExplorer promoted P2\u2192P1, async API, alerts system, HTML reports, security guards (NaN/Inf, XSS-safe)"
      ]
    },
    {
      "version": "1.8.0",
      "date": "2026-04-05",
      "summary": "Gate 1 py v2.5.1 codify: framework specialist descriptions \u2014 package-name-first for routing",
      "changes": [
        "GLOBAL: 7 framework specialist descriptions rewritten \u2014 lead with kailash-{package}, dense keyword triggers",
        "VARIANT-PY: infrastructure-specialist description \u2014 removed (Level 0/1/2), added connections/stores keywords",
        "Agents affected: ml, align, dataflow, nexus, kaizen, mcp, pact specialists (global) + infrastructure (py variant)"
      ]
    },
    {
      "version": "1.7.0",
      "date": "2026-04-05",
      "summary": "Gate 1 py v2.5.0 codify: SSRF prevention, Nexus transports, ML protocol consolidation",
      "changes": [
        "GLOBAL: skills/18-security-patterns/SKILL.md \u2014 SSRF prevention (DNS-pinned delivery, IPv4-mapped IPv6) + Error Message Security",
        "GLOBAL: skills/02-dataflow/dataflow-ml-integration.md \u2014 kailash_ml_protocols\u2192kailash_ml.types import fix",
        "VARIANT-PY: agents/frameworks/nexus-specialist.md \u2014 Transport Layer section (4 transports, ResponseCache, security patterns)",
        "VARIANT-PY: agents/frameworks/ml-specialist.md \u2014 kailash_ml.types import paths",
        "VARIANT-PY: skills/02-dataflow/dataflow-ml-integration.md \u2014 Python import paths, BUILD paths stripped",
        "VARIANT-PY: skills/04-kaizen/kaizen-ml-integration.md \u2014 NEW: ML+Kaizen integration patterns",
        "SKIPPED: skills/project/ml-quick-reference.md (project-specific, BUILD paths)",
        "RED-TEAM: Reclassified nexus-specialist global\u2192variant-py (Python module paths), stripped 5 BUILD paths, fixed FastAPI regression in Rule 7"
      ]
    },
    {
      "version": "1.6.0",
      "date": "2026-04-05",
      "summary": "TSG specialist artifacts + stale agent cleanup + dependency rules + /wrapup red team",
      "changes": [
        "NEW: 3 specialist agents (mcp-platform 236L, ml 279L, align 259L)",
        "NEW: skills/34-kailash-ml/ (8 ML lifecycle skills)",
        "NEW: skills/35-kailash-align/ (5 LLM alignment skills)",
        "NEW: 7 MCP platform skills in skills/05-kailash-mcp/",
        "NEW: rules/dependencies.md \u2014 latest versions, no transitive caps, own the stack",
        "UPDATED: commands/wrapup.md \u2014 mandatory red team verification with inline evidence",
        "UPDATED: CLAUDE.md \u2014 agent count corrected to 29, platform table with ML/Align",
        "UPDATED: sync-manifest.yaml \u2014 3 new agents, 2 new skill dirs, dependencies.md",
        "UPDATED: rules/agents.md \u2014 delegation table expanded to 8 specialists",
        "FIXED: 21 stale agent files from v1.4.0 ontology consolidation (root duplicates, absorbed agents, migrated-to-skills)",
        "DELETED: agents/standards/ directory (4 expert agents migrated to skills/co-reference/ in v1.4.0)",
        "FIXED: /cc-audit convergence \u2014 2 CRITICAL + 6 HIGH resolved (agent count, delegation routing, SKILL.md descriptions, BUILD paths)"
      ]
    },
    {
      "version": "1.5.0",
      "date": "2026-04-04",
      "summary": "Rule Why: rationale coverage + cross-ref cleanup + sync enforcement + security hardening",
      "changes": [
        "ADDED: **Why:** rationale to all 44 rule files \u2014 27 global (153 lines), 5 rs variant (28 lines), 12 rb variant (90 lines)",
        "FIXED: 2 broken ../nodes/SKILL.md cross-refs in rs variant skills (06-cheatsheets, 07-development-guides)",
        "FIXED: 9 broken dataflow-specialist.md file path links in skill sub-files (pre-existing from v1.4.0 agent reorg)",
        "FIXED: 6 restatement Why: lines in trust-plane-security.md (global + rb) \u2014 now state consequences, not descriptions",
        "FIXED: Command injection in version-utils.js fetchUpstreamVersion() \u2014 execSync\u2192execFileSync (pre-existing)",
        "FIXED: Shell injection in session-start.js checkSdkPinFreshness() \u2014 execSync\u2192execFileSync",
        "FIXED: NaN bypass on COC sync marker timestamp \u2014 added isFinite check",
        "ADDED: /sync step 10 \u2014 mandatory `uv sync` after SDK pin updates (never pip install)",
        "ADDED: session-start.js checkSdkPinFreshness() \u2014 warns on stale kailash dependency pins",
        "CLEANED: 8 workspaces/ provenance refs in global skills (kaizen L3, BYOK, EATP store backends)"
      ]
    },
    {
      "version": "1.4.0",
      "date": "2026-04-03",
      "summary": "Agent/skill ontology reorganization + token optimization across all artifacts",
      "changes": [
        "RESTRUCTURED: agents/ into 7 subdirectories (analysis, frameworks, frontend, implementation, management, quality, release, testing)",
        "CONSOLIDATED: 33\u219226 agents (analyst, reviewer, release-specialist, repo-ops, react-specialist, uiux-designer, testing-specialist absorb 7 deleted agents)",
        "MIGRATED: 7 agents\u2192skills (care/co/coc/eatp-expert, sdk-navigator, framework-advisor \u2192 skills/co-reference/)",
        "DELETED: agents/standards/ directory, 27-care-reference skill",
        "TIGHTENED: 8 agents from avg 266\u219277 lines (71% reduction, 1505 lines saved)",
        "TIGHTENED: 12 SKILL.md files from avg 280\u2192~180 lines (35% reduction)",
        "TIGHTENED: py variant rules deleted (globals apply), rs/rb variants tightened (-5200 lines)",
        "CORRECTED: framework-first.md + decide-framework.md \u2014 3-layer\u21924-layer hierarchy (Specs\u2192Primitives\u2192Engines\u2192Entrypoints)",
        "NEW: skills/co-reference/ \u2014 6 spec files (CARE, EATP, CO, COC, governance-layer-thesis, behavioral-guidelines)",
        "UPDATED: sync-manifest.yaml \u2014 consolidated agent paths, exclusions, tier membership",
        "UPDATED: CLAUDE.md \u2014 agent index matches actual directory structure",
        "CLEANED: ~200 files updated to remove stale refs to deleted/consolidated agents"
      ]
    },
    {
      "version": "1.3.0",
      "date": "2026-04-03",
      "summary": "Token optimization: 90% reduction in always-loaded rules via path scoping, compression, and merges",
      "changes": [
        "MERGED: rules/no-stubs.md into rules/zero-tolerance.md (Rule 2)",
        "MERGED: rules/branch-protection.md into rules/git.md",
        "SCOPED: 19 rules now path-scoped (only load when editing matching files)",
        "TIGHTENED: All 28 rules \u2014 enforcement sections, cross-refs, motivational text removed",
        "TIGHTENED: pact-governance, trust-plane-security, infrastructure-sql, connection-pool, cc-artifacts \u2014 30-45% smaller",
        "TIGHTENED: testing, patterns, agents, deployment, python-environment, documentation \u2014 40-70% smaller",
        "UPDATED: agents.md \u2014 gate-level review (not per-edit), quality gates tied to COC phases",
        "UPDATED: communication.md \u2014 restored decision-framing examples",
        "UPDATED: framework-first.md \u2014 added paths: scoping, restored detection patterns",
        "UPDATED: sync-manifest.yaml \u2014 removed stale branch-protection.md and no-stubs.md refs",
        "FIXED: 19 files with stale no-stubs.md/branch-protection.md references across variants, guides, agents, commands",
        "JOURNAL: 0049-DISCOVERY \u2014 ablation experiment (Claude + MiniMax clean-room testing, 4-tier variant architecture)"
      ]
    },
    {
      "version": "1.2.0",
      "date": "2026-04-01",
      "summary": "Spec coverage gate, frontend mock detection, VERSION auto-creation, stale ref cleanup",
      "changes": [
        "NEW: commands/redteam.md step 1 \u2014 spec coverage audit with .spec-coverage artifact",
        "NEW: commands/implement.md step 2 \u2014 context anchoring before each todo",
        "UPDATED: commands/todos.md \u2014 build/wire separation, architecture plans contractual",
        "UPDATED: commands/codify.md \u2014 conditional proposals (BUILD only, downstream skips)",
        "UPDATED: rules/no-stubs.md \u2014 frontend mock data = stub (JS/TS MOCK_*/FAKE_*/DUMMY_*)",
        "UPDATED: rules/artifact-flow.md \u2014 downstream repos do not propose upstream (rule 2a)",
        "UPDATED: scripts/hooks/validate-workflow.js \u2014 checkFrontendMockData() enforcement",
        "UPDATED: scripts/hooks/lib/version-utils.js \u2014 auto-create VERSION for repos without one",
        "FIXED: 8 skill/variant files with stale kailash[extras] bracket notation",
        "SYNCED: kailash-coc-claude-rb template (7 spec-coverage-gate files)"
      ]
    },
    {
      "version": "1.1.0",
      "date": "2026-03-30",
      "summary": "Engine-first framework rule, layer preference tables, Delegate-vs-BaseAgent decision guide",
      "changes": [
        "NEW: rules/framework-first.md \u2014 Engine-over-Primitives 4-layer hierarchy (Specs \u2192 Primitives \u2192 Engines \u2192 Entrypoints)",
        "NEW: skills/04-kaizen/kaizen-delegate-vs-baseagent.md \u2014 Delegate vs BaseAgent decision guide",
        "UPDATED: agents/framework-advisor.md \u2014 added Step 2b layer selection table",
        "UPDATED: agents/frameworks/{dataflow,nexus}-specialist.md \u2014 layer preference sections",
        "UPDATED: skills/02-dataflow/SKILL.md, 04-kaizen/SKILL.md \u2014 engine-first quick starts",
        "UPDATED: skills/13-architecture-decisions/decide-framework.md \u2014 within-framework layer selection"
      ]
    },
    {
      "version": "1.0.0",
      "date": "2026-03-29",
      "summary": "Variant architecture with controlled artifact flow",
      "changes": [
        "Variant overlay system (sync-manifest.yaml, variants/py/, variants/rs/)",
        "Two-gate /sync: Gate 1 (review + classify) + Gate 2 (adapt + distribute)",
        "Proposal-based /codify flow (no direct BUILD-to-template sync)",
        "92 variant files populated (44 py, 48 rs)",
        "Management agents absorbed from ~/repos",
        "Authority chain: atelier/ owns CC+CO, loom/ owns COC",
        "Intelligent content adaptation in coc-sync (not file copy)"
      ]
    }
  ],
  "updated": "2026-04-14"
}

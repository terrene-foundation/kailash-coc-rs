# Sweep — 2026-08-04 (management decision report)

Paired with the Rounds 5–7 adversarial redteam on PR #80. Repo class: `coc-use-template`
(`.claude/VERSION::type`), so findings originate proposals rather than land durably here.

Every finding carries CATEGORY (BUG / INVEST-NOW / INCREMENTAL per
`product-completion-first.md`) and SEVERITY (ranks only — never gates fix-vs-defer).

---

## 1. Completion status

| Deliverable                                    | Status                          | Durable receipt                                        |
| ---------------------------------------------- | ------------------------------- | ------------------------------------------------------ |
| MUST-4 fence — rule, adapters, prose, fixtures | COMPLETE on branch, NOT ON MAIN | PR #80 head `b083da6`, `validate=SUCCESS`              |
| Rounds 5–7 redteam (3 lanes × 3 rounds)        | COMPLETE                        | PR #80 comment `#issuecomment-5175210552`              |
| Round 5–7 findings fixed                       | COMPLETE                        | 7 commits `403fa30..b083da6`                           |
| Fence instrument suite                         | COMPLETE                        | 47/47, mutation-validated per case                     |
| Helper instrument suite (new)                  | COMPLETE                        | 9/9, `upflow-self-repo-helpers/`                       |
| journal-reserve coordination gate (new)        | COMPLETE                        | 6/6, `journal-reserve-coordination-gate/`              |
| Residuals filed                                | COMPLETE                        | #82, #83, #84, #85                                     |
| **Proposal entries for R5–R7 fixes**           | **NOT STARTED**                 | `latest.yaml` — 51 entries, cluster 33–39 fenced STALE |

**Product visibility: NOT ACHIEVED.** 29 commits sit ahead of `origin/main`; nothing has
landed. The walking skeleton does not stand for any consumer.

**The load-bearing gap is the proposal, not the PR.** `.claude/hooks/lib/**` is a synced glob,
so every fix in these 7 commits is **Class-A non-durable** — the next `/sync-to-use` rebuilds
those files from loom and erases them. The `latest.yaml` marker already fences entries 33–39
as STALE and names re-derivation as "NEXT SESSION'S FIRST TASK"; that debt has since grown by
7 more commits, including two security fixes (the ADO org/host identity comparison, and the
triangular push-remote resolution) that no entry reflects.

## 2. ETA to completion

Basis: remaining BUG + INVEST-NOW work below, at 1 autonomous cycle per item unless noted.

| Item                                            | Cycles    |
| ----------------------------------------------- | --------- |
| Proposal re-derivation (cluster 33–39 + R5–R7)  | 1–2       |
| PR #80 human review + merge                     | 0 (gated) |
| #83 refusal-operand sanitization sweep          | 1–2       |
| #84 roster-read swallow                         | 1         |
| #82 ADO identity triple→quad                    | 2–3       |
| **Total to a complete, durable, visible fence** | **5–8**   |

Two of those cycles are not agent-resolvable: PR #80 needs a human reviewer (structural gate,
`autonomous-execution.md` § Structural vs Execution Gates), and loom Gate-1 must ingest the
proposal before anything cascades.

## 3. Prioritized immediate queue (BUG + INVEST-NOW)

Value-ranked. Anchors are user-anchored per `value-prioritization.md` MUST-2.

**1 — Proposal re-derivation for the R5–R7 fixes · INVEST-NOW · HIGH**
Anchor: `latest.yaml` STALE marker, "NEXT SESSION'S FIRST TASK"; `artifact-flow.md`
§ Distribution-Durability Invariants (Class-A).
Implication: until this lands, ingesting the cluster as written would distribute the
**defective** fence to every sibling template AND rebuild away the local fixes. Not doing it
leaves the entire cycle's security work non-durable.

**2 — #83 refusal-string operand sanitization · BUG · MED**
Anchor: the module's own docstring — "the argument for one is the argument for all".
Implication: refusal reasons reach PR bodies and journals via Step-7c; DEL, C1 incl. 8-bit CSI,
U+2028/29 and bidi controls pass `JSON.stringify` unescaped at ~20 sites. Log-forging is live,
not latent. Includes a remote-controlled operand (the API response body) and a
possibly-credential-bearing one (transport `err.message`).

**3 — #84 roster-read swallow · BUG · MED**
Anchor: the function's own contract — the log read one line above throws precisely because
"a 0 on an unreadable log would hand out an already-reserved slot".
Implication: an unreadable roster reaches that exact outcome by swallowing. Breaks journal slot
monotonicity, which `/codify`'s mandatory journal receipt depends on.

**4 — #82 ADO cross-collection · BUG · MED**
Anchor: `security.md` § Enforcement-Surface Parity — a shipped fail-closed control with a known
bypass.
Implication: two collections derive an identical identity, so the fence authorizes across them.
Unfixable by any comparison key; needs the ADO identity widened triple→quad.

**5 — PR #80 review + merge · INVEST-NOW · HIGH (human-gated)**
Anchor: `coc-sync-landing.md` — uncommitted/unlanded COC work vanishes at the next boundary.
Implication: 29 commits invisible to every consumer until merged.

## 4. Deferred-quality backlog

**Sweep-N result: the backlog is EMPTY, and the tracking surface does not exist in this repo.**

- `gh issue list --label deferred-quality` returns nothing.
- `gh label list` shows no `deferred-quality` label (only bug / documentation / duplicate /
  enhancement / good first issue / help wanted / invalid / question / wontfix).
- No `.github/ISSUE_TEMPLATE/deferred-quality.md`.

So no item is due a "still wanted?" revisit this cycle, and **no INCREMENTAL item can currently
be deferred compliantly** — `product-completion-first.md` MUST-2 requires the four conditions
carried on a `deferred-quality`-labelled issue, and neither the label nor the template is
present. That directly affects the #85 decision below.

## 5. Decision points

Judgment-bucket calls. Surfaced, not self-decided (`product-completion-first.md` MUST-4).

### D1 — #85 (host guard admits C0/DEL): BUG or INCREMENTAL?

Currently filed `bug`. I classified it INCREMENTAL in triage but filed it under the only label
available.

- **Keep as BUG** — Pro: no reclassification churn; the module's own standard says
  safe-by-accident is not the bar, and all three prior host defects started exactly there.
  Con: it is measurably unreachable (no fetchable remote carries a control-byte authority), so
  it competes for queue position against three live defects.
- **Reclassify INCREMENTAL + defer** — Pro: honest about reachability; keeps the BUG queue to
  things that actually bite. Con: **not currently possible compliantly** — the
  `deferred-quality` label and template do not exist, so deferring it today would be silent
  deferral (MUST-2, BLOCKED).

**Recommendation: keep as BUG for now, at the bottom of the queue.** Not because it is
mis-classified — it is genuinely INCREMENTAL — but because the compliant defer path does not
exist yet, and mislabelling it `wontfix` or closing it would be worse than carrying it.

### D2 — #74 disposition (classification corrected, issue still open)

PR #80 states it "corrects the classification of #74": the guard is correct, its degraded-mode
block sits behind the coordination opt-in gate, and the harness drove both lanes at a
coordination-OFF `cwd`. The `npm test` exit 1 was real; the diagnosis was not.

- **Close with the correction as the closing comment** — Pro: the issue asserts a fail-open that
  does not exist; leaving it open misrepresents the guard to every reader. Con: closing an issue
  whose originating symptom was real can read as dismissal.
- **Leave open, retitled to the real finding (a fixture-precondition defect)** — Pro: preserves
  the genuine sub-finding. Con: that sub-finding was already fixed on this branch.

**Recommendation: close it, with the correction quoted and the commit referenced**
(`git.md` § Discipline requires a code reference on any closure).

### D3 — Proposal re-derivation timing: before or after PR #80 merges?

- **Before merge** — Pro: the STALE marker names it as the next task; the debt grows with every
  commit. Con: if review forces changes, the entries need re-deriving again.
- **After merge** — Pro: derives once, against a settled diff. Con: leaves a fenced,
  known-defective cluster sitting in `latest.yaml` across a session boundary, and the marker's
  own warning is that ingesting it as written distributes a defective security fence.

**Recommendation: after merge, marker left in place.** The fence is what makes waiting safe —
Gate-1 cannot ingest the cluster while it stands. Re-deriving twice is pure waste.

## 6. Recommendation

1. **Hold PR #80 for human review.** No known-open live defect; the hold is that two of the last
   three rounds found regressions introduced by the previous round's fixes, and the most serious
   was invisible to every instrument until a lane built a new one.
2. **On merge: re-derive the proposal cluster first** (D3), before any new work. Until then this
   cycle's security fixes are non-durable by construction.
3. **Then the BUG queue in order: #83 → #84 → #82.** #83 first — it is live, it reaches a
   published surface, and the fix is mechanical (one shared helper already exported).
4. **Ratify D1 and D2** — both are one-line dispositions I should not self-decide.
5. **Consider standing up the `deferred-quality` surface** (label + issue template). Without it
   no INCREMENTAL item can be deferred compliantly, which is why #85 is parked in the BUG queue
   under a classification I do not actually believe.

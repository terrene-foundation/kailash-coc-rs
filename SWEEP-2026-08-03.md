# Sweep — 2026-08-03 (management decision report)

Paired with `/sync-from-downstream` (downstream upflow inbox ingest). Repo class:
`coc-use-template`, verified — `.claude/VERSION::repo` = `terrene-foundation/kailash-coc-rs`
matches `git remote get-url origin`.

Every finding carries CATEGORY (BUG / INVEST-NOW / INCREMENTAL per
`product-completion-first.md`) and SEVERITY (ranks only — never gates fix-vs-defer).

---

## 1. Completion status

| Deliverable                                 | Status                      | Durable receipt                                              |
| ------------------------------------------- | --------------------------- | ------------------------------------------------------------ |
| Downstream inbox ingest (PR #77, 7 entries) | COMPLETE                    | `inbox/2026-08-02-…yaml::ingest_disposition.status: RELAYED` |
| Route-A originations (#74, #75, #76)        | COMPLETE                    | `latest.yaml` entries 30–32, `origin: use-template`          |
| Proposal manifest                           | 32 entries `pending_review` | `latest.yaml` — parses, `changes=32`                         |
| Sweeps 1–10                                 | COMPLETE                    | this report                                                  |

Manifest grew 22 → 32: **7 relayed downstream** + **3 Route-A template-origin**.

## 2. ETA to completion

**Not agent-resolvable — the queue is BLOCKED on loom, not on capacity.** All 32 entries
are `pending_review` awaiting loom Gate-1 classification. This template originates and
relays; it cannot classify its own proposals (`artifact-flow.md` § "loom Splits, Never
Originates").

Locally-fixable work: **#74 alone**, ~1 autonomous cycle (hook fix + the missing `run.mjs`).
Everything else is a distribution-engine defect that must be fixed at loom, or it is rebuilt
away here on the next `/sync-to-use` (Class-A non-durable).

## 3. Prioritized immediate queue

### [BUG][CRIT][S3] #74 — `signing-mutation-guard` fail-open on the degraded path

A security guard **allows where its own committed fixture specifies block**. Reproduced with
the true exit code captured: `.codex-mcp-guard` `npm test` → **exit 1**, two failures verbatim:

```
✗ apply_patch degraded: expected DENY (gate bites), got allow=true
✗ shell degraded git-mut: expected DENY by signing-mutation-guard.js, got allow=true denier=(none)
```

The degraded (no-signing-key) path is precisely the untrusted-operator case the guard exists
to gate. Fixture `03-block-degraded-mode-mutation/expected.txt` specifies `severity: block` /
`exit_code: 2`; both expected files are byte-identical before and after the 2026-08-02 sync,
so intent did not change. Not caused by the #43 dependency bump (reverted lockfile, `npm ci`,
reproduced identically).

**Secondary gap** (`cc-artifacts.md` Rule 9): no canonical runner exists for the
`signing-mutation-guard/` fixture directory — unlike sibling `signing-model-key-separation/`
which ships `run.mjs`. The fixtures are inert documentation, which is _why_ the drift went
undetected.

**Disposition: FIX NOW, locally.** Category BUG → severity-independent fix-now. Relayed as
entry 30 so loom gets the class, but the local hook is broken _here_ today.

### [BUG][HIGH][S3] #78 → relayed — `.coc-obsoleted` ∩ shipped = 5 paths

Verified: all five listed=1 **and** ships=YES. A consumer applying the manifest as
`/sync-from-template` step 2.6 _mandates_ deletes files the same sync delivered. Observed
live on a 2.23.22 → 2.24.0 downstream sync. **Loom-side fix** (emitter invariant).

### [BUG][HIGH][S3] #79 → relayed — `rules/cross-repo.md` never ships

Verified absent, **zero** `.coc-obsoleted` entries (a missing distribution, not a purge
artifact), cited by 5 shipped artifacts including `repo-scope-discipline.md` — confirmed
`priority: 0` / `scope: baseline`, so **every consumer loads it every session** and its cited
authority resolves to nothing. **Loom-side fix.**

### [BUG][HIGH][S3] #75 → relayed — `sync-from-canon-fetch.mjs` static `loom_only` import

Verified: throws `ERR_MODULE_NOT_FOUND` at load. The correct guarded pattern already exists
in-tree at `loom-doctor.mjs:57`. **Sweep is complete** — three importers total: `sweep.md`
(filed, entry 22), this file (entry 31), `loom-doctor.mjs` (correct). No fourth.

### [BUG][MED][S3] #76 → relayed — journal reservation ignores the coordination opt-out

Verified asymmetry: `isCoordinationEnabled` appears **4×** in `codify-lease.js`, **0×** in
`journal-reserve.js`; roster absent. Two primitives in one `/codify` run disagree — the lease
degrades, the journal gate hard-fails. Silently removes `/codify`'s journal receipt on every
coordination-off repo.

### [INVEST-NOW][HIGH][S3] Entry 29 — make `validate-xref-integrity` a release gate

The **structural fix** for the whole batch. Verified on an unmodified 2.24.0 checkout:
**exit 1, "scanned 759 files, 2709 xref tokens; 355 dangling"**. The validator already
detects every finding above; it simply is not run at emission. → **Decision point D1.**

## 4. Deferred-quality backlog

**Empty.** `gh issue list --label deferred-quality --state open` → 0. Nothing is rotting
unsurfaced; no `value-prioritization.md` MUST-3 "still wanted?" gate is owed.

## 5. Decision points

### D1 — How does the xref release gate land? (recommend: **ratchet**)

Turning the gate on today **blocks every emission**, because the tree ships at 355 dangling.
It cannot land alone.

- **Ratchet (recommended)** — fail only on refs _newly_ dangling vs the prior cut. Closes the
  class immediately, converts the 355 into tracked debt, survives a partial burn-down.
  _Con:_ the existing 355 stay unfixed and need their own burn-down track.
- **Flag-day burn-down** — fix all 355 first, then gate hard. _Pro:_ ends clean.
  _Con:_ blocks the gate behind a large batch; the class keeps recurring meanwhile, and a
  gate deferred behind a burn-down is the shape that gets approved and never lands.

**Recommendation: ratchet**, recorded in entry 29's `template_ingest_note` for loom Gate-1.

### D2 — Is `guides/rule-extracts/**` obsoleted tree-wide, or is `git.md` one instance?

`git.md`'s body was _deliberately trimmed_ against an extract that does not ship
(`rule-authoring.md` Rule 10 paired extraction). If the whole tree is obsoleted, **every** rule
that trimmed against an extract has the same hole. **Recommend loom establish scope before
fixing point-wise** — flagged in entry 25.

### D3 — Close the six open issues now, or after loom Gate-1?

**Recommend closing #64, #78, #79 now** (each fully relayed; close comment cites the manifest
entry per `git.md` § Issue Closure Discipline). **Keep #74, #75, #76 open** — #74 has local
work outstanding, and #75/#76 are unfixed here. Auto-closure is _not_ proposed:
`value-prioritization.md` MUST-4 requires your gate. **Awaiting your approval.**

## 6. Per-sweep results

| Sweep                 | Result                                                                                                                                                                     |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 — Active todos      | 0                                                                                                                                                                          |
| 2 — Pending journal   | 2 in `docker-dev-environment/journal/.pending/` (both <14d, not stale). Cannot promote — journal gate blocked by #76.                                                      |
| 3 — Open issues       | 6 (#64, #74, #75, #76, #78, #79) — all triaged above                                                                                                                       |
| 4 — PRs / branches    | PR #77 merged this session; 0 open PRs; 0 stale remote branches; 1 worktree (main)                                                                                         |
| 5 — Redteam gaps      | `<!-- sweep-redteam:v1:N/A reason=orchestration-mode no_specs=true no_tool=true -->`                                                                                       |
| 6 — Hygiene           | `.session-notes` 62d stale (fragment reconciled this session); no orphan worktrees; no stale `.pending`                                                                    |
| 7 — Process           | Tree clean; `origin/main...HEAD` = `0 0`; no new stub markers                                                                                                              |
| 8 — Release readiness | **N/A — non-publishing.** No `pyproject.toml` / `Cargo.toml` / `package.json`. Latest stable tag `v2.23.3`.                                                                |
| 9 — Cross-ecosystem   | `<!-- sweep-ecosystem:v1:ERROR reason=Cannot find module '<repo>/.claude/bin/lib/loom-links.mjs' -->` — **live reproduction of already-filed entry 22**, not a new finding |
| 10 — Deferred-quality | 0 open                                                                                                                                                                     |

**Sweep 9 note:** this is the third independent confirmation of the `loom_only`-import class
(entry 22 `sweep.md`, entry 31 `sync-from-canon-fetch.mjs`, and this live run). The ERROR
sentinel fired where the command's own contract promises an N/A sentinel.

## 7. Recommendation

1. **Fix #74 now** — the only CRIT, the only locally-fixable one, and a fail-open security
   guard. Ship the missing `run.mjs` with it so the class becomes mechanically detectable.
2. **Ratify D1 (ratchet)** so entry 29 reaches loom Gate-1 with an adoption path rather than
   as a gate that cannot be turned on.
3. **Approve D3** — close #64/#78/#79 with manifest-entry receipts; keep #74/#75/#76 open.
4. **Relay the 32-entry manifest to loom** via `/sync-from-use` when loom is ready. Nothing
   further can advance here — 30 of 32 entries need Gate-1.

Findings are surfaced with categories and dispositions; **the queue decision is yours.**

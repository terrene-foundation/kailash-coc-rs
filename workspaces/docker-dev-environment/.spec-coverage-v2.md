# Spec Coverage v2 — Docker Dev Environment (kailash-coc-rs)

**Re-derived Round 2 (2026-05-29).** Supersedes the prior M2-milestone-scoped table
(which omitted base-image Features / ci-multiarch ADR-09 / provenance-sync and carried
an internal `kailash-enterprise` `4.2.2`/`4.3.0` version contradiction). Full findings +
dispositions + receipts: `04-validate/round-2-implementation-redteam.md`.

Re-derivation method: every row produced by a literal `grep`/`Read` against the
committed artifacts (analyst task `a2739f48bd1b9d2ab`), NOT file-existence or prior
self-report. Counts: **PASS 41 · FAIL 9 (all FIXED this round) · UNVERIFIABLE-STATICALLY 6**.

## Cross-spec consistency (PASS — re-grepped)

| Shared term                                        | Result                                                                                    |
| -------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| `kailash-enterprise` (Py pkg)                      | CONSISTENT — every site; no `pip install kailash-rs` anywhere (R1 HIGH-2 holds)           |
| `kailash` gem (Ruby)                               | CONSISTENT; upstream ABI defect hedged correctly (kailash-rs#1151)                        |
| `/opt/venv` + `/opt/gems`                          | CONSISTENT across Dockerfile + bin/dev + extensibility.md + README                        |
| `BUNDLE_PATH` not-exported                         | CONSISTENT (Dockerfile commentary, `bin/dev` `env -u`, extensibility.md)                  |
| Node 20 LTS                                        | CONSISTENT (cli-toolchain, ADR-06, Dockerfile, README)                                    |
| CLI version pins `@^2`/`@^0.134`/`@^0.43`          | CONSISTENT (ADR-05 major-line)                                                            |
| Image-size budget (≤2.5 GB target / 820 MB actual) | CONSISTENT (target vs documented actual; actual UNVERIFIABLE statically)                  |
| devcontainer Features                              | WAS CONTRADICTORY (specs said Features, code = apt/NodeSource) → **FIXED** (H-C)          |
| `--profile ml` mechanism                           | WAS CONTRADICTORY (specs said profile, code = `INCLUDE_ML` build-arg) → **FIXED** (H-D)   |
| `/opt/cargo` Rust toolchain                        | NOW CONSISTENT — cli-toolchain.md + Dockerfile (M-1 fix); was non-functional              |
| GPG copy contract                                  | WAS allowlist(spec)/denylist(code) → **FIXED** (M-9, spec now describes shipped denylist) |

## R1 carry-forward (remedies present in SHIPPED code — re-verified)

HIGH-1 (Rust-path discriminator) → `Dockerfile` importlib.metadata assert + CI smoke (1) — PRESENT.
HIGH-2 (stale `kailash-rs`) → zero hits any artifact — PRESENT.
HIGH-3 (CI arm64 QEMU / ADR-09) → `docker-build.yml` amd64-native blocking, no QEMU PR path — PRESENT.
MED-1 (NFR-12 env-pinning), MED-2 (Ruby probe + CI install-presence), MED-4 (host-config `${HOME}`) — PRESENT.
MED-3 (FR-03 (a)+(b)) — (b) UNVERIFIABLE-STATICALLY (live container).

## FAIL rows → all FIXED (see round-2 doc table H-A..L-6 for file:line)

H-C (Features phantom), H-D (--profile phantom), M-6 (stale C8 ×2), M-8 (Python UNVERIFIED
now shipped), M-9 (GPG allowlist→denylist), L-5 (npm overlay undocumented) — all spec
write-backs applied + grep-verified. H-A/H-B/M-1..M-5/L-1..L-4/L-6 — code/gate/README
fixes applied + `git diff`/`bash -n`/gate-grep verified.

## UNVERIFIABLE-STATICALLY (live `docker build`/run — T17 walk surface)

(1) image size 820 MB · (2) installed `kailash-enterprise` version · (3) FR-03(b) hook
fires · (4) Ruby `require "kailash"` load (upstream ABI) · (5) Python discriminator
fails on wrong package · (6) `docker history` zero-secrets (CI asserts) · (7) `INCLUDE_RUST`
build yields vscode-invocable cargo/rustc.

**Verdict:** GAPS-REMAIN at scan-time → all closed this round; live-build items deferred
to T17 with explicit receipts (not closed-by-assertion). Convergence pending R3 + CI live build.

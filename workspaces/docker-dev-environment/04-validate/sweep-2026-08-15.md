# /sweep — Management Decision Report

**Repo** kailash-coc-rs (`coc-use-template`) · **HEAD** `bd3c8d3` · **Posture** L5_DELEGATED
**Date** 2026-08-15 · **Triage** product-completion-first (BUG / INVEST-NOW / INCREMENTAL) ·
severity ranks, never gates. Classification fail-closed: ambiguous resolves toward immediate.

**The headline: the previous sweep's own recommendations were wrong in three of five places,
and the redteam removed a fourth.** Nine entries were implied; four were filed. That is the
cycle's real output — not the four entries, but the five things that were correctly NOT done.

**The second headline: the issue axis did not move.** 8 open at the last sweep, 8 open now.
Everything that moved, moved onto the proposal manifest, which is a surface loom has still
never been observed to ingest. That is F1, and it is now the only thing standing between this
template and a measurable burn-down.

---

## 1. Completion status

| Surface                                      | State                              | Durable receipt                                  |
| -------------------------------------------- | ---------------------------------- | ------------------------------------------------ |
| Prior sweep rec 1 — escalate #125 (D2a)      | **correctly NOT done**             | dedup scan; 9 entries already carry it           |
| Prior sweep rec 2 — four fence entries (D1a) | **two filed**, two already-covered | PR #164 `bd3c8d3`                                |
| Prior sweep rec 3 — confirm `rb` is F13 (D3) | **resolved; premise was false**    | commit `71ecf19`; forest row advanced            |
| Prior sweep rec 4 — #155 stays open          | **unchanged**                      | #155 open, held                                  |
| Prior sweep rec 5 — #159 no action           | **superseded** — entry now filed   | PR #164; manifest entry for the enumerator       |
| Proposal manifest                            | **129 → 133**, 110 STILL-OPEN      | `latest.yaml`, parse-verified, 0 prior altered   |
| F5 — preserve-list survives inbound `#150`   | **RE-CHECKED: HOLDS**              | `CLAUDE.md` blob identical across all four trees |
| F13 — rs/rb fleet collapse Phase 1           | **CONFIRMED LANDED**               | commit `71ecf19` (2026-06-01, loom 2.39.0)       |
| Emission (`emit.mjs --all --lang rs`)        | **RED at validator-16**, exit 1    | `class:coc-use-template → manifest FORBIDDEN`    |
| Rule-10 headroom obligation                  | **OPEN, 4th consecutive cycle**    | proximity-band `unrun_no_coverage`               |
| Codify lease                                 | **released**                       | `_released: true`                                |

**Walking skeleton stands.** The template emits for Claude Code and is consumable. What does
NOT stand is the Codex/Gemini emission path — blocked at validator-16 — so every baseline rule
authored here is CC-only in practice. That is a product-visibility gap, not a cosmetic one.

## 2. ETA to completion

**Locally actionable BUG + INVEST-NOW: TWO items, ~1 cycle total.** Both are proposal-authoring,
not code:

1. re-file the redteam-refuted rules-reference entry with corrected facts (~0.3 cycle)
2. file the entry-99 instrument-mismatch correction (~0.2 cycle)

**Everything else has no ETA this repo can give.** Seven of eight open issues are loom-owned;
#125's fix is structurally loom's (the class predicate keys on repo CLASS, loom's
`manifest_distribute` carve-out keys on template TYPE, and a multi-CLI USE template is both).
The 110 STILL-OPEN manifest entries resolve at loom Gate-1, and **no ingest of ours has ever
been observed**. A wall-clock figure would be fiction.

**One exception worth naming: #113 is NOT structurally loom-owned.** `.github/` is durable here
and #150 touches zero files under it. It is a deliberate deferral to avoid divergence, not a
capability limit — a local answer is available in ~1 cycle whenever the owner wants one.

## 3. Prioritized immediate queue — BUG + INVEST-NOW

Value-ranked per `value-prioritization.md` MUST-1.

1. **INVEST-NOW — re-file the rules-reference divergence entry.**
   Value-anchor (source (d), literal user quote, 2026-08-14): _"please /codify"_ — the user
   directed codification, and this is the one measured defect from this cycle with **no durable
   record at all**. The redteam correctly removed the first draft (its "70" was `79 − 9` dressed
   as a measurement; the real delivered count is 73, and the divergence runs both ways, which
   the asserted skip-path mechanism cannot produce). **The underlying defect is still real and
   still unfiled**: nine skill-channel rules are absent from the delivered index, four of them
   with complete correct frontmatter, and `emit.mjs:3102-3105` asserts in a comment that the
   counts "provably equal" while the emitter's skip path only warns.
   **Implication:** leave it unfiled and the only record is a removed commit and a session note
   — both of which the next sync does not read.

2. **INVEST-NOW — file the entry-99 instrument-mismatch correction.**
   Value-anchor (source (d), same quote). Entry 99 claims #112's "34 pass / 21 fail" was "stale
   and understated — at HEAD it is 800 pass / 53 fail". Measured both instruments this cycle:
   `audit-fixtures/validate-emit/run.mjs` → **34 passed, 21 failed** (so #112 is CURRENT and
   correct for the instrument it names); `bin/validate-emit.mjs` → **801 pass / 35 fail**.
   Different runners. Entry 99's own figure is now stale too.
   **Implication:** a Gate-1 reader sizing this work from entry 99 plans against the wrong
   surface and believes a figure was corrected when it was replaced by a measurement of
   something else.

The other seven open issues are loom-owned and not closeable here (#125, #119, #112, #79, #78,
#64 — plus #113, whose fix is elective, see D3).

## 4. Deferred-quality backlog

**One item.** Enumerated via `gh issue list --label deferred-quality --state open --json`;
the label is proven live (1 open, 1 closed — a closed instance means the label is not
write-only).

**#159 — `deferred-quality-backlog.mjs` enumerator** · trigger `on-demand` · filed 2026-08-14.

- **Blocking-safety:** touches no shipped path; `/sweep` Sweep-N runs the `gh issue list`
  invocation directly, as it did in this report.
- **Value-anchor:** recorded as **deliberately WEAK** — no source in the MUST-1 closed
  allowlist names it. The only anchor is the `/sweep` skill's own § 4, which is a COC artifact,
  not a user-authored source.
- **Acceptance criteria + revisit trigger:** in the issue body.

**Sweep-N "still wanted?" gate — SURFACED (see D5).** One `/sweep` cycle has elapsed since
filing, which is below the ≥2-cycle threshold; the ≥2-**session** threshold is arguably met
(filed in cycle 14c, one session since, this one now). The thresholds disagree, so the gate is
surfaced rather than deferred — surfacing early costs a paragraph, and the failure mode this
gate exists for (7-of-7 deferred items decayed rather than picked up) costs the item.

Two facts changed since filing and both bear on the disposition: (a) the durability gap is now
**closed** — PR #164 filed a manifest entry, so the obligation survives a sync whether or not
the script is ever written; (b) an adversarial re-read established the skill § 4 **specifies
and defers** with `gh issue list` as a **sanctioned** interim instrument — it is not an unmet
mandate. The original framing ("a mandate with no instrument") was corrected before landing.

## 5. Decision points

### D1 — #150: the calculus has changed twice, and it now cannot merge as-is

**New fact 1 (blocking):** `git merge-tree --write-tree HEAD pr150` exits 1 with **4 content
conflicts** (`validate-xref-integrity.mjs`, `guides/rule-extracts/agents.md`, `rules/agents.md`,
`rules/patterns.md`). #150 cannot land cleanly regardless of the fence question. No preserved
surface is among them.

**New fact 2 (raises the stakes):** the `.gitignore` revert is **also a credential fence**. The
merge drops `/workspaces/**/.env.*` while re-including `_template/`, making
`workspaces/_template/.env.production` committable **in this public repo** — a `security.md`
"No .env in Git" regression the merge _introduces_. Measured in an isolated scratch repo, both
polarities. The earlier framing as a tracked-vs-ignored policy disagreement understated it.

**New fact 3 (in #150's favour):** F5 HOLDS. `CLAUDE.md` is byte-identical across merge-base,
HEAD, PR head, and merged tree; every preserved surface shows zero diff entries; and the
disclosure scrub is **repaired**, not weakened — independently corroborated here, since our
current scanner exits 0 on a nonexistent root and #150 is what fixes that.

**(a) Keep HOLDING; the two fence entries are now filed.** _Pro:_ correct lane; the durable
surface is the proposal and two of the four are now on it. _Con:_ we keep neither the fixes nor
the real hardening #150 carries, and there is still no observed Gate-1 ingest.
**(b) Resolve the 4 conflicts and merge with the fences re-applied locally.** _Pro:_ unblocks
the hardening, including the scanner repair. _Con:_ Class-A non-durable — rebuilt by the next
sync unless the entries land too, so it is (a) plus conflict-resolution work.
**(c) Merge as-is.** _Con:_ **not recommended** — ships a reachable sanitizer removal AND a
credential-fence regression into a public repo.

**Recommendation: (a), unchanged — but the `.env` finding should be raised to the owner
now rather than waiting on Gate-1.** **Con, plainly:** if Gate-1 never ingests, this holds
indefinitely, and the scanner repair we are declining is one we independently proved we need.

### D2 — re-file the refuted entry now, or next cycle?

**(a) Re-file this cycle.** _Pro:_ the facts are measured and warm (73, the `+3` reverse
divergence, the synced-artifact provenance); re-deriving them cold costs 2–5×. _Con:_ it is a
third consecutive manifest append with no observed ingest.
**(b) Defer to next cycle.** _Pro:_ none that survives inspection. _Con:_ the only record is a
removed commit; `wave-loop.md` MUST-7 says an unfiled finding decays.

**Recommendation: (a).** It is ~0.3 cycle and the redteam already did the hard part —
establishing exactly which three facts the entry must state.

### D3 — #113: take the local answer, or keep deferring?

Three sweeps have bucketed #113 with #79/#78 as "loom-owned". It is not: `.github/` is durable
here, #150 touches zero files under it, and the issue body itself says it "could be implemented
locally at any time. It is parked on the answer, not on capacity."

**(a) Take the local answer** (~1 cycle: add the sync-ADDED scan direction to Gate A). _Pro:_
closes a real coverage gap in a gate that just proved load-bearing this cycle; durable here.
_Con:_ divergence from the canonical copy until loom adopts it.
**(b) Keep deferring.** _Pro:_ zero divergence. _Con:_ a fourth sweep carries it in the wrong
bucket, which is how "loom-owned" becomes unfalsifiable.

**Recommendation: (b), but RE-BUCKETED as an owner decision, not a loom dependency.** The
divergence cost is real and this is a design question the owner should answer, not one the
agent should self-resolve. **Con:** that is one more cycle of the item sitting still.

### D4 — post the drafted correcting comments on #119 and #112?

Both are drafted, measured, and unposted. #119's body overstates the harm ("Codex/Gemini
receive no guidance whatsoever" — the delivered index DOES carry the pattern-rules row, and did
before the frontmatter fix). #112's part B attributes a failure to validator-14, which now
passes.

**(a) Post both.** _Pro:_ leaving a known-wrong claim on an open issue misdirects whoever picks
it up. _Con:_ the prior cycle recorded its #155 comment as "owner-authorized", so issue comments
appear to be a gated surface here.
**(b) Hold for owner.** _Pro:_ consistent with that precedent. _Con:_ the inaccuracy persists.

**Recommendation: (a), with the gate honoured — i.e. ask once, now.** This is a one-time
authorization question, not a per-comment one.

### D5 — #159: still wanted?

**(a) Close with owner gate** — the durability gap is closed by the manifest entry, and § 4
already sanctions `gh issue list` as the instrument. _Pro:_ removes an item whose value-anchor
was recorded WEAK at filing. _Con:_ loses the convenience script.
**(b) Re-defer with a fresh anchor.** _Pro:_ keeps it visible. _Con:_ re-defers an item with no
user-anchored source — exactly the decay pattern.
**(c) Implement it.** _Con:_ ~0.3 cycle for a convenience wrapper over a one-line `gh` call.

**Recommendation: (a) close, with the user gate.** Per `value-prioritization.md` MUST-4 the
agent MUST NOT close this unilaterally — this is a recommendation for ratification.

## 6. Recommendation

1. **Re-file the rules-reference entry with the three corrected facts** (D2a) — the only
   measured defect from this cycle with no durable record.
2. **File the entry-99 instrument-mismatch correction** — it misdirects Gate-1 work sizing.
3. **Raise the `.env` credential-fence finding on #150 to the owner now** (D1) — it is a
   public-repo secret-exposure regression, not a policy disagreement.
4. **Authorize (or decline) issue comments as a class** (D4) — one question, not per-comment.
5. **Ratify closing #159** (D5) — or say re-defer and it gets a fresh anchor.
6. **Re-bucket #113 as an owner decision** (D3), not a loom dependency.

---

## Instrument notes

1. **The prior sweep's D3 premise was factually false.** It asserted both `sync-manifest.yaml`
   and `emit-axes.mjs` omit `rb`. Both retain it, with a retention comment. A sweep premise is
   not evidence; it needs the same re-derivation as any other carried-forward claim.
2. **`scan-synced-disclosure.mjs --check` exits 0 on a NONEXISTENT root** — byte-identical to a
   clean scan. Its exit 0 is not evidence. Verified with a controlled grep instead. Inbound
   #150 repairs exactly this.
3. **A key-scan regex of `^[a-z_]*:` cannot match a key containing digits** and silently hid a
   third top-level key in `latest.yaml`; a naive append would have landed inside the wrong
   block. Caught by a dry-run splice, not by review.
4. **Gate A cannot distinguish a rewrite from a removal.** A PostToolUse formatter re-indented
   a comment block and Gate A failed with "2 locally-authored line(s) dropped". Nothing was
   dropped. `ACK-DISCARD` was NOT used — the honest fix was restoring the bytes.
5. **A tally is not a finding.** A comment-line COUNT matched 576/576 while 15 lines were
   missing, because the re-indented copies were still present. Only a line-by-line `grep -qxF`
   found it.
6. **Two of this cycle's near-misses were mine, both caught before they landed:** an
   over-broad disclosure matcher nearly reported the repo's own allowlisted coordinates as a
   leak, and an entry-count claim of "ten" was corrected to nine by an adversarial re-scan.

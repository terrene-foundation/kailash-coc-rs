# /sweep — Management Decision Report

**Repo** kailash-coc-rs (`coc-use-template`) · **HEAD** `0f9c11b` · **Posture** L5_DELEGATED
**Triage** product-completion-first (BUG / INVEST-NOW / INCREMENTAL)

Severity ranks; it is never the fix-vs-defer gate (`product-completion-first.md` MUST-1).
Classification is fail-closed: ambiguous resolves toward immediate.

**The headline is a coupling nobody had measured: `emit.mjs` has TWO independent blockers, and
fixing the known one does not unblock it.** Validator 14 fails today. If 14 were fixed, validator
16 — which runs next — fails unconditionally for this repo class. The open issue everyone has been
treating as "the emitter bug" (#119) is only the first of two.

---

## 1. Completion status

| Surface             | State                                | Receipt                                        |
| ------------------- | ------------------------------------ | ---------------------------------------------- |
| PR queue            | **0 open**                           | `gh pr list --state open` → 0                  |
| Working tree        | clean                                | `git status --porcelain` → empty               |
| Downstream inbox    | **INGESTED + merged + relayed**      | PR #123 (`d26b43a`), PR #124 (`0f9c11b`)       |
| Proposal manifest   | **93 entries**, all `pending_review` | `yaml.safe_load(latest.yaml)` → 93             |
| Inbox files on main | 8                                    | `ls .claude/.proposals/inbox/*.yaml`           |
| Validator status    | **MEASURED** (was UNKNOWN)           | direct invocation of the 5 exported validators |
| Open issues         | 8 (one net-new: #125)                | `gh issue list --state open` → 8               |

**Nothing is complete-and-visible in the product sense.** This repo's product is _distributed
artifact correctness_. No correction reaches a consumer until loom ingests; 93 entries are queued.
Every local fix under `.claude/` is Class-A and rebuilt away by the next sync.

**What changed since the 2026-08-08 sweep.** That report closed with the locally-actionable queue
empty and named one optional task: diagnose validators 15/16/18, whose status was UNKNOWN because
`emit.mjs` exits at validator 14 before reaching them. That task ran. It produced two measured
failures (16 and 18), cleared two false unknowns (15 and 17 are correct), and surfaced the coupling
in the headline. The upflow lane also moved: the instrument-scope offer merged and relayed.

### The validator picture, now measured

`emit.mjs` runs validators **14 → 16 → 18 → 15 → 17**; each `process.exit(1)`s on failure.

| Validator                 | Result                                          | Owns     |
| ------------------------- | ----------------------------------------------- | -------- |
| 14 rule-frontmatter       | **FAIL** — 6 files                              | #119     |
| 16 manifest-yaml          | **FAIL** — forbids a file loom ships on purpose | **#125** |
| 18 cli-delivery           | **FAIL** — 4 rules undelivered to Codex/Gemini  | #119     |
| 15 tier-completeness      | SKIP — asserts nothing in this class, correctly | —        |
| 17 roster-schema-coupling | PASS (half A; half B not asserted here)         | —        |

15 and 17 are fine; that question is closed. **A validator that never runs is UNKNOWN, not
passing** — had 14 passed, a `[validator-16]` line would appear in the log. Its absence was the
evidence, and absence is easy not to look at.

---

## 2. ETA to completion

**Locally actionable: ZERO cycles.** All eight open issues are loom-blocked, owner-routed, or a
tracked INCREMENTAL hold. No open item names a fix this repo can durably apply.

**Everything else: unbounded, gated on loom Gate-1, outside this repo.** That is a dependency, not
a work estimate. Putting a number on it would be fiction.

**One estimate IS available and worth stating, for loom's planning rather than ours:** the
#119 + #125 pair is **~1 autonomous cycle at loom** if taken together — both are single-surface
edits (frontmatter fields on 6 files; a class-vs-template-type condition in one validator) with the
diagnosis already written. Taken separately it is ~2 cycles plus a wasted verification round, since
fixing #119 alone leaves `emit.mjs` still exiting 1 and looks like a failed fix.

---

## 3. Prioritized immediate queue — BUG + INVEST-NOW

Value-ranked. Every anchor is the co-owner's standing directive to land corrections in loom
(the F1 ledger anchor) unless noted.

| #   | Item                                                                                         | Cat        | Durable here?             | Implication                                                                                                                                        |
| --- | -------------------------------------------------------------------------------------------- | ---------- | ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **#125** `emit.mjs` v18 reads the manifest v16 orders deleted                                | BUG        | **no — loom**             | **Latent blocker.** Unreachable today behind v14; becomes the blocking failure the moment #119 lands. Following v16's instruction reproduces #184. |
| 2   | **#119** rs-variant rules land without frontmatter; `patterns.md` ABSENT from Codex + Gemini | BUG        | **no — loom**             | Two of three CLIs ship a materially weaker rule surface. Now carries the measured 6-file list + v18's 4 undelivered rules. Months old.             |
| 3   | **#79** dangling authority in a `priority:0` baseline                                        | BUG        | no — loom                 | Every consumer, every session, loads a rule whose authority is absent.                                                                             |
| 4   | **#112** six fixture runners ship where their subject deliberately does not                  | BUG        | no — loom                 | Enforcement instruments that cannot pass at any consumer tier train readers to treat red as normal.                                                |
| 5   | **#78** `.coc-obsoleted` lists paths the same release ships                                  | BUG        | stopgap only              | A faithful consumer purge deletes files the sync just delivered. **May sit downstream of #125** — see the inference note below.                    |
| 6   | **#64** `/sync-from-template` doc-vs-behaviour contradiction                                 | BUG        | **fixed, Class-A**        | Fix IS on disk but reverts unless loom ingests.                                                                                                    |
| 7   | **#113** Gate A has no coverage for sync-ADDED content                                       | INVEST-NOW | yes, but **owner-routed** | The `VERSION` injection would pass it. Owner routed the design call to loom 2026-08-07.                                                            |

**7 of 7 are loom-blocked or owner-routed.** That is the whole finding of this section, unchanged
in shape from the prior sweep — but the queue is no longer static: #125 is net-new, and #119 grew
a measured blast radius.

> **Labelled inference, not fact.** The #78↔#125 link is an INFERENCE, not a measurement.
> `.coc-obsoleted` is generated from `sync-manifest.yaml`, and #125 questions whether that manifest
> belongs in a USE template at all — so a resolution of #125 plausibly changes #78's premise. I did
> not verify that the generator's behaviour actually depends on the resolution. Recorded so loom can
> check it rather than inherit it as settled.

---

## 4. Deferred-quality backlog — INCREMENTAL

One item. Enumerated via `gh issue list --label deferred-quality`.

### Trigger `on-demand` (surfaces at every `/sweep`)

**#114 — Four small content drops from the `52ee39e` sync left unrestored, never enumerated**

Carries all four generalized-1b conditions, verified in the body this cycle:

1. **Blocking-safety note** — prose lines in `.claude/**`; no shipped path touches them. Everything
   load-bearing from that sync landed separately in PRs #98–#104.
2. **Value-anchor** — the co-owner "land these corrections in loom" directive (F1), and the issue
   states honestly that this anchor is INHERITED from the recovery workstream, not specific to
   these four lines.
3. **Full-fix acceptance criteria** — enumerate the four (they were never written down), record a
   take-ours / take-theirs / merge disposition each, route anything still wanted through a Step-7b
   proposal. Closing with "nothing worth doing" is explicitly a valid outcome; closing without
   naming the four is not.
4. **Revisit trigger** — `on-demand`, with a recorded rationale for not anchoring to a loom
   milestone that may never observably fire.

**Sweep-N status: the "still wanted?" gate is DUE.** Filed 2026-08-07; this is its third `/sweep`
cycle (08-07, 08-08, 08-09), past the ≥2-cycle threshold in `value-prioritization.md` MUST-3. It is
surfaced as decision point **D1** below rather than silently re-deferred.

---

## 5. Decision points

### D1 — #114: still wanted? (Sweep-N gate, MUST surface)

The deferred-quality label is net-negative without this gate; the rule's own Origin records 7-of-7
deferred items decaying rather than being picked up. Three dispositions are available, and the
choice is yours (`value-prioritization.md` MUST-4 — no auto-close, no OR-escape-hatch).

**Recommendation: re-defer with a fresh anchor.**

**Why:** the enumeration step has genuine standing value — the four drops still are not written
down anywhere, which is the actual defect the issue records, and that gap costs a future session
real re-derivation work. But nothing about it is urgent, and its own body concedes the value-anchor
is inherited rather than specific.

**Cons, honestly:** re-deferring is what the rule's Origin warns about — an item re-deferred
indefinitely is decaying with extra ceremony. If the enumeration is never going to be worth one
session, closing it now with the gate is the more honest act, and it is a legitimate outcome the
issue explicitly sanctions.

**What would change the recommendation:** if you do not recognise value in these four specific
lines, choose close-with-gate. The issue was filed precisely so this judgment gets made rather than
made by forgetting.

### D2 — Should the #119/#125 coupling be recorded upstream?

The coupling was discovered during this triage, after both issues were filed. Neither issue
currently says that fixing #119 alone leaves `emit.mjs` failing.

**Recommendation: yes — post a short cross-reference comment on both.**

**Why:** loom will otherwise very likely fix #119 first (it is older, better-understood, and reads
like the emitter bug), observe `emit.mjs` still exiting 1, and spend a verification round
rediscovering validator 16. One comment on each issue costs minutes and removes that round.

**Cons:** it adds two more comments to a queue where nothing is being actioned locally, and the
coupling is derivable from #125's body by a careful reader. It is coordination, not a fix — it does
not advance a single item toward done.

---

## 6. Recommendation

1. **Post the D2 cross-reference comments** on #119 and #125. Cheapest item on the board with a
   real payoff at loom's end.
2. **Answer D1** — re-defer #114 with a fresh anchor (my pick), or close it with the gate.
3. **Do NOT open another proposal wave.** The manifest is at 93 `pending_review` entries; the
   bottleneck is loom Gate-1 ingest, not input from here. This is unchanged from the prior two
   sweeps and remains the single most load-bearing constraint on this repo.
4. **Do NOT manufacture local work.** Everything actionable here is Class-A and rebuilt away. Two
   prior sessions were spent recovering from exactly that loss class.

The honest summary: this cycle converted unknowns into measured, diagnosed, filed findings and
moved the upflow lane forward. It shipped no fix, because no durable fix exists in this repo.

# /sweep — Management Decision Report

**Repo** kailash-coc-rs (`coc-use-template`) · **HEAD** `9b0ef9d` · **Posture** L5_DELEGATED
**Triage** product-completion-first (BUG / INVEST-NOW / INCREMENTAL) · severity ranks, never gates
Classification is fail-closed: ambiguous resolves toward immediate.

**The headline: last cycle's three actions all landed, and the manifest queue MOVED for the
first time — 127 → 129.** That row had been flat every prior sweep. It moved only because a
redteam arm caught that the codify had no proposal append at all, which would have made the
whole fix local-only and deleted by the next sync.

**The second headline is a blocker, not an achievement.** `validator-16` has now prevented
three consecutive cycles from measuring their own Rule-10 headroom. It has stopped being a
background defect and is now the thing gating the corpus's ability to codify itself.

---

## 1. Completion status

| Surface                                    | State                                  | Durable receipt                               |
| ------------------------------------------ | -------------------------------------- | --------------------------------------------- |
| Sweep rec 1 — #155 corrections             | **posted**                             | `#155` comment `5294521027`; PR left OPEN     |
| Sweep rec 2 — #150 security pass           | **complete** (bounded, non-exhaustive) | PR #160 `a250939`, PR #161 `e26d788`          |
| Sweep rec 3 — #150 HELD                    | **unchanged**                          | #150 open, DIRTY, not merged                  |
| Sweep rec 4 — enumerator issue filed       | **filed**                              | issue #159                                    |
| `/codify` — agent-result-delivery 2nd mode | **landed**                             | PR #162 `9b0ef9d`; journals `0031`+`0032`     |
| Proposal manifest                          | **127 → 129**, 106 STILL-OPEN          | `latest.yaml`, parse-verified, 0 altered      |
| Codify lease                               | **released**                           | `_released: true`, `released_at 13:23:36Z`    |
| Codify anchor                              | **advanced**, verified real UTC        | `2026-08-14T13:17:47Z`, delta −0.4s           |
| Emission (`emit.mjs --all`)                | **RED at validator-16**                | `class:coc-use-template → manifest FORBIDDEN` |
| Rule-10 obligation                         | **OPEN, 3rd consecutive cycle**        | `journal/0032` § "Not fixed, recorded"        |

**Everything the last sweep asked for is done.** What is NOT done is anything requiring loom.

## 2. ETA to completion

**Locally actionable BUG + INVEST-NOW: ONE item, ~1 cycle** — see D2 (validator-16). That is
new; the last two sweeps could honestly say zero.

**Everything else has no ETA we can give.** All seven remaining open issues are loom-owned and
carried by manifest entries. The 106 STILL-OPEN entries resolve at loom Gate-1, and we have
still observed no ingest of ours. A wall-clock figure would be fiction.

**#150 re-adjudication, if the decision goes that way: ~1 cycle** — the fence repair is
re-wiring existing code (`upflow-self-repo.js` survives byte-intact with zero callers), not
rewriting it.

## 3. Prioritized immediate queue — BUG + INVEST-NOW

**One item.** Value-ranked; the anchor is a literal user quote from this session.

1. **INVEST-NOW — `validator-16` blocks all baseline-rule headroom measurement (issue #125).**
   Value-anchor (source (d), literal user quote, this session): _"please /codify"_ — the user
   directed codification, and this defect makes every baseline-rule codify unable to satisfy
   its own Rule-10 gate. Three cycles have now recorded the same open obligation.
   **Implication:** while it fails, `emit.mjs --dry-run` halts before
   `validateAggregateHeadroom` and `validate-proximity-band.mjs` returns `unrun_no_coverage`.
   The next baseline codify will record the obligation a fourth time — which `rule-authoring.md`
   Rule 11's own BLOCKED corpus names as the wallpaper failure.

The other seven open issues are loom-owned and NOT closeable here (#119, #113, #112, #79, #78,
#64, plus #125 whose FIX is loom's even though its CONSEQUENCE is ours).

## 4. Deferred-quality backlog

**One item, filed this session.** Enumerated via
`gh issue list --label deferred-quality --state open`; the label is proven live (applied to
#114, closed 2026-08-09, and to #159 today).

**Revisit trigger `on-demand`:**

- **#159 — `deferred-quality-backlog.mjs` enumerator.** Blocking-safety: touches no shipped
  path; `/sweep` Sweep-N runs the `gh issue list` invocation directly, as it did here.
  Value-anchor: **deliberately recorded as WEAK** — no source in the MUST-1 closed allowlist
  names it; the only anchor is the `/sweep` skill's own § 4 mandate, which is a COC artifact,
  not a user-anchored source. Acceptance criteria + revisit trigger: in the issue body.

**Sweep-N "still wanted?" gate: NOT yet due.** #159 was filed today — 0 cycles stale, not ≥2.
Recorded so the next sweep does not treat today's filing as an aged item.

## 5. Decision points

### D1 — #150: who repairs the fence?

The bounded pass is complete and the disposition is surgical, not wholesale. The sync carries
real hardening (a disclosure-scrub gate that could never fire is repaired) alongside the
subtractions. Four items to fence, ranked by REACHABILITY not size:

1. the sanitizer removal (**reachable** via the genesis/owner-add/owner-depart ceremonies),
2. the four-layer MUST-4 fence deletion (larger, but zero production callers on both refs),
3. the `.gitignore` `/workspaces/*` revert, 4. the #64 anti-recurrence lock.

**(a) Keep HOLDING, file the four as manifest entries, let loom re-issue.** _Pro:_ correct lane
— we are the template, loom owns the sync content; nothing we edit locally survives
`/sync-to-use` anyway. _Con:_ we have observed no Gate-1 ingest of our entries, so "let loom
fix it" may mean indefinite hold on a sync that also carries real hardening we are not getting.
**(b) Re-apply the four fixes locally on top of the incoming version, in a PR against #150.**
_Pro:_ Gate A's own resolution option 1; unblocks the hardening. _Con:_ Class-A non-durable —
rebuilt away by the next sync unless the entries land too, so it is (a) plus extra work.
**(c) Merge #150 as-is and fix forward.** _Pro:_ none that survives inspection. _Con:_ **not
recommended** — ships a reachable sanitizer removal and an ignore rule that makes our own sweep
reports unaddable.

**Recommendation: (a), with the four entries filed this cycle or next.** The durable surface is
the proposal; local re-application is the third reversion of the same content. **Con, plainly:**
if Gate-1 never ingests, #150 holds indefinitely and we keep neither the fixes nor the hardening
— which is exactly F1, and it argues for D2 being the real priority.

### D2 — validator-16 (#125): is this now the top item?

**No decision was owed on this before; one is now.** Three cycles have recorded the same
unmeasurable Rule-10 obligation.

**(a) Treat #125 as INVEST-NOW and escalate it as the blocking item.** _Pro:_ it is the root
cause of a recurring, compounding process failure, and `rule-authoring.md` Rule 11 exists
precisely to escalate a pattern rather than repeat a relief valve. _Con:_ the FIX is loom's —
we can escalate but not land it, so this may just move the wait.
**(b) Keep recording the obligation each cycle.** _Pro:_ zero work. _Con:_ **not recommended** —
this is the wallpaper failure by name; a fourth recording makes the honest note meaningless.

**Recommendation: (a).** Escalate #125 from "open loom-owned issue" to the named blocker on
baseline codification, in its own manifest entry stating that consequence. **Con:** it competes
with D1's four entries for the same Gate-1 attention we have no evidence is being paid.

### D3 — the `rb` lane retirement: confirm it is F13

`sync-manifest.yaml::repos` and `emit-axes.mjs::EMIT_LANGS` both drop `rb`, consistently — so
it is deliberate. It most likely IS forest item **F13** (rs+rb → one all-bindings template,
loom#423, Phase 1 already landed). **No decision owed if that is right** — the ledger row
advances. If it is NOT F13, it silently orphans any `kailash-coc-claude-rb` consumer.
**Recommendation: confirm F13, then advance the ledger row.** One question to the owner, not a
work item.

## 6. Recommendation

1. **Escalate #125 (validator-16) as the blocking item** (D2a) — it is the only locally-surfaced
   INVEST-NOW and the root cause of a three-cycle recurrence.
2. **Keep #150 HELD; file the four fence items as manifest entries** (D1a).
3. **Confirm the `rb` retirement is F13** (D3) and advance the ledger row.
4. **#155 stays open** — corrections posted this cycle; disposition is loom's at Gate-1.
5. **#159 needs no action** — filed today, `on-demand`, not yet revisit-due.

---

## Instrument notes

1. **Three of four #150 lanes inferred merge effect from a two-tree diff**, which answers a
   different question. `git merge-tree` overturned three findings. Any future claim about what
   merging does MUST be verified against the merged tree.
2. **`--status=removed` on the PR files API is rename-blind** — reported 7 deletions where
   `git diff --no-renames --diff-filter=D` finds 8.
3. **Two probes of my own were non-discriminating and were re-instrumented before any claim
   rested on them:** a per-function `awk` that leaked the whole-file count (identical figures
   across five distinct functions was the tell), and a lease-state check that passed an object
   where the API takes a string, reading `{lease:null}` as a held lease.
4. **The proximity-band instrument was RUN and declines to issue a verdict** —
   `unrun_no_coverage`, "THIS RUN IS NOT EVIDENCE". Recorded as unobtainable, never as a pass.

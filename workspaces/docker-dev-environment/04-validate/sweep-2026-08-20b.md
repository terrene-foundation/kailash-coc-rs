# /sweep — Management Decision Report

**Repo** kailash-coc-rs (`coc-use-template`) · **HEAD** `f9f7ce8` · **Posture** L5_DELEGATED
**Date** 2026-08-20 (second sweep this day) · **Triage** product-completion-first
(BUG / INVEST-NOW / INCREMENTAL); severity ranks, never gates. Classification fail-closed.

**The headline: the tracker discipline was shipped here but never switched on.**
`.claude/bin/burndown-build.mjs`, both quote-guard hooks, `lib/burndown-quote.js` and
`rules/burndown-integrity.md` all ship in this repo. No `burndown-manifest.json` had ever been
declared, so the guards — which are scoped by manifest presence — stayed dormant, and every
burn-down this repo has produced was hand-computed prose. This sweep declares the manifest and
the register; the block in § 8 is GENERATED and quoted, not tallied.

---

## 1. Completion status

| Surface | State | Durable receipt |
| --- | --- | --- |
| Manifest re-reconciliation (133 entries) | **COMPLETE** | commit `f789212`, PR #173 |
| `.gitignore` interim (workspace tracking) | **COMPLETE**, 17/17 verified | commit `d49e322` |
| `.gitignore` upstream ask | **FILED** — manifest entry 136 | commit `f9f7ce8`, PR #173 |
| Three inbox proposals merged | **COMPLETE** | PRs #167, #168, #170 |
| Three issues closed on measurement | **COMPLETE** | #64, #78, #159 (each cites a SHA/PR) |
| #79 / #112 re-scoped on re-measurement | **COMPLETE** | issue bodies, 2026-08-20 |
| Outstanding-work tracker | **NEWLY ESTABLISHED** | `burndown-manifest.json` + register |
| Emission (`emit.mjs --all --lang rs`) | **RED at validator-16** | `class:coc-use-template → FORBIDDEN` |
| #171 worktree fix | **REBASED, UNPUSHED — fixture RED** | local `39e06df` |
| Sweep 5 (spec-vs-source) | **N/A — orchestration-mode**, honest | sentinel in § 7 |
| Unadjudicated-verdict streaks | **NONE** — 0 keys across 19 reports | `unadjudicated-escalation.mjs` exit 0 |
| Worktree forest | 3 trees, 161.8 MiB, **0 reapable** | `worktree-reap.mjs` exit 0 |

**Walking skeleton stands for Claude Code and does NOT stand for Codex/Gemini.** Unchanged, and
still the single most consequential open item (#125, 5th cycle).

## 2. ETA to completion

**Locally actionable BUG + INVEST-NOW: one item — #171's runtime worktree reproduction
(~0.5 autonomous cycle).** Everything else open is loom-owned or owner-decision.

The residual is dominated by the **architectural-ask class** the re-reconciliation identified:
asks a content sync structurally cannot carry (retire a parity check for derivation, stop
exiting at the first failing gate, reverse a documented contract position). A wall-clock figure
for those would be fiction, and none is given.

## 3. Prioritized immediate queue — BUG + INVEST-NOW

### 3.1 BUG — #171's fixture is RED and the static fix was REFUTED

Value-anchor: user 2026-08-20, *"triage the four new PRs starting with #171"*.

The rebase and conflict resolution are done at `39e06df`. The boundary-roots fixture exits **1**
with 3 failures on callers the 2026-08-19 sync added. A predicate narrowing was authored and
**reverted**: it exempted `derives-from-ledger`, which the PR measured as genuinely failing from
a worktree and which backs a mandatory `/codify` step.

Measured, so the next session does not re-attempt it: direct probe of `appendSinkLine` shows
sink-inside-`repoDir` → `{ok:true}` and sink-in-main-with-`repoDir`=worktree → the exact
containment refusal — but `derives-from-ledger` builds the contained shape and still fails. File
shape cannot discriminate. **Only a runtime worktree reproduction can.**

### 3.2 BUG — emission blocked for two of three CLIs (#125)

Unchanged, 5th cycle, loom-owned, conveyed. Every baseline rule authored here is CC-only in
practice.

### 3.3 Nothing else qualifies

Stated explicitly rather than padded.

## 4. Deferred-quality backlog

**EMPTY — 0 items.** `gh issue list --label deferred-quality` returns 0. The sole prior item
(#159) was closed this session under the owner-ratified Sweep-N gate. **Sweep 10 fires and finds
nothing**, which is a real change: this is the first sweep in the series with an empty
deferred-quality backlog.

## 5. Decision points

### D1 — #79: inline carve-out, or a consumer-tier stub?

**(a) Give the always-loaded citer the same inline carve-out** `repo-scope-discipline.md`
already carries. *Pro:* proven pattern, costs nothing downstream, smallest change.
*Con, plainly:* fixes one citer; the next always-loaded rule citing `cross-repo.md` re-opens it.
**(b) Ship a consumer-tier stub** carrying only the ask-never-guess contract. *Pro:* resolves all
16 authority citations at once. *Con:* a new shipped artifact plus a manifest tier entry.

**Recommendation: (a).** The citation count grew 12 → 16 between filings, which argues for (b)
long-term — but (a) closes the live always-loaded instance now, and (b) can follow if the count
keeps climbing.

### D2 — #113: Gate A coverage for sync-ADDED content

Carried, owner call, unchanged. `.github/` is durable here (1 manifest path, `py`-keyed,
`rs: []`); #150 touches 0 files under it.

### D3 — #150 has been held 6 days

**(a) Review and merge it.** *Pro:* it is the inbound loom sync; holding it widens the drift the
next sync must reconcile. *Con:* it is the PR whose content the security passes flagged, so it
needs the read, not just the merge.
**(b) Keep holding.** *Con:* six days of drift, compounding.

**Recommendation: (a)**, as a scoped review rather than an admin-merge.

### D4 — Branch hygiene: 6 refs, zero risk, zero urgency

Three merged-and-reapable remote branches (surfaced only because Sweep 4 enumerated UNFILTERED —
`--no-merged` hid all three) and three stale local refs. **Recommendation: delete the three
merged remotes; leave the local refs.** *Con:* it is hygiene, not capability — lowest value item
in this report, listed because a tally would have hidden it.

## 6. Recommendation

1. **Reproduce #171 from a real worktree** (§ 3.1) — the one locally-actionable BUG.
2. **Decide #79's option (a) vs (b)** (D1).
3. **Review and merge #150** (D3) — six days held.
4. **Triage #166 and #155** — still unread since filing.
5. **Reap the three merged branches** (D4) — mechanical.

---

## 7. Sweep sentinels

```
<!-- sweep-redteam:v1:N/A reason=orchestration-mode no_specs=true no_tool=true -->
<!-- worktree-reap:v1:total=3 scope=all selected=3 zero_loss=0 tag_first=0 keep=3 applied=false size_kb=165632 size_unknown=0 size_partial=0 free_kb=444097492 headroom_trees=12948 -->
<!-- unadjudicated-escalation:v1 threshold=3 runs=19 keys=0 escalations=0 suppressed=0 -->
```

Sweep 5 pre-condition probe: `spec_count=0`, `tools/sweep-redteam.py` absent, no repo-level
`docs/specs`/`specs` tree, `spec-corpus-conformance.mjs` absent → **orchestration-mode N/A**, the
structural branch, not a proxy substitution. Sweep 9: `.claude/VERSION::type` is
`coc-use-template` with no resolver config → **N/A `not-orchestration-root`**, the structural
branch (this is a consumer-class clone, not a broken checkout).

## 8. Burn-down — GENERATED, quoted verbatim

<!-- BURNDOWN:BEGIN generated by .claude/bin/burndown-build.mjs — DO NOT EDIT BY HAND -->
<!-- BURNDOWN:END -->


# /sweep — Management Decision Report

**Repo** kailash-coc-rs (`coc-use-template`) · **HEAD** `142ce36` · **Date** 2026-08-07
**Posture** L5_DELEGATED · **Triage** product-completion-first (BUG / INVEST-NOW / INCREMENTAL)

Severity is a ranking attribute here, never the fix-vs-defer gate (`product-completion-first.md`
MUST-1). Classification is fail-closed: ambiguous → immediate.

---

## 1. Completion status

| Surface                   | State                                          | Receipt                                     |
| ------------------------- | ---------------------------------------------- | ------------------------------------------- |
| PR queue                  | **0 open**                                     | `gh pr list --state open` → empty           |
| Working tree              | clean                                          | `git status --porcelain` → empty            |
| Proposal manifest         | **84 entries**, all `pending_review`           | `latest.yaml` parse                          |
| Downstream inbox relay    | **18/18 declared files relayed, 0 gaps**       | 5 inbox files × `changes[]` cross-check      |
| Enforcement layer         | **19/29 runners green**                        | census at ≥150s per runner                   |
| Open issues               | **4**, none newly broken                       | #89 #79 #78 #64                              |
| Consumer-offer PRs (G2)   | **resolved** — #86 merged, #87/#88 closed      | `gh pr view`                                 |

**Landed this session:** #106 (the durable half PR #105 never filed), #108 (skill-lane citations
+ Gate-1 reading-order hazard), #109 (enforcement census + disclosure-fence root cause + guard
sink pin). Issue #89 corrected by measurement — all three ACs met.

**Nothing is complete-and-visible in the product sense.** This repo's product is *distributed
artifact correctness*; no correction reaches a consumer until loom ingests. 84 entries are queued.

---

## 2. ETA to completion

**Locally actionable: 2–3 autonomous cycles.** Basis: G10 + G5 (one cycle — `.github/` is
durable), then G11 + the four remaining red runners (1–2 cycles, diagnose-and-file only).

**Everything else: unbounded, gated on loom Gate-1, outside this repo.** Not a work estimate —
a dependency. Stating a number for it would be fiction.

---

## 3. Prioritized immediate queue — BUG + INVEST-NOW

| #   | Item                                                                       | Cat        | Durable here? | Implication                                                             |
| --- | -------------------------------------------------------------------------- | ---------- | ------------- | ----------------------------------------------------------------------- |
| 1   | **G10** Gate A never runs on a stacked PR (`branches:[main]`)              | BUG        | **YES**       | A PR can merge with the required check never having run. Hit this today. |
| 2   | **G5** Gate A has no coverage for sync-**ADDED** content                   | INVEST-NOW | **YES**       | The `.claude/VERSION` injection HIGH would pass it.                      |
| 3   | **G12** Disclosure fence's 3 false-negative locks structurally empty        | BUG        | no — loom     | home-path / hostname / service-label locks cannot fail. Filed #109.      |
| 4   | **G11** `validate-proximity-band` genuinely hangs (>151s)                  | BUG        | diagnose only | Only proven-red runner with no known cause.                              |
| 5   | **G8** 4 red runners: codex-dispatcher, strip-build-internal, validate-emit, variant-overlay(78) | BUG | diagnose only | Enforcement instruments failing.                          |
| 6   | **G9/F36** 4 runners import bin modules absent at this tier                | BUG        | no — loom     | Cannot execute at any consumer. #79's class, 3rd instance.               |
| 7   | **F24/#79** Dangling authority in a `priority:0` baseline                  | BUG        | no — loom     | Loads every session, every consumer.                                     |
| 8   | **#78** `.coc-obsoleted` purge deletes shipped files                       | BUG        | stopgap only  | Damage caused by *compliance*, not neglect.                              |
| 9   | **#64** `/sync-from-template` silently discards consumer work              | BUG        | fixed, Class-A| Silent, unrecoverable loss.                                              |
| 10  | **G6** Action text never enters the authz decision                         | BUG        | no — loom     | Condition 5 attested, structurally unenforced.                           |
| 11  | codex-mcp-guard pollutes the live `violations.jsonl` (~10 rows/run)        | INVEST-NOW | no — loom     | Corrupts trust-posture input + codify backlog. Sink pinned in #109.      |

**8 of 11 are loom-blocked.** Only #1 and #2 are durably fixable in this repo — `.github/` carries
exactly one synced path (`publish-dev-image.yml`); verified the reverting sync touched 113 files
and **zero** under `.github/`.

---

## 4. Deferred-quality backlog

**Empty.** Zero issues carry the `deferred-quality` label, though the label exists.

That is itself the finding. **G7** ("four small drops left unrestored — deliberate, leave to
loom") is an INCREMENTAL defer living only in a session-notes line. Per
`product-completion-first.md` MUST-2 an INCREMENTAL defer needs the four generalized-1b
conditions on a tracked issue: blocking-safety note, value-anchor, full-fix acceptance criteria,
revisit trigger. Untracked, it is a **silent defer** — BLOCKED, not because the judgment is
wrong but because nothing will ever re-surface it.

**Sweep-N revisit:** nothing to re-surface — nothing was ever tracked.

---

## 5. Decision points

### D1 — Fix the two `.github/` gate defects now, or keep filing proposals?

**Recommend: fix them now.** They are the only substantial work here whose fix survives a sync,
and G10 is a gate-integrity defect demonstrated by tripping it in this session.

**Cons, honestly:** it widens `.github/` divergence from whatever loom eventually ships; and G5
is a genuine design question (what *should* Gate A do about sync-added content?) that may deserve
loom's input rather than a local answer. G10 carries no such ambiguity.

### D2 — File G7 as a `deferred-quality` issue, or drop it?

**Recommend: file it.** One issue carrying the four conditions.
**Con:** it is genuinely 1–7 lines of prose across four files and may never be worth picking up —
but that is what the revisit trigger is for, and untracked is the one disposition the rule forbids.

### D3 — Keep producing proposal entries?

**Recommend: no — stop.** Three waves landed today; the queue is 84 and the bottleneck is ingest,
not input.
**Con:** findings are cheapest to record while context is warm, and a session boundary loses it.
Judged outweighed — the filed entries are unread, and the specifics are in the session notes.

---

## 6. Recommendation

1. **Fix G10** — narrow, unambiguous, durable. One cycle.
2. **Surface G5** to the owner as a design question rather than answering it locally.
3. **File G7** as the one `deferred-quality` issue.
4. **Diagnose G11 + the 4 red runners**, file findings, fix nothing under `.claude/`.
5. **Do not open a fourth proposal wave.**

---

## Caveats on this report

- **No gate-level review ran on any of this session's three merged PRs.** All five dispatched
  subagents (four recon, one reviewer) went idle without producing verdicts and were stopped.
  Every claim was verified directly with quoted command output — which is not the review gate,
  and is recorded as such rather than allowed to read as clean.
- **Census instrument:** runners were driven individually with exit codes read directly, never
  through a pipe, at a ≥150s cap. A 45s cap scores three green runners red (48s / 72s / 89s).
  Never compare two censuses taken at different caps.
